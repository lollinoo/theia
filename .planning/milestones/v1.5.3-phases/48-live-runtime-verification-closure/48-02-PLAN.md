---
phase: 48-live-runtime-verification-closure
plan: 02
type: execute
wave: 2
depends_on:
  - "48-01"
files_modified:
  - .planning/phases/40-collectors/40-HUMAN-UAT.md
  - .planning/v1.5.3-MILESTONE-AUDIT.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Phase 40 HUMAN-UAT explicitly finalizes the accepted Prometheus `probe_success` environment limitation, with no remaining suggestion that live proof is still pending."
    - "The milestone audit reflects the actual live-runtime state for Phases 40, 42, and 45: resolved debt is removed, while any recorded failed live checks remain explicit."
    - "The refreshed audit still records the remaining non-live debt honestly: planning traceability cleanup and Nyquist validation backfill."
  artifacts:
    - path: ".planning/phases/40-collectors/40-HUMAN-UAT.md"
      provides: "Final collector human-UAT evidence that closes the environment-limited Prometheus proof debt"
      contains: "accepted environment limitation"
    - path: ".planning/v1.5.3-MILESTONE-AUDIT.md"
      provides: "Milestone audit refreshed to remove unresolved live-runtime debt while preserving remaining metadata/Nyquist debt"
      contains: "Human Verification Summary"
  key_links:
    - from: ".planning/phases/40-collectors/40-HUMAN-UAT.md"
      to: ".planning/phases/40-collectors/40-VERIFICATION.md"
      via: "The final Phase 40 artifact retains the verified hostname/alert enrichment evidence while explicitly classifying `probe_success` absence as an accepted lab limitation."
      pattern: "Live Prometheus Enrichment Probe|accepted environment limitation|probe_success"
    - from: ".planning/v1.5.3-MILESTONE-AUDIT.md"
      to: ".planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md"
      via: "The audit's human-verification summary and tech-debt sections must reflect that Phase 42 no longer has pending live checks."
      pattern: "42-HUMAN-UAT|pending: 0|cutover"
    - from: ".planning/v1.5.3-MILESTONE-AUDIT.md"
      to: ".planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md"
      via: "The audit must stop reporting a missing Phase 45 UAT artifact once the file exists and the live check is recorded."
      pattern: "45-HUMAN-UAT|pending: 0|cadence"
---

<objective>
Finalize the remaining Phase 40 collector proof as accepted human-UAT evidence
and refresh the milestone audit so it reflects the actual Phase 48 live-runtime
outcomes after Plan 48-01 lands.

Purpose:
- Close the last live-runtime documentation gap still attached to Phase 40.
- Reconcile the milestone audit with the now-complete HUMAN-UAT artifacts from
  Phases 40, 42, and 45.
- Preserve failed live checks honestly if any occurred, and preserve the
  remaining non-live debt so milestone archival points to real follow-up work
  instead of stale runtime reminders.

Output:
- `.planning/phases/40-collectors/40-HUMAN-UAT.md` rewritten as final accepted
  limitation evidence instead of unresolved partial follow-up.
- `.planning/v1.5.3-MILESTONE-AUDIT.md` refreshed so stale pending/missing
  live-runtime debt is gone, while any recorded failed live checks and the
  remaining planning/Nyquist debt remain visible.
</objective>

<execution_context>
@/home/azmin/.codex/get-shit-done/workflows/execute-plan.md
@/home/azmin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.5.3-MILESTONE-AUDIT.md
@.planning/phases/40-collectors/40-HUMAN-UAT.md
@.planning/phases/40-collectors/40-VERIFICATION.md
@.planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md
@.planning/phases/44-frontend-integration/44-HUMAN-UAT.md
@.planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md
@.planning/phases/46-detail-delta-gap-closure/46-HUMAN-UAT.md

<interfaces>
Phase 40 final-state requirement from the phase contract:
```md
Phase 40 either captures completed Prometheus enrichment proof or explicitly
records the accepted environment limitation and rationale as final human-UAT
evidence.
```

Stale live-runtime debt wording that must be refreshed after this plan:
```md
- "`40-HUMAN-UAT.md` is environment-limited ..."
- "`42-HUMAN-UAT.md` still has 3 pending ..."
- "`45-VERIFICATION.md` still calls for one live canvas/websocket cadence check, and there is no persisted `45-HUMAN-UAT.md` artifact."
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize Phase 40 collector HUMAN-UAT as accepted limitation evidence</name>
  <files>
    .planning/phases/40-collectors/40-HUMAN-UAT.md
  </files>
  <read_first>
    - .planning/v1.5.3-MILESTONE-AUDIT.md
    - .planning/phases/40-collectors/40-HUMAN-UAT.md
    - .planning/phases/40-collectors/40-VERIFICATION.md
  </read_first>
  <action>
    Convert Phase 40's current partial wording into final accepted evidence
    without inventing new Prometheus proof.

    1. Keep the existing file structure, frontmatter, test headings, and the
       successful live SNMP collector result intact.

    2. Rewrite `## Current Test` so it explicitly says human verification is
       approved for Phase 40 closure, with live SNMP collector proof complete
       and Prometheus `probe_success` reachability remaining an accepted lab
       limitation in this environment.

    3. Rewrite the `### 2. Live Prometheus Enrichment Probe` result so the final
       state is explicit rather than unresolved:
       - keep the observed hostname fallback, explicit-label hostname, and alert
         mapping evidence
       - keep the statement that SNMP remained authoritative for core metrics
         and throughput
       - replace the old `result: partial — ...` wording with one explicit
         final label:
         `result: skipped — accepted environment limitation; hostname fallback, explicit-label hostname mapping, and alert mapping passed, SNMP remained authoritative for core metrics and throughput, and Prometheus exposed no probe_success series in this lab`
       - state plainly that the current Prometheus environment exposes no
         `probe_success` series, so reachability enrichment could not be
         exercised in this lab

    4. Keep summary counts honest and final:
       - `total: 2`
       - `passed: 1`
       - `issues: 0`
       - `pending: 0`
       - `skipped: 1`
       - `blocked: 0`

    5. Replace `## Gaps` with final closure wording:
       `None. Prometheus probe_success absence is an accepted environment limitation for this lab and no longer blocks milestone archival.`
  </action>
  <verify>
    <automated>rtk rg -n 'accepted environment limitation' .planning/phases/40-collectors/40-HUMAN-UAT.md && rtk rg -n 'result: skipped — accepted environment limitation' .planning/phases/40-collectors/40-HUMAN-UAT.md && rtk rg -n 'pending: 0' .planning/phases/40-collectors/40-HUMAN-UAT.md && ! rtk rg -n 'result: partial —|could not be validated during this phase' .planning/phases/40-collectors/40-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n 'accepted environment limitation' .planning/phases/40-collectors/40-HUMAN-UAT.md` returns at least 2 matches
    - `rtk rg -n 'result: skipped — accepted environment limitation' .planning/phases/40-collectors/40-HUMAN-UAT.md` returns 1 match
    - `rtk rg -n 'pending: 0' .planning/phases/40-collectors/40-HUMAN-UAT.md` returns 1 match
    - `rtk rg -n 'skipped: 1' .planning/phases/40-collectors/40-HUMAN-UAT.md` returns 1 match
    - `! rtk rg -n 'result: partial —|could not be validated during this phase' .planning/phases/40-collectors/40-HUMAN-UAT.md`
    - `rtk rg -n 'SNMP remained authoritative for core metrics and throughput|probe_success' .planning/phases/40-collectors/40-HUMAN-UAT.md` returns matches
    - `rtk rg -n 'accepted environment limitation' .planning/phases/40-collectors/40-HUMAN-UAT.md && rtk rg -n 'result: skipped — accepted environment limitation' .planning/phases/40-collectors/40-HUMAN-UAT.md && rtk rg -n 'pending: 0' .planning/phases/40-collectors/40-HUMAN-UAT.md && ! rtk rg -n 'result: partial —|could not be validated during this phase' .planning/phases/40-collectors/40-HUMAN-UAT.md` exits 0
  </acceptance_criteria>
  <done>
    Phase 40's HUMAN-UAT artifact now records the Prometheus limitation as final
    accepted evidence instead of unresolved runtime debt.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refresh the milestone audit so unresolved live-runtime debt is gone</name>
  <files>
    .planning/v1.5.3-MILESTONE-AUDIT.md
  </files>
  <read_first>
    - .planning/v1.5.3-MILESTONE-AUDIT.md
    - .planning/phases/40-collectors/40-HUMAN-UAT.md
    - .planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md
    - .planning/phases/44-frontend-integration/44-HUMAN-UAT.md
    - .planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md
    - .planning/phases/46-detail-delta-gap-closure/46-HUMAN-UAT.md
  </read_first>
  <action>
    Refresh `.planning/v1.5.3-MILESTONE-AUDIT.md` so it reflects the finalized
    Phase 40 artifact and the actual recorded Phase 42/45 live outcomes.

    1. Update the frontmatter `audited:` timestamp to the current UTC time.

    2. Remove or rewrite only the stale live-runtime debt entries from
       frontmatter:
       - delete the Phase 40 `tech_debt` item after `40-HUMAN-UAT.md` is
         finalized as an accepted limitation
       - delete or rewrite the Phase 42 and Phase 45 `tech_debt` items so they
         match the recorded HUMAN-UAT outcomes from Plan 48-01:
         - if a phase has no failed live checks, remove its stale debt item
         - if a phase has any failed live checks, keep a debt item but rewrite
           it to describe the recorded failure instead of saying the checks are
           pending or the artifact is missing
       - delete the `documentation_gaps` item about the missing
         `45-HUMAN-UAT.md` artifact
       - keep the remaining planning-docs and Nyquist items untouched

    3. In `## Requirements Coverage`, update the `PIPE-01` note so it points to
       the accepted limitation wording in `40-HUMAN-UAT.md` instead of saying
       live proof is still limited.

    4. In `## Phase Verification Summary`, rewrite the Phase 40, Phase 42, and
       Phase 45 rows so they no longer describe pending human/runtime work:
       - Phase 40 should say the accepted limitation is finalized
       - Phase 42 and Phase 45 should say the HUMAN-UAT artifacts now exist and
         summarize the recorded outcome honestly
       - if a phase passed its live checks, say the artifact is complete
       - if a phase failed any live checks, say the artifact records the failure
         and the runtime debt remains open
       - keep the other phase rows unchanged unless a wording fix is necessary
         for consistency

    4a. Normalize every stale `human_needed` status contradicted by finalized
        HUMAN-UAT artifacts:
        - Phase 44 and Phase 46 already have completed HUMAN-UAT artifacts with
          `pending: 0`; their `## Phase Verification Summary` rows must no
          longer use `human_needed`
        - if Phase 40, 42, or 45 also have finalized HUMAN-UAT artifacts with
          `pending: 0`, do not leave them as `human_needed` either
        - use status/note text that matches the actual recorded outcome:
          complete/closed when there are no failed live checks, or explicit
          recorded-runtime-failure wording when failures remain

    5. Rewrite `## Human Verification Summary` so it states:
       - Phase 40 now has `passed: 1`, `skipped: 1`, `pending: 0`, with the
         skipped probe check accepted as an environment limitation
       - Phase 42 now reports the actual `passed`, `issues`, and `pending: 0`
         counts from `42-HUMAN-UAT.md`
       - Phase 45 now reports the actual `passed`, `issues`, and `pending: 0`
         counts from `45-HUMAN-UAT.md`
       - if both phases have zero failed live checks, say live-runtime/UAT debt
         is closed
       - otherwise say live-runtime evidence is now recorded but failed live
         checks remain open debt
       - remaining debt is planning traceability cleanup plus Nyquist coverage

    6. Remove or rewrite any prose in `## Tech Debt By Phase`,
       `## Audit Verdict`, the top-of-report `**Status:**` line, and the
       post-table `## E2E Flow Status` caveat sentence that still claims:
       - unresolved Phase 40 live proof debt
       - Phase 42 pending live smoke tests
       - Phase 45 missing live cadence proof
       - milestone-level live-runtime/UAT sign-off remains incomplete because
         evidence is missing rather than because failed runtime checks were
         actually recorded
       - generic wording such as `remaining debt is live verification` or
         `The remaining caveat is live-environment sign-off` after the actual
         42/45 outcomes are known
       - if Phase 42 and Phase 45 have no failed live checks, rewrite those
         lines to closed-runtime-debt wording
       - if either phase records a failed live check, rewrite those lines to
         recorded-failure wording instead of generic pending-signoff wording

    6a. Recompute every aggregate summary line affected by the live-runtime
        refresh, especially the `## Tech Debt By Phase` `**Total:** ...` line,
        so the final item count and phase count match the debt bullets and
        remaining `### Phase ...` sections that still exist after stale
        runtime-only debt is removed or rewritten.

    7. Keep the audit status as `tech_debt` unless some other remaining section
       proves the milestone is fully clean. This plan only closes the
       live-runtime debt, not the remaining metadata or Nyquist work.
  </action>
  <verify>
    <automated>! rtk rg -n '42-HUMAN-UAT\\.md still has 3 pending|no persisted .*45-HUMAN-UAT\\.md.* artifact|remaining debt is live verification|The remaining caveat is live-environment sign-off|\\| 40 - Collectors \\| human_needed \\||\\| 42 - Pipeline Orchestrator & Cutover \\| human_needed \\||\\| 44 - Frontend Integration \\| human_needed \\||\\| 45 - Polling Cadence Gap Closure \\| human_needed \\||\\| 46 - Detail Delta Gap Closure \\| human_needed \\|' .planning/v1.5.3-MILESTONE-AUDIT.md && rtk node -e "const fs=require('fs'); const audit=fs.readFileSync('.planning/v1.5.3-MILESTONE-AUDIT.md','utf8'); const u42=fs.readFileSync('.planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md','utf8'); const u45=fs.readFileSync('.planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md','utf8'); const failed=/^result: failed — /m.test(u42)||/^result: failed — /m.test(u45); const tech=((audit.split('## Tech Debt By Phase')[1]||'').split('\\n---')[0]||''); const itemCount=(tech.match(/^- /gm)||[]).length; const phaseCount=(tech.match(/^### Phase /gm)||[]).length; const expected='**Total:** '+itemCount+' debt items across '+phaseCount+' phases plus planning metadata and validation coverage.'; if(!/accepted environment limitation/.test(audit)) throw new Error('missing phase 40 closure'); if(!/planning traceability/.test(audit)) throw new Error('missing planning traceability debt'); if(!/Nyquist/.test(audit)) throw new Error('missing Nyquist debt'); if(!tech) throw new Error('missing tech debt section'); if(/42-HUMAN-UAT\\.md still has 3 pending|no persisted .*45-HUMAN-UAT\\.md.* artifact|remaining debt is live verification|The remaining caveat is live-environment sign-off/.test(audit)) throw new Error('stale live-runtime prose'); if(/\\| 40 - Collectors \\| human_needed \\||\\| 42 - Pipeline Orchestrator & Cutover \\| human_needed \\||\\| 44 - Frontend Integration \\| human_needed \\||\\| 45 - Polling Cadence Gap Closure \\| human_needed \\||\\| 46 - Detail Delta Gap Closure \\| human_needed \\|/.test(audit)) throw new Error('stale human_needed'); if(!tech.includes(expected)) throw new Error('bad tech debt total: '+expected); if(failed){ if(!/live-runtime evidence is now recorded but failed live checks remain open debt|runtime debt remains open due to recorded failed live checks|recorded failed live checks keep runtime debt open/.test(audit)) throw new Error('missing failure branch wording'); } else { if(!/live-runtime\\/UAT debt is closed|live-runtime debt is closed/.test(audit)) throw new Error('missing closed wording'); if(/runtime debt remains open due to recorded failed live checks|recorded failed live checks keep runtime debt open/.test(audit)) throw new Error('stale fail wording'); }"</automated>
  </verify>
  <acceptance_criteria>
    - `! rtk rg -n '42-HUMAN-UAT\\.md still has 3 pending' .planning/v1.5.3-MILESTONE-AUDIT.md`
    - `! rtk rg -n 'no persisted .*45-HUMAN-UAT\\.md.* artifact' .planning/v1.5.3-MILESTONE-AUDIT.md`
    - `! rtk rg -n 'remaining debt is live verification' .planning/v1.5.3-MILESTONE-AUDIT.md`
    - `! rtk rg -n 'The remaining caveat is live-environment sign-off' .planning/v1.5.3-MILESTONE-AUDIT.md`
    - `! rtk rg -n '\\| 40 - Collectors \\| human_needed \\||\\| 42 - Pipeline Orchestrator & Cutover \\| human_needed \\||\\| 44 - Frontend Integration \\| human_needed \\||\\| 45 - Polling Cadence Gap Closure \\| human_needed \\||\\| 46 - Detail Delta Gap Closure \\| human_needed \\|' .planning/v1.5.3-MILESTONE-AUDIT.md`
    - `rtk rg -n 'accepted environment limitation' .planning/v1.5.3-MILESTONE-AUDIT.md` returns at least 1 match
    - `rtk rg -n 'planning traceability' .planning/v1.5.3-MILESTONE-AUDIT.md` returns at least 1 match
    - `rtk rg -n 'Nyquist' .planning/v1.5.3-MILESTONE-AUDIT.md` returns at least 1 match
    - `rtk node -e "const fs=require('fs'); const audit=fs.readFileSync('.planning/v1.5.3-MILESTONE-AUDIT.md','utf8'); const u42=fs.readFileSync('.planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md','utf8'); const u45=fs.readFileSync('.planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md','utf8'); const failed=/^result: failed — /m.test(u42)||/^result: failed — /m.test(u45); const tech=((audit.split('## Tech Debt By Phase')[1]||'').split('\\n---')[0]||''); const itemCount=(tech.match(/^- /gm)||[]).length; const phaseCount=(tech.match(/^### Phase /gm)||[]).length; const expected='**Total:** '+itemCount+' debt items across '+phaseCount+' phases plus planning metadata and validation coverage.'; if(/42-HUMAN-UAT\\.md still has 3 pending|no persisted .*45-HUMAN-UAT\\.md.* artifact|remaining debt is live verification|The remaining caveat is live-environment sign-off/.test(audit)) throw new Error('stale debt'); if(/\\| 40 - Collectors \\| human_needed \\||\\| 42 - Pipeline Orchestrator & Cutover \\| human_needed \\||\\| 44 - Frontend Integration \\| human_needed \\||\\| 45 - Polling Cadence Gap Closure \\| human_needed \\||\\| 46 - Detail Delta Gap Closure \\| human_needed \\|/.test(audit)) throw new Error('stale human_needed'); if(!/accepted environment limitation/.test(audit)) throw new Error('missing phase 40 closure'); if(!/planning traceability/.test(audit)) throw new Error('missing planning traceability debt'); if(!/Nyquist/.test(audit)) throw new Error('missing Nyquist debt'); if(!tech.includes(expected)) throw new Error('bad tech debt total: '+expected); if(failed){ if(!/live-runtime evidence is now recorded but failed live checks remain open debt|runtime debt remains open due to recorded failed live checks|recorded failed live checks keep runtime debt open/.test(audit)) throw new Error('missing failure branch wording'); } else if(!/live-runtime\\/UAT debt is closed|live-runtime debt is closed/.test(audit)) throw new Error('missing closed wording');"` exits 0
  </acceptance_criteria>
  <done>
    The milestone audit no longer reports stale pending/missing live-runtime
    debt and now reflects the actual recorded runtime outcome while still
    pointing to the remaining planning-traceability and Nyquist follow-up work.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| finalized HUMAN-UAT artifacts -> milestone audit | Audit conclusions are only as accurate as the UAT artifacts they cite |
| accepted environment limitation -> milestone archive | A skipped lab-only limitation must not be misrepresented as unresolved product debt or as completed live proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-02-01 | T | `.planning/v1.5.3-MILESTONE-AUDIT.md` | mitigate | Read the finalized Phase 40, 42, and 45 HUMAN-UAT artifacts first, then remove only the debt lines those artifacts actually close; if any live check failed, preserve that runtime debt explicitly instead of pretending the milestone is clean. |
| T-48-02-02 | R | `.planning/phases/40-collectors/40-HUMAN-UAT.md` | mitigate | Require explicit accepted-limitation wording tied to `probe_success` absence, preserved hostname/alert evidence, and SNMP authority so later readers can see exactly why the skipped check is not unresolved debt. |
| T-48-02-03 | I | audit and UAT prose | mitigate | Limit edits to device-facing behavior, summary counts, and localhost-oriented runtime notes; do not copy credentials, tokens, or non-essential raw logs into archival markdown. |
</threat_model>

<verification>
- `rtk rg -n 'accepted environment limitation' .planning/phases/40-collectors/40-HUMAN-UAT.md`
- `rtk rg -n 'pending: 0' .planning/phases/40-collectors/40-HUMAN-UAT.md`
- `rtk node -e "const fs=require('fs'); const audit=fs.readFileSync('.planning/v1.5.3-MILESTONE-AUDIT.md','utf8'); const u42=fs.readFileSync('.planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md','utf8'); const u45=fs.readFileSync('.planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md','utf8'); const failed=/^result: failed — /m.test(u42)||/^result: failed — /m.test(u45); const tech=((audit.split('## Tech Debt By Phase')[1]||'').split('\\n---')[0]||''); const itemCount=(tech.match(/^- /gm)||[]).length; const phaseCount=(tech.match(/^### Phase /gm)||[]).length; const expected='**Total:** '+itemCount+' debt items across '+phaseCount+' phases plus planning metadata and validation coverage.'; if(/42-HUMAN-UAT\\.md still has 3 pending|no persisted .*45-HUMAN-UAT\\.md.* artifact|remaining debt is live verification|The remaining caveat is live-environment sign-off/.test(audit)) throw new Error('stale debt'); if(!/accepted environment limitation/.test(audit)) throw new Error('missing phase 40 closure'); if(!/planning traceability/.test(audit)) throw new Error('missing planning traceability debt'); if(!/Nyquist/.test(audit)) throw new Error('missing Nyquist debt'); if(!tech.includes(expected)) throw new Error('bad tech debt total: '+expected); if(failed){ if(!/live-runtime evidence is now recorded but failed live checks remain open debt|runtime debt remains open due to recorded failed live checks|recorded failed live checks keep runtime debt open/.test(audit)) throw new Error('missing failure branch wording'); } else if(!/live-runtime\\/UAT debt is closed|live-runtime debt is closed/.test(audit)) throw new Error('missing closed wording'); }"` 
</verification>

<success_criteria>
- `40-HUMAN-UAT.md` explicitly records the accepted Prometheus limitation with `pending: 0`
- `v1.5.3-MILESTONE-AUDIT.md` stops reporting stale pending/missing runtime debt and reflects the actual recorded Phase 42/45 outcome honestly
- The audit still points clearly to the remaining planning metadata and Nyquist work instead of pretending the milestone is fully clean
</success_criteria>

<output>
After completion, create `.planning/phases/48-live-runtime-verification-closure/48-02-SUMMARY.md`
</output>
