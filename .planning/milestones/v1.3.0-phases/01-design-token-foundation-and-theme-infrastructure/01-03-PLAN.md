---
phase: 01-design-token-foundation-and-theme-infrastructure
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - frontend/src/components/Canvas.tsx
  - frontend/src/components/LinkEdge.tsx
  - frontend/src/components/DeviceCard.tsx
  - frontend/src/components/icons/DeviceIcon.tsx
  - frontend/src/types/metrics.ts
  - frontend/src/components/InterfaceStatsPanel.tsx
  - frontend/src/components/LinkDetailsPanel.tsx
  - frontend/src/components/LinkCreatePanel.tsx
autonomous: true
requirements:
  - FOUND-06

must_haves:
  truths:
    - "User sees all status indicators, link colors, and device cards change appearance correctly when switching between dark and light themes"
    - "No hardcoded hex color values remain in any component file"
    - "All status colors use var(--color-status-*) references that respond to theme switching"
    - "SVG icons use currentColor instead of hardcoded fill colors"
  artifacts:
    - path: "frontend/src/components/Canvas.tsx"
      provides: "10 hex values replaced with CSS variable references"
      min_lines: 30
    - path: "frontend/src/components/LinkEdge.tsx"
      provides: "15 hex values replaced with CSS variable references"
      min_lines: 30
    - path: "frontend/src/components/DeviceCard.tsx"
      provides: "4 hex values replaced with Tailwind token classes"
      min_lines: 30
    - path: "frontend/src/types/metrics.ts"
      provides: "3 hex values replaced with CSS variable references"
      contains: "var(--color-status"
  key_links:
    - from: "frontend/src/types/metrics.ts"
      to: "frontend/src/index.css"
      via: "CSS variable references resolve against @theme token definitions"
      pattern: "var\\(--color-status"
    - from: "frontend/src/components/Canvas.tsx"
      to: "frontend/src/index.css"
      via: "CSS variable references for status colors, outline, surface"
      pattern: "var\\(--"
    - from: "frontend/src/components/DeviceCard.tsx"
      to: "frontend/src/index.css"
      via: "Tailwind token classes (bg-surface, bg-bg, bg-on-bg-secondary)"
      pattern: "bg-surface|bg-bg|bg-on-bg"
---

<objective>
Replace all 45 hardcoded hex color values across 8 source files with CSS variable token references or Tailwind token classes.

Purpose: This is the final step to make the entire UI theme-responsive. Without this, toggling themes would change backgrounds and text but leave status indicators, SVG icons, canvas elements, and link colors stuck on the old dark-theme values. This completes FOUND-06.

Output: Zero hardcoded hex colors in any component file. Every color reference resolves through the CSS token system defined in Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-token-foundation-and-theme-infrastructure/01-CONTEXT.md
@.planning/phases/01-design-token-foundation-and-theme-infrastructure/01-RESEARCH.md
@.planning/phases/01-design-token-foundation-and-theme-infrastructure/01-01-SUMMARY.md
@.planning/phases/01-design-token-foundation-and-theme-infrastructure/01-02-SUMMARY.md

<interfaces>
<!-- Token system from Plan 01 index.css provides these CSS variables -->
Status color tokens (theme-invariant, same value both themes per D-08):
- `var(--color-status-up)` = #00E676 (per D-09, replaces old #00c853)
- `var(--color-status-down)` = #FF1744 (unchanged hex)
- `var(--color-status-probing)` = #FFEA00 (replaces old #ffc107)
- `var(--color-status-unknown)` = #9E9E9E (replaces old #657786)

Surface tokens (theme-responsive via --nt-* primitives):
- `var(--nt-surface)` = dark: #1E1E21, light: #EDEDF0 (replaces old #363647)
- `var(--nt-bg)` = dark: #161618, light: #F5F5F7 (replaces old #2d2d3d)
- `var(--nt-surface-high)` = dark: #262629, light: #E4E4E8
- `var(--nt-elevated)` = dark: #2E2E32, light: #FFFFFF (replaces old #3f3f53)
- `var(--color-outline)` = var(--nt-outline) = dark: #333338, light: #D0D0D8 (replaces old #4a4a5e)

Text tokens:
- `var(--nt-on-bg)` = dark: #F5F5F7, light: #1A1A1C
- `var(--nt-on-bg-secondary)` = dark: #8899A6, light: #5A5A6E (replaces old #8899a6)
- `var(--nt-on-bg-muted)` = dark: #657786, light: #8A8A93 (replaces old #657786 and #666)

Tailwind utility classes (generated by @theme inline):
- `bg-surface`, `bg-bg`, `bg-elevated`, `bg-surface-high`
- `text-on-bg`, `text-on-bg-secondary`, `text-on-bg-muted`
- `border-outline`, `border-outline-subtle`
- `bg-on-bg-secondary` (for secondary-colored backgrounds like handles)

<!-- From Plan 02: ReactFlow imports already migrated to @xyflow/react -->
All files in this plan already have @xyflow/react imports (migrated in Plan 02).
Canvas.tsx already has useTheme() and colorMode={resolvedTheme} wired.

<!-- Hardcoded hex audit from research: 45 occurrences across 8 files -->
Audit reference: 01-RESEARCH.md "Hardcoded Hex Audit" section
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded hex values in Canvas.tsx and LinkEdge.tsx</name>
  <files>frontend/src/components/Canvas.tsx, frontend/src/components/LinkEdge.tsx</files>
  <read_first>
    - frontend/src/components/Canvas.tsx (full file -- has 10 hex values spread across ~1300 lines)
    - frontend/src/components/LinkEdge.tsx (full file -- has 15 hex values in status/utilization color logic)
    - .planning/phases/01-design-token-foundation-and-theme-infrastructure/01-RESEARCH.md (Hardcoded Hex Audit section for exact line-by-line replacement map; Pitfall 3 for JS var() pattern)
  </read_first>
  <action>
    **Canvas.tsx replacements (10 occurrences):**

    All replacements use `var(--color-*)` or `var(--nt-*)` CSS variable syntax in JavaScript string values. This works because React applies these as inline style values, and the browser resolves the CSS variable at render time.

    Status color function returns (typically in a `getStatusColor` or similar function):
    - `'#00c853'` -> `'var(--color-status-up)'` (also fixes D-09: new green)
    - `'#ff1744'` -> `'var(--color-status-down)'`
    - `'#ffc107'` -> `'var(--color-status-probing)'` (also fixes D-10: new yellow #FFEA00)
    - `'#657786'` -> `'var(--color-status-unknown)'` (also fixes D-10: new gray #9E9E9E)

    Canvas infrastructure colors:
    - Connection line `stroke: '#4a4a5e'` -> `stroke: 'var(--color-outline)'`
    - Background dots `color="#3f3f53"` -> `color="var(--nt-outline)"` (Note: Background component `color` prop accepts CSS values)
    - MiniMap `nodeColor` callbacks returning `'#ff1744'` -> `'var(--color-status-down)'`
    - MiniMap `nodeColor` callbacks returning `'#ffc107'` -> `'var(--color-status-probing)'`
    - MiniMap `style` or props with `'#363647'` -> `'var(--nt-surface)'` (background)
    - MiniMap border/mask with `'#4a4a5e'` -> `'var(--color-outline)'`

    **LinkEdge.tsx replacements (15 occurrences):**

    This file has conditional logic that assigns `strokeColor` based on alert status and utilization. Every hex color return must be replaced:

    - All `'#ff1744'` (appears ~4 times) -> `'var(--color-status-down)'`
    - All `'#ffc107'` (appears ~5 times) -> `'var(--color-status-probing)'`
    - All `'#00c853'` (appears ~1 time) -> `'var(--color-status-up)'`
    - `'#4a4a5e'` (appears ~2 times) -> `'var(--color-outline)'`
    - `'#8899a6'` (appears ~1 time) -> `'var(--nt-on-bg-secondary)'`
    - `'#7a7a9e'` (appears ~1 time, hover variant of outline) -> `'var(--nt-on-bg-muted)'`

    Also check for the `utilizationColor(utilization)` function call -- this function is in `types/metrics.ts` and will be fixed in Task 2. If `utilizationColor` returns a hex, do NOT fix it here; it's fixed in Task 2.

    The `utilization === null ? '#4a4a5e' : utilizationColor(utilization)` pattern: replace the `'#4a4a5e'` fallback with `'var(--color-outline)'`.

    **IMPORTANT:** After making all replacements, search the entire Canvas.tsx and LinkEdge.tsx for any remaining `#[0-9a-fA-F]{3,8}` patterns. If any hex colors remain that are NOT inside comments, they must also be replaced. The audit may have missed edge cases.
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia/frontend && grep -nE "'#[0-9a-fA-F]{3,8}'" src/components/Canvas.tsx src/components/LinkEdge.tsx | grep -v "^.*:.*//.*#" && echo "FAIL: hex values still present" || echo "OK: no hardcoded hex in Canvas/LinkEdge"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/Canvas.tsx does NOT contain `'#00c853'`, `'#ff1744'`, `'#ffc107'`, `'#657786'`, `'#4a4a5e'`, `'#3f3f53'`, `'#363647'`
    - frontend/src/components/Canvas.tsx contains `'var(--color-status-up)'`
    - frontend/src/components/Canvas.tsx contains `'var(--color-status-down)'`
    - frontend/src/components/Canvas.tsx contains `'var(--color-outline)'`
    - frontend/src/components/Canvas.tsx contains `'var(--nt-surface)'`
    - frontend/src/components/LinkEdge.tsx does NOT contain `'#ff1744'`, `'#ffc107'`, `'#00c853'`, `'#4a4a5e'`, `'#8899a6'`, `'#7a7a9e'`
    - frontend/src/components/LinkEdge.tsx contains `'var(--color-status-down)'`
    - frontend/src/components/LinkEdge.tsx contains `'var(--color-status-probing)'`
    - frontend/src/components/LinkEdge.tsx contains `'var(--color-outline)'`
    - Zero hex color strings (matching `'#[0-9a-fA-F]{3,8}'`) remain in either file outside of comments
  </acceptance_criteria>
  <done>All 25 hardcoded hex values in Canvas.tsx (10) and LinkEdge.tsx (15) replaced with CSS variable references. Status colors use var(--color-status-*), surface/outline colors use var(--nt-*) or var(--color-*).</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded hex values in DeviceCard, DeviceIcon, metrics.ts, and remaining panels</name>
  <files>frontend/src/components/DeviceCard.tsx, frontend/src/components/icons/DeviceIcon.tsx, frontend/src/types/metrics.ts, frontend/src/components/InterfaceStatsPanel.tsx, frontend/src/components/LinkDetailsPanel.tsx, frontend/src/components/LinkCreatePanel.tsx</files>
  <read_first>
    - frontend/src/components/DeviceCard.tsx (4 hex values in Tailwind arbitrary value classes)
    - frontend/src/components/icons/DeviceIcon.tsx (7 hex values in SVG fill/stroke attributes)
    - frontend/src/types/metrics.ts (3 hex values in color return functions -- look for utilizationColor or similar)
    - frontend/src/components/InterfaceStatsPanel.tsx (1 hex value)
    - frontend/src/components/LinkDetailsPanel.tsx (1 hex value)
    - frontend/src/components/LinkCreatePanel.tsx (1 hex value)
    - .planning/phases/01-design-token-foundation-and-theme-infrastructure/01-RESEARCH.md (Hardcoded Hex Audit section for exact replacements)
  </read_first>
  <action>
    **DeviceCard.tsx replacements (4 occurrences):**

    These are Tailwind arbitrary value classes that must be replaced with token utility classes:

    1. `!bg-[#8899a6]` -> `!bg-on-bg-secondary` (Note: Tailwind v4 moved `!` to end of class: `bg-on-bg-secondary!`. However, check if the `!` is even needed after the token migration. If the Handle component's default background is overridden by the token, `!` may be unnecessary. Keep `!` only if the override is still required.)
    2. `bg-[#1a1a24]` -> `bg-surface` (card header background -- maps to dark #1E1E21 which is close enough; the new design system surface is the correct semantic choice)
    3. `bg-[#12121a]` -> `bg-bg` (card body background -- maps to dark #161618)
    4. `bg-[#8899a6]/50` -> `bg-on-bg-muted/50` (placeholder handle)

    **Also update any remaining old Tailwind token classes in DeviceCard.tsx:**
    - `text-text-primary` -> `text-on-bg`
    - `text-text-secondary` -> `text-on-bg-secondary`
    - `border-border-subtle` -> `border-outline`
    - `bg-bg-canvas` -> `bg-bg`
    - `bg-bg-surface` -> `bg-surface`
    - `bg-bg-elevated` -> `bg-elevated`
    - `text-accent` -> `text-primary`
    - `bg-status-up` -> `bg-status-up` (unchanged)

    **DeviceIcon.tsx replacements (7 occurrences):**

    All SVG `fill` and `stroke` attributes using `#2d2d3d` (old canvas background) should be replaced with `currentColor`. This makes the icon inherit its color from the parent's text color, which is set by the theme via CSS tokens.

    - `fill="#2d2d3d"` -> `fill="currentColor"` (all instances)
    - `stroke="#2d2d3d"` -> `stroke="currentColor"` (all instances)
    - Any other hardcoded SVG colors should also become `currentColor` or a CSS variable

    **metrics.ts replacements (3 occurrences):**

    This file has a `utilizationColor` function (or similar) that returns hex colors:
    - `'#ff1744'` -> `'var(--color-status-down)'`
    - `'#ffc107'` -> `'var(--color-status-probing)'`
    - `'#00c853'` -> `'var(--color-status-up)'`

    These returned values are used in inline `style` props, so `var()` references work correctly.

    **InterfaceStatsPanel.tsx replacement (1 occurrence):**
    - `'#657786'` -> `'var(--color-status-unknown)'`

    **LinkDetailsPanel.tsx replacement (1 occurrence):**
    - `color: '#666'` -> `color: 'var(--nt-on-bg-muted)'`

    **LinkCreatePanel.tsx replacement (1 occurrence):**
    - `color: '#666'` -> `color: 'var(--nt-on-bg-muted)'`

    **Also scan ALL 6 files for any old Tailwind token classes** (bg-bg-canvas, text-text-primary, border-border-subtle, text-accent, etc.) and update them to the new names following the same mapping used in NavBar.tsx.

    **Final sweep:** After all replacements, run a grep across the entire `frontend/src/` directory for any remaining hardcoded hex patterns: `grep -rnE "'#[0-9a-fA-F]{3,8}'|\"#[0-9a-fA-F]{3,8}\"" frontend/src/ --include="*.ts" --include="*.tsx"`. Exclude test files and index.css. Fix any remaining occurrences.
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia/frontend && grep -rnE "'#[0-9a-fA-F]{3,8}'|\"#[0-9a-fA-F]{3,8}\"" src/components/DeviceCard.tsx src/components/icons/DeviceIcon.tsx src/types/metrics.ts src/components/InterfaceStatsPanel.tsx src/components/LinkDetailsPanel.tsx src/components/LinkCreatePanel.tsx | grep -v "^.*:.*//.*#" && echo "FAIL: hex values still present" || echo "OK: no hardcoded hex in remaining files" && grep -rnE "bg-\[#|text-\[#" src/components/DeviceCard.tsx && echo "FAIL: arbitrary hex classes remain" || echo "OK: no arbitrary hex Tailwind classes"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/DeviceCard.tsx does NOT contain `bg-[#1a1a24]`, `bg-[#12121a]`, `bg-[#8899a6]`
    - frontend/src/components/DeviceCard.tsx contains `bg-surface` (card header)
    - frontend/src/components/DeviceCard.tsx contains `bg-bg` (card body)
    - frontend/src/components/icons/DeviceIcon.tsx does NOT contain `#2d2d3d`
    - frontend/src/components/icons/DeviceIcon.tsx contains `currentColor`
    - frontend/src/types/metrics.ts does NOT contain `'#ff1744'`, `'#ffc107'`, `'#00c853'`
    - frontend/src/types/metrics.ts contains `var(--color-status-`
    - frontend/src/components/InterfaceStatsPanel.tsx does NOT contain `'#657786'`
    - frontend/src/components/InterfaceStatsPanel.tsx contains `var(--color-status-unknown)`
    - frontend/src/components/LinkDetailsPanel.tsx does NOT contain `'#666'`
    - frontend/src/components/LinkDetailsPanel.tsx contains `var(--nt-on-bg-muted)`
    - frontend/src/components/LinkCreatePanel.tsx does NOT contain `'#666'`
    - frontend/src/components/LinkCreatePanel.tsx contains `var(--nt-on-bg-muted)`
    - Zero hex color strings remain across all 6 files outside of comments
  </acceptance_criteria>
  <done>All 17 hardcoded hex values replaced across DeviceCard.tsx (4), DeviceIcon.tsx (7), metrics.ts (3), InterfaceStatsPanel.tsx (1), LinkDetailsPanel.tsx (1), LinkCreatePanel.tsx (1). Zero hardcoded hex colors remain in any non-test source file.</done>
</task>

<task type="auto">
  <name>Task 3: Update remaining Tailwind class names across all component files and verify build</name>
  <files>frontend/src/components/Canvas.tsx, frontend/src/components/DeviceCard.tsx, frontend/src/components/LinkEdge.tsx</files>
  <read_first>
    - frontend/src/components/Canvas.tsx (search for old token class names)
    - frontend/src/components/DeviceCard.tsx (search for old token class names)
    - frontend/src/components/LinkEdge.tsx (search for old token class names)
    - .planning/phases/01-design-token-foundation-and-theme-infrastructure/01-RESEARCH.md (Token-to-old-class mapping table)
  </read_first>
  <action>
    **Step 1: Sweep ALL component files** in `frontend/src/` for old Tailwind token class names that no longer exist in the new token system.

    Run a search for these old patterns across the entire frontend/src directory:
    ```
    bg-bg-canvas, bg-bg-surface, bg-bg-elevated
    text-text-primary, text-text-secondary
    border-border-subtle
    text-accent (as a standalone class, not text-accent-purple)
    bg-accent (as a standalone class)
    ```

    For each match, apply the mapping:
    - `bg-bg-canvas` -> `bg-bg`
    - `bg-bg-surface` -> `bg-surface`
    - `bg-bg-elevated` -> `bg-elevated`
    - `text-text-primary` -> `text-on-bg`
    - `text-text-secondary` -> `text-on-bg-secondary`
    - `border-border-subtle` -> `border-outline`
    - `text-accent` -> `text-primary`
    - `bg-accent` -> `bg-primary`
    - `border-accent` -> `border-primary`
    - `text-accent-purple` -> `text-tertiary`
    - `border-accent-purple` -> `border-tertiary`
    - `text-status-up` -> `text-status-up` (unchanged)

    **Do NOT touch** files already fully updated in Plan 02 (NavBar.tsx, App.tsx) unless they have additional old classes.

    **Step 2: Run `cd frontend && npx vite build`** to verify the entire application compiles. Fix any TypeScript or build errors that arise from the ReactFlow v12 migration or token changes.

    **Step 3: Run `cd frontend && npm test`** to verify existing tests still pass. If DeviceCard.test.tsx has assertions against old class names, update those assertions to match new token classes.

    **Step 4: Final comprehensive hex audit.** Run:
    ```bash
    grep -rnE "'#[0-9a-fA-F]{3,8}'|\"#[0-9a-fA-F]{3,8}\"" src/ --include="*.ts" --include="*.tsx" | grep -v "\.test\." | grep -v "node_modules"
    ```
    If any results appear that are NOT in comments, fix them. The goal is zero hardcoded hex colors in production source files.
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia/frontend && grep -rnE "bg-bg-canvas|bg-bg-surface|bg-bg-elevated|text-text-primary|text-text-secondary|border-border-subtle" src/ --include="*.tsx" --include="*.ts" | grep -v "\.test\." | grep -v node_modules && echo "FAIL: old Tailwind classes remain" || echo "OK: all old classes migrated"</automated>
    <automated>cd /home/azmin/projects/theia/frontend && npx vite build 2>&1 | tail -3</automated>
    <automated>cd /home/azmin/projects/theia/frontend && npm test 2>&1 | tail -5</automated>
    Note: Build may take 15-30 seconds and tests ~15 seconds. These are run as separate verification steps to isolate failure causes.
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `bg-bg-canvas`, `bg-bg-surface`, `bg-bg-elevated`, `text-text-primary`, `text-text-secondary`, `border-border-subtle` in any non-test .tsx/.ts file
    - `npx vite build` exits with code 0 (successful build)
    - `npm test` exits with code 0 (all tests pass)
    - Zero hardcoded hex color strings in non-test, non-CSS source files (verified by grep)
  </acceptance_criteria>
  <done>All old Tailwind token classes migrated across entire frontend. Build succeeds. Tests pass. Zero hardcoded hex colors remain in production source files. FOUND-06 is complete.</done>
</task>

</tasks>

<verification>
1. `grep -rnE "'#[0-9a-fA-F]{3,8}'|\"#[0-9a-fA-F]{3,8}\"" frontend/src/ --include="*.ts" --include="*.tsx" | grep -v "\.test\." | grep -v "//.*#"` returns zero matches
2. `grep -rnE "bg-bg-canvas|bg-bg-surface|bg-bg-elevated|text-text-primary|text-text-secondary|border-border-subtle" frontend/src/ --include="*.tsx" --include="*.ts" | grep -v "\.test\."` returns zero matches
3. `cd frontend && npx vite build` exits 0
4. `cd frontend && npm test` exits 0
</verification>

<success_criteria>
- All 45 hardcoded hex color values replaced with CSS variable references or Tailwind token classes across 8 source files
- All old Tailwind class names (bg-bg-canvas, text-text-primary, etc.) migrated to new token names across all component files
- Status colors updated to new D-09/D-10 values via token system (#00E676, #FFEA00, #9E9E9E)
- SVG icons use currentColor instead of hardcoded fills
- Vite build and all tests pass
- FOUND-06 requirement fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-token-foundation-and-theme-infrastructure/01-03-SUMMARY.md`
</output>
