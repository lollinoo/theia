---
phase: 02-component-restyling
plan: 06
type: execute
wave: 3
depends_on: [02-02,02-04]
files_modified:
  - frontend/src/components/Dashboard.tsx
  - frontend/src/components/Dashboard.test.tsx
  - frontend/src/components/dashboard/DeviceTable.tsx
  - frontend/src/components/dashboard/DeviceRow.tsx
  - frontend/src/components/dashboard/BackupPanel.tsx
  - frontend/src/components/dashboard/BackupHistoryTable.tsx
  - frontend/src/components/dashboard/BulkBackupPanel.tsx
  - frontend/src/components/dashboard/ConfigViewer.tsx
  - frontend/src/components/dashboard/SSHCredentialForm.tsx
  - frontend/src/components/dashboard/VendorSettingsPanel.tsx
  - frontend/src/components/Canvas.tsx
autonomous: true
requirements: [COMP-07, COMP-12, THEME-05]
must_haves:
  truths:
    - "Dashboard uses Outfit for labels and JetBrains Mono for metric columns"
    - "DeviceRow status dots have glow effects matching StatusDot pattern"
    - "Dashboard filter bar has no border-b separator"
    - "DeviceTable header row has no border-b separator"
    - "All dashboard sub-components use surface tiers instead of border layout separators"
    - "No layout border-[tblr] separators remain in any component file (full audit)"
    - "All 25+ components render without errors in both dark and light themes"
  artifacts:
    - path: "frontend/src/components/Dashboard.tsx"
      provides: "Dashboard restyled with Neon Topography typography and no-line rule"
    - path: "frontend/src/components/dashboard/DeviceTable.tsx"
      provides: "DeviceTable with no-line header and JetBrains Mono metric columns"
    - path: "frontend/src/components/dashboard/DeviceRow.tsx"
      provides: "DeviceRow with glow status dots and no-line row separators"
  key_links:
    - from: "frontend/src/components/Dashboard.tsx"
      to: "frontend/src/components/dashboard/DeviceTable.tsx"
      via: "import DeviceTable"
      pattern: "DeviceTable"
    - from: "frontend/src/components/dashboard/DeviceTable.tsx"
      to: "frontend/src/components/dashboard/DeviceRow.tsx"
      via: "import DeviceRow"
      pattern: "DeviceRow"
---

<objective>
Restyle Dashboard, all dashboard sub-components, and Canvas.tsx overlay elements. Perform full no-line rule audit across all component files. Verify THEME-05 (all components correct in both themes).

Purpose: This is the final restyling plan. It completes the Dashboard view, enforces the no-line rule globally, and verifies the phase success criterion of dual-theme correctness.
Output: All components restyled, zero layout border separators remaining, both themes verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-restyling/02-CONTEXT.md
@.planning/phases/02-component-restyling/02-RESEARCH.md
@.planning/phases/02-component-restyling/02-UI-SPEC.md
@.planning/phases/02-component-restyling/02-02-SUMMARY.md
@.planning/phases/02-component-restyling/02-03-SUMMARY.md
@.planning/phases/02-component-restyling/02-04-SUMMARY.md

<interfaces>
From frontend/src/components/dashboard/DeviceRow.tsx (current):
```typescript
// Line 28: `border-b border-outline/50 hover:bg-elevated/50`
// Line 66: `border border-outline` on action button
```

From frontend/src/components/dashboard/DeviceTable.tsx (current):
```typescript
// Line 60: `border-b border-outline text-left text-on-bg-secondary` on thead tr
```

From frontend/src/components/Dashboard.tsx (current):
```typescript
// Line 78: `border-b border-outline bg-surface/50` on filter bar
// Lines 82,94,107,119: form inputs with `border border-outline`
```

UI-SPEC No-Line Rule exceptions (these borders are ALLOWED):
- `ring-1 ring-outline` on DeviceCard default state (ghost border fallback)
- `border border-outline-subtle` on form inputs (ghost border fallback, not layout separator)
- `border border-glass-border` on glassmorphism overlay surfaces (functional, not layout)

UI-SPEC Form Input Contract (for Dashboard filter bar inputs):
`rounded-md border border-outline-subtle bg-elevated px-2 py-1.5 text-xs text-on-bg focus:border-primary focus:ring-1 focus:ring-primary/30 focus:outline-none`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Dashboard, DeviceTable, DeviceRow with typography tokens and no-line rule</name>
  <files>frontend/src/components/Dashboard.tsx, frontend/src/components/Dashboard.test.tsx, frontend/src/components/dashboard/DeviceTable.tsx, frontend/src/components/dashboard/DeviceRow.tsx</files>
  <read_first>
    - frontend/src/components/Dashboard.tsx
    - frontend/src/components/Dashboard.test.tsx
    - frontend/src/components/dashboard/DeviceTable.tsx
    - frontend/src/components/dashboard/DeviceRow.tsx
  </read_first>
  <action>
**Dashboard.tsx:**

1. Line 78: Remove `border-b border-outline` from the filter bar. Change `flex items-center gap-3 px-4 py-3 border-b border-outline bg-surface/50` to `flex items-center gap-3 px-4 py-3 bg-surface/50`. The surface tier contrast (`bg-surface/50` on `bg-bg` canvas) provides sufficient visual separation.

2. Lines 82, 94: Update filter select elements. Change `border border-outline bg-elevated` to `border border-outline-subtle bg-elevated`. Add `focus:ring-1 focus:ring-primary/30` after `focus:border-primary`.

3. Line 107: Update search input. Same changes as above. Also change `placeholder-on-bg-secondary` to `placeholder-on-bg-muted`.

4. Line 119: Update reset button. Change `border border-outline` to no border, use `bg-surface-high hover:bg-elevated` instead.

5. Add `transition-colors duration-200` to the main dashboard container.

**DeviceTable.tsx:**

1. Read fully. Line 60: Remove `border-b border-outline` from the thead `<tr>`. The header row sits on a surface tier that distinguishes it from data rows.

2. Ensure column headers use UI-SPEC Label typography: `text-[12px] uppercase tracking-[0.16em] text-on-bg-secondary font-normal`. The existing `text-on-bg-secondary` is correct; add `uppercase tracking-[0.16em]` if not present, and ensure `text-[12px]` or `text-xs`.

3. Ensure metric columns (CPU, MEM, etc.) headers are in Outfit (default) with the label style above.

**DeviceRow.tsx:**

1. Read fully. Line 28: Remove `border-b border-outline/50` from the `<tr>`. Use alternating surface tiers for row differentiation. Apply `even:bg-surface-high/30` or simply rely on hover state `hover:bg-elevated/50` for interactive feedback. If the parent DeviceTable component passes an index or uses CSS `:nth-child`, add `[&:nth-child(even)]:bg-surface-high/30` to the row.

2. Ensure metric value cells use `font-mono text-[11px] font-semibold` per UI-SPEC Metric role.

3. Ensure status dots in the row use the glow pattern: find any `h-2 w-2 rounded-full bg-status-*` and add the severity-scaled shadow matching StatusDot. Or import StatusDot directly and use it.

4. Line 66: Update action button. Change `border border-outline` to `bg-surface-high`. Remove the border, use surface tier.

5. Add `transition-colors duration-150` to the row for smooth theme switching.

**Dashboard.test.tsx:**

Read existing tests. Add or update tests to verify:
- Filter bar does NOT render with `border-b` class
- Dashboard renders without errors (smoke test for THEME-05)
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia/frontend && npx vitest run src/components/Dashboard.test.tsx && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `Dashboard.tsx` filter bar does NOT contain `border-b border-outline`
    - `Dashboard.tsx` filter inputs contain `border-outline-subtle` and `focus:ring-1 focus:ring-primary/30`
    - `DeviceTable.tsx` thead tr does NOT contain `border-b border-outline`
    - `DeviceRow.tsx` tr does NOT contain `border-b border-outline/50`
    - `DeviceRow.tsx` action button does NOT contain `border border-outline` (uses `bg-surface-high`)
    - Dashboard tests pass
    - `npx vite build` exits 0
  </acceptance_criteria>
  <done>Dashboard, DeviceTable, DeviceRow restyled with no-line rule, correct typography tokens, glow status dots</done>
</task>

<task type="auto">
  <name>Task 2a: Restyle BackupPanel, BackupHistoryTable, BulkBackupPanel, ConfigViewer</name>
  <files>frontend/src/components/dashboard/BackupPanel.tsx, frontend/src/components/dashboard/BackupHistoryTable.tsx, frontend/src/components/dashboard/BulkBackupPanel.tsx, frontend/src/components/dashboard/ConfigViewer.tsx</files>
  <read_first>
    - frontend/src/components/dashboard/BackupPanel.tsx
    - frontend/src/components/dashboard/BackupHistoryTable.tsx
    - frontend/src/components/dashboard/BulkBackupPanel.tsx
    - frontend/src/components/dashboard/ConfigViewer.tsx
  </read_first>
  <action>
**BackupPanel.tsx:**
- Replace `border border-outline p-3` on containers (lines ~87, ~164) with `bg-surface-high rounded-lg p-3`
- Replace `border border-outline bg-elevated/30 p-3` (line ~107) with `bg-surface-high p-3`
- Add `transition-colors duration-200` to root

**BackupHistoryTable.tsx:**
- Replace `border border-outline` on table container (line ~102) with `bg-surface-high rounded-lg overflow-hidden`
- Replace `border-t border-outline` separator (line ~126) with `mt-3`
- Add `transition-colors duration-200`

**BulkBackupPanel.tsx:**
- Replace `border border-outline p-3` on info container (line ~152) with `bg-surface-high rounded-lg p-3`
- Add `transition-colors duration-200`

**ConfigViewer.tsx:**
- Replace `border-b border-outline` on tab bar (line ~108) with no border (tabs have their own active state styling)
- Replace `border border-outline` on action buttons (lines ~152, ~160) with `bg-surface-high hover:bg-elevated`
- Replace `border border-outline bg-bg` on empty state and code container (lines ~170, ~188) with `bg-surface-high rounded-lg`
- Replace `border-r border-outline/30` on line number column (line ~193) with just padding (the monospace column is visually distinct enough)
- Add `transition-colors duration-200`
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia/frontend && npx vite build 2>&1 | tail -5 && npx vitest run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `BackupPanel.tsx` does NOT contain `border border-outline p-3` (uses `bg-surface-high`)
    - `BackupHistoryTable.tsx` does NOT contain `border-t border-outline`
    - `BulkBackupPanel.tsx` does NOT contain `border border-outline p-3`
    - `ConfigViewer.tsx` does NOT contain `border-b border-outline` on tab bar
    - `ConfigViewer.tsx` does NOT contain `border-r border-outline/30` on line number column
    - All four files contain `transition-colors duration-200`
    - `npx vite build` exits 0, all tests pass
  </acceptance_criteria>
  <done>BackupPanel, BackupHistoryTable, BulkBackupPanel, ConfigViewer restyled with no-line rule and surface tiers</done>
</task>

<task type="auto">
  <name>Task 2b: Restyle SSHCredentialForm, VendorSettingsPanel, and Canvas.tsx overlays</name>
  <files>frontend/src/components/dashboard/SSHCredentialForm.tsx, frontend/src/components/dashboard/VendorSettingsPanel.tsx, frontend/src/components/Canvas.tsx</files>
  <read_first>
    - frontend/src/components/dashboard/SSHCredentialForm.tsx
    - frontend/src/components/dashboard/VendorSettingsPanel.tsx
    - frontend/src/components/Canvas.tsx
  </read_first>
  <action>
**SSHCredentialForm.tsx:**
- Update input class constants (lines ~17, ~19): `border border-outline` -> `border border-outline-subtle`, add `focus:ring-1 focus:ring-primary/30`, `placeholder-on-bg-secondary/40` -> `placeholder-on-bg-muted`
- Replace `border border-outline bg-elevated/40` on display divs (line ~234) with `bg-surface-high`
- Replace `border border-outline` on action buttons (lines ~191, ~256) with `bg-surface-high hover:bg-elevated`
- Add `transition-colors duration-200`

**VendorSettingsPanel.tsx:**
- Update input class constant (line ~87): `border border-outline` -> `border border-outline-subtle`, add `focus:ring-1 focus:ring-primary/30`
- Replace `border border-outline bg-elevated/30` on info containers (line ~167) with `bg-surface-high`
- Add `transition-colors duration-200`

**Canvas.tsx (overlay elements only -- NOT decomposing, per deferred decision):**
- Lines ~852, ~865: The loading/error overlay divs have `border border-outline bg-surface/85`. Replace with `bg-surface/85 shadow-panel` (remove border, add shadow for depth).
- Add `transition-colors duration-200` to these overlay containers.
- Do NOT restructure Canvas.tsx code. Only touch the className strings on the overlay elements.
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia/frontend && npx vite build 2>&1 | tail -5 && npx vitest run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `SSHCredentialForm.tsx` input constants contain `border-outline-subtle` and `focus:ring-1 focus:ring-primary/30`
    - `SSHCredentialForm.tsx` does NOT contain `border border-outline bg-elevated/40` (uses `bg-surface-high`)
    - `VendorSettingsPanel.tsx` input constant contains `border-outline-subtle`
    - `VendorSettingsPanel.tsx` does NOT contain `border border-outline bg-elevated/30` (uses `bg-surface-high`)
    - `Canvas.tsx` overlay divs do NOT contain `border border-outline` (uses `shadow-panel`)
    - All three files contain `transition-colors duration-200`
    - `npx vite build` exits 0, all tests pass
  </acceptance_criteria>
  <done>SSHCredentialForm, VendorSettingsPanel, and Canvas.tsx overlays restyled with no-line rule, surface tiers, standardized inputs</done>
</task>

<task type="auto">
  <name>Task 3: Full no-line audit and THEME-05 verification</name>
  <files></files>
  <read_first>
    - frontend/src/index.css
  </read_first>
  <action>
**No-Line Rule Audit (COMP-12):**

Run a comprehensive grep to find ALL remaining layout border patterns:
```bash
grep -rn "border-[tblr] border-outline" --include="*.tsx" frontend/src/components/
grep -rn "border-b border-outline" --include="*.tsx" frontend/src/components/
grep -rn "border-t border-outline" --include="*.tsx" frontend/src/components/
grep -rn "border-l border-outline" --include="*.tsx" frontend/src/components/
```

For each result, classify as:
- **Layout separator** (MUST REMOVE): `border-t border-outline pt-4` between sections, `border-b border-outline` between items, `border-l border-outline` on panel edges
- **Ghost border fallback** (KEEP): `ring-1 ring-outline` on DeviceCard, `border border-outline-subtle` on form inputs
- **Functional border** (KEEP): `border border-glass-border` on glassmorphism overlays

Fix any remaining layout separators found. Replace with surface tier shifts or spacing.

Then verify the full list of exceptions:
- Form inputs: `border border-outline-subtle` is the new standard (NOT `border-outline`)
- Check if any form inputs still use `border border-outline` instead of `border border-outline-subtle`. Fix them.

**THEME-05 Verification:**

Run the full test suite to verify no runtime errors in either theme:
```bash
cd frontend && npx vitest run --reporter=verbose
```

Run the Vite production build to verify no compilation errors:
```bash
cd frontend && npx vite build
```

Check that no hardcoded hex colors crept back in:
```bash
grep -rn "#[0-9a-fA-F]\{6\}" --include="*.tsx" frontend/src/components/ | grep -v "test\." | grep -v "\.test\."
```
Any hits should be in test fixture data only, not in className or style attributes.

If any remaining issues are found, fix them in this task.
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia/frontend && npx vitest run --reporter=verbose 2>&1 | tail -10 && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "border-t border-outline" --include="*.tsx" frontend/src/components/` returns zero results (no layout separators)
    - `grep -rn "border-b border-outline" --include="*.tsx" frontend/src/components/` returns zero results for layout separator patterns (filter bar, section dividers). Note: a few functional borders on glassmorphism overlays may remain and are acceptable.
    - `grep -rn "border-l border-outline" --include="*.tsx" frontend/src/components/` returns zero results
    - Any remaining `border border-outline` patterns are ONLY on form inputs that should be `border-outline-subtle` (fix if found)
    - `npx vitest run` exits 0 with all tests passing
    - `npx vite build` exits 0
    - No hardcoded hex colors in className or style attributes of production component files
  </acceptance_criteria>
  <done>Full no-line audit complete with zero layout separator borders remaining, all tests passing, THEME-05 verified</done>
</task>

</tasks>

<verification>
- Dashboard and all sub-components restyled
- Full no-line audit: zero border-t/b/l layout separators across all 35+ component files
- All form inputs use standard contract (border-outline-subtle, focus:ring)
- All components have transition-colors for theme switching
- All tests pass in both themes
- Vite production build succeeds
- No hardcoded hex colors in component files
</verification>

<success_criteria>
1. `grep "border-[tblr] border-outline" --include="*.tsx" -r frontend/src/components/` returns no layout separator results
2. All 55+ tests pass
3. Vite build clean
4. Dashboard renders with correct typography (Outfit labels, JetBrains Mono metrics)
5. Phase 2 complete: all 13 requirements (COMP-01 through COMP-12, THEME-05) addressed
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-restyling/02-06-SUMMARY.md`
</output>
