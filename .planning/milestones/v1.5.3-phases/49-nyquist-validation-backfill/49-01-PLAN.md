---
phase: 49-nyquist-validation-backfill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/39-domain-types-db-migration/39-VALIDATION.md
  - .planning/phases/41-jittered-scheduler/41-VALIDATION.md
  - .planning/phases/43-websocket-detail-on-demand/43-VALIDATION.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Phase 39 has a Nyquist validation artifact that maps POLL-03 and POLL-05 to the already-accepted automated evidence in the phase plans and 39-VERIFICATION.md."
    - "Phase 41 has a Nyquist validation artifact that maps POLL-01, POLL-02, and POLL-04 to the already-accepted scheduler test evidence in the phase plans and 41-VERIFICATION.md."
    - "Phase 43 has a Nyquist validation artifact that maps WS-02 to the already-accepted websocket/backend/frontend automated evidence in the phase plans and 43-VERIFICATION.md."
    - "All three new validation artifacts set `nyquist_compliant: true`, preserve the repo's validation-doc structure, and explicitly state that no manual-only verification debt remains for these phases."
  artifacts:
    - path: ".planning/phases/39-domain-types-db-migration/39-VALIDATION.md"
      provides: "Backfilled validation contract for the Phase 39 domain types and migration work"
      contains: "POLL-03"
      contains_2: "POLL-05"
    - path: ".planning/phases/41-jittered-scheduler/41-VALIDATION.md"
      provides: "Backfilled validation contract for the Phase 41 scheduler work"
      contains: "POLL-01"
      contains_2: "POLL-04"
    - path: ".planning/phases/43-websocket-detail-on-demand/43-VALIDATION.md"
      provides: "Backfilled validation contract for the Phase 43 websocket detail-subscription work"
      contains: "WS-02"
      contains_2: "All phase behaviors have automated verification."
  key_links:
    - from: ".planning/phases/39-domain-types-db-migration/39-VALIDATION.md"
      to: ".planning/phases/39-domain-types-db-migration/39-VERIFICATION.md"
      via: "The validation doc must reuse the exact automated package/build evidence already accepted for POLL-03 and POLL-05."
      pattern: "POLL-03|POLL-05|go test -race|go build ./..."
    - from: ".planning/phases/41-jittered-scheduler/41-VALIDATION.md"
      to: ".planning/phases/41-jittered-scheduler/41-VERIFICATION.md"
      via: "The validation doc must reuse the exact scheduler timing/dispatch/package evidence already accepted for POLL-01, POLL-02, and POLL-04."
      pattern: "POLL-01|POLL-02|POLL-04|internal/scheduler"
    - from: ".planning/phases/43-websocket-detail-on-demand/43-VALIDATION.md"
      to: ".planning/phases/43-websocket-detail-on-demand/43-VERIFICATION.md"
      via: "The validation doc must reuse the exact websocket/hub/worker/frontend automated evidence already accepted for WS-02."
      pattern: "WS-02|internal/ws|internal/worker|frontend"
---

<objective>
Backfill the missing Nyquist validation artifacts for the fully automated
v1.5.3 phases: 39, 41, and 43.

Purpose:
- Convert already-accepted verifier evidence into durable `*-VALIDATION.md`
  artifacts without changing product code.
- Make the automated-only phases explicitly Nyquist-compliant.
- Keep the later Phase 49 audit refresh honest by creating concrete validation
  docs before any milestone claim changes.

Output:
- `.planning/phases/39-domain-types-db-migration/39-VALIDATION.md`
- `.planning/phases/41-jittered-scheduler/41-VALIDATION.md`
- `.planning/phases/43-websocket-detail-on-demand/43-VALIDATION.md`
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.5.3-MILESTONE-AUDIT.md
@.planning/phases/38-state-engine/38-VALIDATION.md
@.planning/milestones/v1.5.0-phases/24-backend-api-profiles-assignments-winbox-credentials/24-VALIDATION.md
@.planning/phases/39-domain-types-db-migration/39-01-PLAN.md
@.planning/phases/39-domain-types-db-migration/39-02-PLAN.md
@.planning/phases/39-domain-types-db-migration/39-03-PLAN.md
@.planning/phases/39-domain-types-db-migration/39-04-PLAN.md
@.planning/phases/39-domain-types-db-migration/39-VERIFICATION.md
@.planning/phases/41-jittered-scheduler/41-01-PLAN.md
@.planning/phases/41-jittered-scheduler/41-02-PLAN.md
@.planning/phases/41-jittered-scheduler/41-03-PLAN.md
@.planning/phases/41-jittered-scheduler/41-VERIFICATION.md
@.planning/phases/43-websocket-detail-on-demand/43-01-PLAN.md
@.planning/phases/43-websocket-detail-on-demand/43-02-PLAN.md
@.planning/phases/43-websocket-detail-on-demand/43-03-PLAN.md
@.planning/phases/43-websocket-detail-on-demand/43-VERIFICATION.md

<interfaces>
Validation docs created in this plan must keep the existing structure:
- frontmatter with `phase`, `slug`, `status`, `nyquist_compliant`, `wave_0_complete`, and `created`
- `## Test Infrastructure`
- `## Sampling Rate`
- `## Per-Task Verification Map`
- `## Wave 0 Requirements`
- `## Manual-Only Verifications`
- `## Validation Sign-Off`

For these three phases, the manual-only section must end with:
`All phase behaviors have automated verification.`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Phase 39 validation artifact from accepted automated evidence</name>
  <files>.planning/phases/39-domain-types-db-migration/39-VALIDATION.md</files>
  <read_first>
    - .planning/phases/39-domain-types-db-migration/39-VALIDATION.md (create if missing)
    - .planning/phases/38-state-engine/38-VALIDATION.md
    - .planning/milestones/v1.5.0-phases/24-backend-api-profiles-assignments-winbox-credentials/24-VALIDATION.md
    - .planning/phases/39-domain-types-db-migration/39-01-PLAN.md
    - .planning/phases/39-domain-types-db-migration/39-02-PLAN.md
    - .planning/phases/39-domain-types-db-migration/39-03-PLAN.md
    - .planning/phases/39-domain-types-db-migration/39-04-PLAN.md
    - .planning/phases/39-domain-types-db-migration/39-VERIFICATION.md
  </read_first>
  <action>
    Create `.planning/phases/39-domain-types-db-migration/39-VALIDATION.md`
    as a completed Nyquist validation artifact for the already-shipped Phase 39
    work.

    Use these exact frontmatter values:
    - `phase: 39`
    - `slug: domain-types-db-migration`
    - `status: verified`
    - `nyquist_compliant: true`
    - `wave_0_complete: true`
    - `created:` set to the execution date

    Populate the body by mining accepted evidence from the Phase 39 plans and
    verification report:
    - Use the existing validation-doc structure from Phase 38 / Phase 24.
    - Build `## Test Infrastructure`, `## Sampling Rate`, and the per-task
      verification map from the exact automated commands and package/build
      evidence already accepted in `39-VERIFICATION.md`.
    - Map the plan tasks to Phase 39's two requirement IDs:
      `POLL-03` and `POLL-05`.
    - Record Wave 0 as already satisfied by the tests and migration/runtime
      artifacts that exist in the repo today; do not invent missing test debt.
    - In `## Manual-Only Verifications`, state exactly:
      `All phase behaviors have automated verification.`
    - Check every sign-off box and end with an approval line dated at
      execution time.
  </action>
  <verify>
    <automated>rtk bash -lc 'test -f .planning/phases/39-domain-types-db-migration/39-VALIDATION.md && rg -n "^phase: 39$|^nyquist_compliant: true$|POLL-03|POLL-05|All phase behaviors have automated verification\\." .planning/phases/39-domain-types-db-migration/39-VALIDATION.md'</automated>
  </verify>
  <acceptance_criteria>
    - `rtk test -f .planning/phases/39-domain-types-db-migration/39-VALIDATION.md` exits 0
    - `rtk rg -n '^phase: 39$' .planning/phases/39-domain-types-db-migration/39-VALIDATION.md` returns 1 match
    - `rtk rg -n '^nyquist_compliant: true$' .planning/phases/39-domain-types-db-migration/39-VALIDATION.md` returns 1 match
    - `rtk rg -n 'POLL-03|POLL-05' .planning/phases/39-domain-types-db-migration/39-VALIDATION.md` returns both requirement IDs
    - `rtk rg -n 'All phase behaviors have automated verification\\.' .planning/phases/39-domain-types-db-migration/39-VALIDATION.md` returns 1 match
    - `rtk rg -n 'go build ./\\.\\.\\.|go test -race' .planning/phases/39-domain-types-db-migration/39-VALIDATION.md` returns at least 1 match
  </acceptance_criteria>
  <done>The Phase 39 validation artifact exists, is Nyquist-compliant, and maps POLL-03/POLL-05 to accepted automated evidence only.</done>
</task>

<task type="auto">
  <name>Task 2: Create the Phase 41 validation artifact from accepted scheduler evidence</name>
  <files>.planning/phases/41-jittered-scheduler/41-VALIDATION.md</files>
  <read_first>
    - .planning/phases/41-jittered-scheduler/41-VALIDATION.md (create if missing)
    - .planning/phases/38-state-engine/38-VALIDATION.md
    - .planning/phases/41-jittered-scheduler/41-01-PLAN.md
    - .planning/phases/41-jittered-scheduler/41-02-PLAN.md
    - .planning/phases/41-jittered-scheduler/41-03-PLAN.md
    - .planning/phases/41-jittered-scheduler/41-VERIFICATION.md
  </read_first>
  <action>
    Create `.planning/phases/41-jittered-scheduler/41-VALIDATION.md` as a
    completed Nyquist validation artifact for the already-shipped Phase 41
    scheduler work.

    Use these exact frontmatter values:
    - `phase: 41`
    - `slug: jittered-scheduler`
    - `status: verified`
    - `nyquist_compliant: true`
    - `wave_0_complete: true`
    - `created:` set to the execution date

    Populate the body from accepted Phase 41 evidence:
    - Reuse the validation-doc structure already used by Phases 24 and 38.
    - Build the per-task verification map from the exact Phase 41 plans and the
      scheduler-focused commands already accepted in `41-VERIFICATION.md`.
    - Cover all three requirement IDs: `POLL-01`, `POLL-02`, and `POLL-04`.
    - Keep the validation centered on the existing `internal/scheduler`
      package tests and timing/distribution evidence already verified.
    - In `## Manual-Only Verifications`, state exactly:
      `All phase behaviors have automated verification.`
    - Check every sign-off box and end with an approval line dated at
      execution time.
  </action>
  <verify>
    <automated>rtk bash -lc 'test -f .planning/phases/41-jittered-scheduler/41-VALIDATION.md && rg -n "^phase: 41$|^nyquist_compliant: true$|POLL-01|POLL-02|POLL-04|All phase behaviors have automated verification\\." .planning/phases/41-jittered-scheduler/41-VALIDATION.md'</automated>
  </verify>
  <acceptance_criteria>
    - `rtk test -f .planning/phases/41-jittered-scheduler/41-VALIDATION.md` exits 0
    - `rtk rg -n '^phase: 41$' .planning/phases/41-jittered-scheduler/41-VALIDATION.md` returns 1 match
    - `rtk rg -n '^nyquist_compliant: true$' .planning/phases/41-jittered-scheduler/41-VALIDATION.md` returns 1 match
    - `rtk rg -n 'POLL-01|POLL-02|POLL-04' .planning/phases/41-jittered-scheduler/41-VALIDATION.md` returns all three requirement IDs
    - `rtk rg -n 'All phase behaviors have automated verification\\.' .planning/phases/41-jittered-scheduler/41-VALIDATION.md` returns 1 match
    - `rtk rg -n 'internal/scheduler|TestInitialOffset_DistributionAcrossBuckets|go test ./internal/scheduler -count=1' .planning/phases/41-jittered-scheduler/41-VALIDATION.md` returns at least 1 match
  </acceptance_criteria>
  <done>The Phase 41 validation artifact exists, is Nyquist-compliant, and maps POLL-01/POLL-02/POLL-04 to accepted automated scheduler evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Create the Phase 43 validation artifact from accepted websocket and frontend evidence</name>
  <files>.planning/phases/43-websocket-detail-on-demand/43-VALIDATION.md</files>
  <read_first>
    - .planning/phases/43-websocket-detail-on-demand/43-VALIDATION.md (create if missing)
    - .planning/phases/38-state-engine/38-VALIDATION.md
    - .planning/phases/43-websocket-detail-on-demand/43-01-PLAN.md
    - .planning/phases/43-websocket-detail-on-demand/43-02-PLAN.md
    - .planning/phases/43-websocket-detail-on-demand/43-03-PLAN.md
    - .planning/phases/43-websocket-detail-on-demand/43-VERIFICATION.md
  </read_first>
  <action>
    Create `.planning/phases/43-websocket-detail-on-demand/43-VALIDATION.md`
    as a completed Nyquist validation artifact for the already-shipped Phase 43
    websocket detail-subscription work.

    Use these exact frontmatter values:
    - `phase: 43`
    - `slug: websocket-detail-on-demand`
    - `status: verified`
    - `nyquist_compliant: true`
    - `wave_0_complete: true`
    - `created:` set to the execution date

    Populate the body from accepted Phase 43 evidence:
    - Reuse the same section ordering as the existing validation docs.
    - Build the per-task verification map from the exact Phase 43 plans and the
      backend websocket, worker, and frontend lifecycle commands already
      accepted in `43-VERIFICATION.md`.
    - Cover the single requirement ID `WS-02`.
    - Keep the document explicit that the subscription flow is justified by
      automated backend and frontend coverage already present in the repo.
    - In `## Manual-Only Verifications`, state exactly:
      `All phase behaviors have automated verification.`
    - Check every sign-off box and end with an approval line dated at
      execution time.
  </action>
  <verify>
    <automated>rtk bash -lc 'test -f .planning/phases/43-websocket-detail-on-demand/43-VALIDATION.md && rg -n "^phase: 43$|^nyquist_compliant: true$|WS-02|All phase behaviors have automated verification\\." .planning/phases/43-websocket-detail-on-demand/43-VALIDATION.md'</automated>
  </verify>
  <acceptance_criteria>
    - `rtk test -f .planning/phases/43-websocket-detail-on-demand/43-VALIDATION.md` exits 0
    - `rtk rg -n '^phase: 43$' .planning/phases/43-websocket-detail-on-demand/43-VALIDATION.md` returns 1 match
    - `rtk rg -n '^nyquist_compliant: true$' .planning/phases/43-websocket-detail-on-demand/43-VALIDATION.md` returns 1 match
    - `rtk rg -n 'WS-02' .planning/phases/43-websocket-detail-on-demand/43-VALIDATION.md` returns at least 1 match
    - `rtk rg -n 'All phase behaviors have automated verification\\.' .planning/phases/43-websocket-detail-on-demand/43-VALIDATION.md` returns 1 match
    - `rtk rg -n 'internal/ws|internal/worker|frontend/src/hooks/useWebSocket.ts' .planning/phases/43-websocket-detail-on-demand/43-VALIDATION.md` returns at least 1 match
  </acceptance_criteria>
  <done>The Phase 43 validation artifact exists, is Nyquist-compliant, and maps WS-02 to accepted automated websocket/backend/frontend evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan edits only planning artifacts. The trust boundary is between accepted
verification evidence and newly created validation docs that will later be used
to justify milestone archival.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Tampering | 39/41/43 validation docs | mitigate | Build each validation doc directly from the accepted phase plans and verification reports; do not invent new evidence or new requirement mappings |
| T-49-02 | Repudiation | Nyquist compliance claims | mitigate | Require `nyquist_compliant: true` only after the file contains explicit requirement IDs, verification map content, and sign-off checks |
| T-49-03 | Integrity | Manual-only debt tracking | mitigate | State `All phase behaviors have automated verification.` only for phases whose verifier reports already leave no human-only debt |
</threat_model>

<verification>
After all three tasks complete:
1. `rtk test -f .planning/phases/39-domain-types-db-migration/39-VALIDATION.md`
2. `rtk test -f .planning/phases/41-jittered-scheduler/41-VALIDATION.md`
3. `rtk test -f .planning/phases/43-websocket-detail-on-demand/43-VALIDATION.md`
4. `rtk rg -n '^nyquist_compliant: true$' .planning/phases/39-domain-types-db-migration/39-VALIDATION.md .planning/phases/41-jittered-scheduler/41-VALIDATION.md .planning/phases/43-websocket-detail-on-demand/43-VALIDATION.md` returns 3 matches
5. `rtk rg -n 'POLL-03|POLL-05' .planning/phases/39-domain-types-db-migration/39-VALIDATION.md` returns both IDs
6. `rtk rg -n 'POLL-01|POLL-02|POLL-04' .planning/phases/41-jittered-scheduler/41-VALIDATION.md` returns all 3 IDs
7. `rtk rg -n 'WS-02' .planning/phases/43-websocket-detail-on-demand/43-VALIDATION.md` returns at least 1 match
8. `rtk rg -n 'All phase behaviors have automated verification\\.' .planning/phases/39-domain-types-db-migration/39-VALIDATION.md .planning/phases/41-jittered-scheduler/41-VALIDATION.md .planning/phases/43-websocket-detail-on-demand/43-VALIDATION.md` returns 3 matches
</verification>

<success_criteria>
1. Phase 39 has a Nyquist validation artifact that covers POLL-03 and POLL-05 with automated evidence only.
2. Phase 41 has a Nyquist validation artifact that covers POLL-01, POLL-02, and POLL-04 with automated evidence only.
3. Phase 43 has a Nyquist validation artifact that covers WS-02 with automated evidence only.
4. None of the three docs leave manual-only verification debt behind.
</success_criteria>

<output>
After completion, create `.planning/phases/49-nyquist-validation-backfill/49-01-SUMMARY.md`.
</output>
