node_modules
dist

# Test files (not needed in production image)
*.test.ts
*.test.tsx
src/test-setup.ts
vitest.config.ts

# TypeScript build config (only tsconfig.app.json is needed for tsc -b)
tsconfig.node.json
