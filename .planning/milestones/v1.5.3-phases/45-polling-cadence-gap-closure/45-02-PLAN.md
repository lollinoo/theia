---
phase: 45-polling-cadence-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/state/store.go
  - internal/state/store_test.go
  - internal/worker/pipeline_test.go
autonomous: true
requirements:
  - POLL-02
  - WS-01
  - WS-04
user_setup: []

must_haves:
  truths:
    - "Overview `last_polled_at` and `expected_poll_interval_seconds` stay owned by performance polls even when operational or static polls run later."
    - "Operational and static polls can still update reachability, uptime, and topology without making the canvas look freshly performance-polled."
    - "Immediate detail `snapshot_delta` messages no longer contaminate the shared snapshot atom with non-performance freshness/cadence metadata."
  artifacts:
    - path: "internal/state/store.go"
      provides: "Performance-owned freshness/cadence metadata in the runtime state store"
      contains: "VolatilityClassPerformance"
    - path: "internal/state/store_test.go"
      provides: "Regression coverage that operational/static updates do not overwrite performance freshness metadata"
      contains: "OperationalPoll"
    - path: "internal/worker/pipeline_test.go"
      provides: "Runtime regression coverage for overview snapshot and detail delta metadata ownership"
      contains: "FreshnessMetadata"
  key_links:
    - from: "internal/state/store.go"
      to: "internal/worker/snapshot_builder.go"
      via: "Overview websocket metadata comes from performance-owned `LastPolledAt` / `ExpectedInterval` in `state.DeviceState`"
      pattern: "LastPolledAt|ExpectedInterval"
    - from: "internal/worker/pipeline.go"
      to: "internal/ws/messages.go"
      via: "Targeted detail deltas reuse the shared `snapshot_delta` contract and therefore must preserve performance-owned metadata"
      pattern: "publishSubscribedDetailDelta|MessageTypeSnapshotDelta"
---

<objective>
Close `INT-02` by making performance polls the sole owners of overview
freshness/cadence metadata, then prove through store and runtime tests that
operational/static polls can no longer overwrite what the canvas and shared
snapshot atom render.

Purpose:
- Satisfy roadmap success criteria 3 and 4 for Phase 45.
- Preserve Phase 42's existing websocket payload family and Phase 44's
  operator-facing UI semantics without frontend redesign.
- Stop cross-tier metadata contamination in both the overview broadcast path
  and the targeted detail-delta path that merges into the same frontend state.

Output:
- `state.Store.Update(...)` stamps `LastPolledAt`, `ExpectedInterval`, and
  `Stale=false` only for performance-owned freshness/cadence updates.
- Store tests lock the mixed-tier ownership rules.
- Pipeline regression tests prove overview snapshots and subscribed detail
  deltas retain performance cadence metadata after later operational/static
  polls.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.5.3-MILESTONE-AUDIT.md
@.planning/phases/42-pipeline-orchestrator-cutover/42-CONTEXT.md
@.planning/phases/43-websocket-detail-on-demand/43-02-SUMMARY.md
@.planning/phases/44-frontend-integration/44-CONTEXT.md
@.planning/phases/44-frontend-integration/44-01-SUMMARY.md
@internal/state/store.go
@internal/state/store_test.go
@internal/worker/pipeline.go
@internal/worker/pipeline_test.go
@internal/worker/snapshot_builder.go
@internal/ws/messages.go

<interfaces>
Current state ownership seam:
```go
type DeviceState struct {
    Reachability     ReachabilityStatus
    Stale            bool
    LastPolledAt     time.Time
    ExpectedInterval time.Duration
}

type StateUpdate struct {
    DeviceID         uuid.UUID
    VolatilityClass  domain.VolatilityClass
    PollSuccess      bool
    ExpectedInterval time.Duration
    Timestamp        time.Time
}
```

Current targeted detail send path:
```go
func (p *PipelineOrchestrator) publishSubscribedDetailDelta(device domain.Device) {
    deviceState, ok := p.stateStore.GetDevice(device.ID)
    delta := buildDeviceDetailDelta(device, deviceState)
    p.hub.SendTo(client, ws.Message{Type: ws.MessageTypeSnapshotDelta, Payload: delta})
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move freshness and cadence ownership to performance updates only inside the state store</name>
  <files>
    internal/state/store.go
    internal/state/store_test.go
  </files>
  <read_first>
    - .planning/v1.5.3-MILESTONE-AUDIT.md (INT-02 evidence and WS-01 / WS-04 gap statements)
    - .planning/phases/44-frontend-integration/44-CONTEXT.md (D-04 through D-15: frontend freshness/cadence are backend-owned semantics)
    - internal/state/store.go (current unconditional LastPolledAt / ExpectedInterval / Stale=false writes before the volatility switch)
    - internal/state/store_test.go (existing mixed-tier state expectations that currently document the contamination)
    - internal/domain/poll_class.go (performance vs operational/static cadence split)
  </read_first>
  <behavior>
    - performance polls, including failed performance attempts, remain the owners of freshness/cadence metadata
    - operational and static polls still update their own concerns (reachability, uptime, topology) without resetting freshness
    - legacy unstamped callers keep the existing compatibility path; this change is about explicit tiered updates, not a hidden cutover regression
  </behavior>
  <action>
    Refactor `internal/state/store.go` so only performance-owned freshness
    updates mutate `LastPolledAt`, `ExpectedInterval`, and `Stale`.

    Implement the ownership change exactly this way:

    1. Remove the unconditional pre-switch writes that currently run for every
       update:
       - `next.LastPolledAt = u.Timestamp`
       - `next.ExpectedInterval = ...`
       - `next.Stale = false`

    2. Add a small helper (or equivalent inline block) that performs those
       three writes for performance-owned freshness updates:
       - set `LastPolledAt = u.Timestamp`
       - update `ExpectedInterval` when `u.ExpectedInterval > 0`
       - clear `Stale`

    3. Call that helper only from:
       - `domain.VolatilityClassPerformance`
       - the legacy default path used for unstamped callers

       Do not call it from `VolatilityClassOperational` or
       `VolatilityClassStatic`.

    4. Preserve the rest of the tiered behavior:
       - operational polls still drive reachability and can update uptime
       - static polls still remain topology/inventory-only
       - failed performance polls still keep last-known metrics/link data while
         advancing performance freshness ownership

    5. Update/add state tests to lock the new rules:
       - the existing operational mixed-tier test must now assert
         `LastPolledAt` and `ExpectedInterval` stay on the earlier performance
         values after the operational update
       - add `TestStoreUpdate_StaticPollDoesNotOverwritePerformanceFreshnessMetadata`
       - keep `TestStoreUpdate_FailedPerformancePollKeepsLastKnownMetricsAndLinks`
         asserting that failed performance polls still advance
         `LastPolledAt`
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia && rtk go test ./internal/state -run 'TestStoreUpdate_(OperationalPoll|StaticPoll|FailedPerformancePoll)' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n 'LastPolledAt = u\\.Timestamp|next\\.ExpectedInterval|next\\.Stale = false' internal/state/store.go` shows these writes only in performance-owned or legacy code paths, not before the switch
    - `rtk rg -n 'StaticPollDoesNotOverwritePerformanceFreshnessMetadata' internal/state/store_test.go` returns 1 match
    - `rtk rg -n 'want 30s|want 45s|want performance' internal/state/store_test.go` returns updated mixed-tier freshness assertions
    - `cd /home/azmin/projects/theia && rtk go test ./internal/state -run 'TestStoreUpdate_(OperationalPoll|StaticPoll|FailedPerformancePoll)' -count=1 -v` exits 0
  </acceptance_criteria>
  <done>
    The runtime state store now treats freshness/cadence as performance-owned
    metadata instead of letting any volatility class overwrite it.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove overview snapshots and targeted detail deltas keep performance-owned metadata after mixed-tier polls</name>
  <files>
    internal/worker/pipeline_test.go
  </files>
  <read_first>
    - .planning/v1.5.3-MILESTONE-AUDIT.md (INT-02 and broken canvas freshness/cadence flow)
    - .planning/phases/42-pipeline-orchestrator-cutover/42-CONTEXT.md (snapshot/snapshot_delta family must stay unchanged)
    - .planning/phases/43-websocket-detail-on-demand/43-02-SUMMARY.md (detail deltas are pushed after performance, operational, and static polls on the same snapshot_delta contract)
    - internal/worker/pipeline.go (runTask, publishSubscribedDetailDelta, broadcastOnce)
    - internal/worker/pipeline_test.go (existing detail-subscription and broadcast helpers)
    - internal/worker/snapshot_builder.go (overview metadata read path from state.DeviceState)
    - internal/ws/messages.go (shared DeviceMetricsDTO fields and snapshot_delta message type)
  </read_first>
  <behavior>
    - the fix is proven at the runtime/websocket layer, not only in store unit tests
    - targeted detail deltas remain on `snapshot_delta`; they just stop carrying non-performance freshness ownership
    - no frontend files are changed because Phase 45 is a correctness fix for backend-owned semantics
  </behavior>
  <action>
    Extend `internal/worker/pipeline_test.go` with runtime regressions that
    prove the shared websocket state atom keeps performance freshness/cadence
    semantics after later mixed-tier polls.

    Add these exact cases:

    1. `TestPipelineOrchestratorBroadcastOnce_MixedTierPollsKeepPerformanceFreshnessMetadata`
       - seed one device and cache
       - run a performance task with `ExpectedInterval=30s`
       - then run operational and static tasks with their own expected
         intervals (`60s` and `300s`)
       - call `broadcastOnce(...)`
       - assert the resulting snapshot keeps:
         - `device_metrics[deviceID].last_polled_at` from the performance poll
         - `device_metrics[deviceID].expected_poll_interval_seconds == 30`
       - also assert reachability/status can still reflect the later
         operational/static processing

    2. `TestPipelineOrchestratorRunTask_DetailDeltaKeepsPerformanceFreshnessMetadataAfterOperationalPoll`
       - subscribe a websocket client using the existing test server helpers
       - run one performance task first to seed performance freshness metadata
       - then run an operational task
       - read the targeted `snapshot_delta`
       - assert `LastPolledAt` and `ExpectedPollIntervalSeconds` in the detail
         delta still reflect the performance poll, not the 60s operational
         cadence

    3. Update the existing contradictory worker regressions so they align with
       Phase 45 semantics instead of continuing to assert mixed-tier overwrite
       behavior:
       - `TestPipelineOrchestratorStaticTaskUpdatesStorePersistsTopologyAndSignalsNotify`
         must seed prior performance freshness metadata, run the static task,
         and then assert `LastPolledAt` / `ExpectedPollIntervalSeconds` remain
         on the earlier performance-owned values while topology persistence and
         notify behavior still pass
       - `TestPipelineOrchestratorRunTask_OperationalPollSendsDetailDeltaToSubscribedClient`
         must either be renamed/replaced by the new mixed-tier detail test or
         updated in place so operational detail delivery keeps the performance
         `ExpectedPollIntervalSeconds` instead of switching to `60`

    4. Keep the existing detail-subscription contract intact:
       - message type stays `snapshot_delta`
       - no new websocket family
       - no broadening of overview payload sections
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia && rtk go test ./internal/worker -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n 'BroadcastOnce_MixedTierPollsKeepPerformanceFreshnessMetadata|DetailDeltaKeepsPerformanceFreshnessMetadataAfterOperationalPoll' internal/worker/pipeline_test.go` returns 2 matches
    - `rtk rg -n 'TestPipelineOrchestratorStaticTaskUpdatesStorePersistsTopologyAndSignalsNotify|TestPipelineOrchestratorRunTask_OperationalPollSendsDetailDeltaToSubscribedClient' internal/worker/pipeline_test.go` returns matches in the updated contradictory tests
    - `rtk rg -n 'MessageTypeSnapshotDelta|ExpectedPollIntervalSeconds|LastPolledAt' internal/worker/pipeline_test.go` returns matches in the new and updated regression cases
    - `cd /home/azmin/projects/theia && rtk go test ./internal/worker -count=1 -v` exits 0
  </acceptance_criteria>
  <done>
    Mixed-tier runtime tests now prove that overview broadcasts and subscribed
    detail deltas cannot overwrite performance freshness/cadence metadata.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| collector results -> state store | Different volatility classes write into the shared runtime state for one device |
| state store -> websocket snapshot | Backend metadata becomes operator-visible freshness and cadence labels on the shared frontend snapshot atom |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-04 | T | `internal/state/store.go` mixed-tier state writes | mitigate | Restrict `LastPolledAt`, `ExpectedInterval`, and `Stale=false` to performance-owned (and legacy compatibility) paths so operational/static updates cannot tamper with performance freshness metadata. |
| T-45-05 | D | freshness/staleness semantics in overview websocket payloads | mitigate | Prevent operational/static polls from refreshing cadence metadata, then add runtime tests proving stale performance metrics are no longer masked by unrelated polls. |
| T-45-06 | T | `internal/worker/pipeline.go` targeted detail delta path | mitigate | Keep detail traffic on `snapshot_delta` but assert through pipeline websocket tests that post-operational detail sends preserve performance-owned `LastPolledAt` and `ExpectedPollIntervalSeconds`. |
</threat_model>

<verification>
- `cd /home/azmin/projects/theia && rtk go test ./internal/state -run 'TestStoreUpdate_(OperationalPoll|StaticPoll|FailedPerformancePoll)' -count=1 -v`
- `cd /home/azmin/projects/theia && rtk go test ./internal/worker -count=1 -v`
</verification>

<success_criteria>
- Operational and static polls no longer advance overview freshness/cadence metadata.
- Overview broadcasts keep performance-owned `last_polled_at` and `expected_poll_interval_seconds` after mixed-tier runtime activity.
- Targeted detail deltas remain on `snapshot_delta` while preserving performance-owned cadence semantics in the shared snapshot atom.
</success_criteria>

<output>
After completion, create `.planning/phases/45-polling-cadence-gap-closure/45-02-SUMMARY.md`
</output>
