---
phase: 44-frontend-integration
plan: 04
type: execute
wave: 2
depends_on: ["44-02"]
files_modified:
  - frontend/src/components/DeviceConfigPanel.tsx
  - frontend/src/components/DeviceConfigPanel.test.tsx
autonomous: true
requirements:
  - POLL-06
user_setup: []

must_haves:
  truths:
    - "The device configuration panel shows the assigned default cadence as read-only context and lets the operator keep that default or set a per-device seconds override."
    - "Changing the override saves inline from the polling section, updates the device resource through `updateDevice()`, and takes effect without a page refresh."
    - "The polling section remains override-first; it does not become a poll-class editor."
  artifacts:
    - path: "frontend/src/components/DeviceConfigPanel.tsx"
      provides: "Device-backed polling override UI with inline save and validation"
      contains: "Default cadence:"
    - path: "frontend/src/components/DeviceConfigPanel.test.tsx"
      provides: "Regression coverage for default/preset/custom override behavior"
      contains: "Polling Override"
  key_links:
    - from: "frontend/src/components/DeviceConfigPanel.tsx"
      to: "frontend/src/api/client.ts"
      via: "Polling section saves through `updateDevice()` with `poll_interval_override`"
      pattern: "updateDevice\\(device\\.id, \\{ poll_interval_override:"
    - from: "frontend/src/components/DeviceConfigPanel.tsx"
      to: "frontend/src/types/api.ts"
      via: "UI derives default cadence and current override from `device.poll_class` and `device.poll_interval_override`"
      pattern: "poll_class|poll_interval_override"
---

<objective>
Replace the legacy settings-key polling section in `DeviceConfigPanel` with the
device-backed override-first workflow defined in the Phase 44 context and UI
contract.

Purpose:
- Finish POLL-06 by wiring the operator-facing override UX to the new device
  API contract from 44-02.
- Preserve the existing inline-save feel while removing the old
  `polling_interval_seconds:{device.id}` settings dependency.
- Keep class editing out of scope per D-09.

Output:
- Device polling section shows `Default cadence: every {duration} ({class} class)`.
- Default, preset, and custom override flows save through `updateDevice()`.
- Tests cover default, preset, custom, clear, and invalid-value behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/44-frontend-integration/44-CONTEXT.md
@.planning/phases/44-frontend-integration/44-UI-SPEC.md
@frontend/src/components/DeviceConfigPanel.tsx
@frontend/src/components/DeviceConfigPanel.test.tsx
@frontend/src/types/api.ts
@frontend/src/api/client.ts
@internal/api/device_handler.go

<interfaces>
Device contract after 44-02:
```ts
export interface Device {
  poll_class: 'core' | 'standard' | 'low';
  poll_interval_override: number | null;
}
```

Device update contract after 44-02:
```ts
export async function updateDevice(
  id: string,
  payload: Partial<{
    poll_interval_override: number | null;
  }>,
): Promise<Device>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the legacy settings-key polling section with a device-backed override-first UI</name>
  <files>
    frontend/src/components/DeviceConfigPanel.tsx
  </files>
  <read_first>
    - .planning/phases/44-frontend-integration/44-CONTEXT.md (D-07 through D-10)
    - .planning/phases/44-frontend-integration/44-UI-SPEC.md (required copy, placeholder, and inline-save contract)
    - frontend/src/components/DeviceConfigPanel.tsx (current pollingKey/settings-based section)
    - frontend/src/types/api.ts (device poll fields added in 44-02)
    - frontend/src/api/client.ts (`updateDevice()` payload shape from 44-02)
    - internal/api/device_handler.go (backend validation range for override seconds)
  </read_first>
  <behavior>
    - the section is override-first per D-09: show default cadence context, then let the operator keep default or set seconds
    - saving happens inline in the polling section per D-10 and updates the device resource immediately
    - the panel does not become a poll-class editor
  </behavior>
  <action>
    Update the polling section in `frontend/src/components/DeviceConfigPanel.tsx`
    exactly as follows:

    1. Remove the legacy device-specific settings-key wiring for polling only:
       - remove `const pollingKey = \`polling_interval_seconds:${device.id}\``
       - remove polling writes through `updateSetting(...)`
       - stop reading polling defaults from `fetchSettings()` on mount

       Keep `fetchSettings()` and `updateSetting()` usage for Grafana URL only.

    2. Replace the polling preset list with these exact options:
       ```ts
       const POLLING_PRESETS = [
         { label: 'Use device default', value: 'default' },
         { label: '15 seconds', value: '15' },
         { label: '30 seconds', value: '30' },
         { label: '60 seconds', value: '60' },
         { label: '5 minutes', value: '300' },
         { label: 'Custom...', value: 'custom' },
       ];
       ```

    3. Derive the current polling UI state from `device.poll_interval_override`:
       - `null` => select `default`
       - `15`, `30`, `60`, or `300` => select that preset
       - any other valid integer => select `custom` and seed the input with that value

    4. Add a read-only context block above the select with this exact copy:
       `Default cadence: every {duration} ({class} class)`

       Use the device poll class from `device.poll_class` and map durations:
       - `core -> every 30s`
       - `standard -> every 1m`
       - `low -> every 5m`

    5. Keep the existing inline `Saved` badge and implement polling saves through
       `updateDevice(device.id, { poll_interval_override: ... })`:
       - selecting `default` sends `poll_interval_override: null`
       - selecting a preset sends that integer
       - custom input sends the parsed integer after validation
       - on success: show `Saved` and call `onDeviceUpdated(updated)`

    6. Keep the custom input placeholder exactly:
       `Seconds (5-3600)`

    7. Validate custom override input as an integer between 5 and 3600:
       - invalid input shows:
         `Polling override must be an integer between 5 and 3600 seconds`
       - invalid input must not call `updateDevice(...)`

    8. Preserve the rest of `DeviceConfigPanel` behavior unchanged:
       - `Save Changes` still handles the broader device form
       - Grafana URL still saves through settings
       - no modal or full-panel submit button is added for polling
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia/frontend && rtk npm test -- src/components/DeviceConfigPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n 'Use device default|Custom\\.\\.\\.|Default cadence:' frontend/src/components/DeviceConfigPanel.tsx` returns matches
    - `! rtk rg -n 'polling_interval_seconds:' frontend/src/components/DeviceConfigPanel.tsx`
    - `rtk rg -n 'poll_interval_override' frontend/src/components/DeviceConfigPanel.tsx` returns matches
    - `rtk rg -n 'Seconds \\(5-3600\\)' frontend/src/components/DeviceConfigPanel.tsx` returns 1 match
    - `rtk rg -n 'Polling override must be an integer between 5 and 3600 seconds' frontend/src/components/DeviceConfigPanel.tsx` returns 1 match
    - `cd /home/azmin/projects/theia/frontend && rtk npm test -- src/components/DeviceConfigPanel.test.tsx` exits 0
  </acceptance_criteria>
  <done>
    The device panel polling section now saves a device-backed override instead
    of writing a legacy settings key.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add panel regression tests for default, preset, custom, and clear override flows</name>
  <files>
    frontend/src/components/DeviceConfigPanel.test.tsx
  </files>
  <read_first>
    - frontend/src/components/DeviceConfigPanel.test.tsx (existing mock style and render helpers)
    - frontend/src/components/DeviceConfigPanel.tsx (updated polling section from Task 1)
    - frontend/src/api/client.ts (`updateDevice` import and mock target)
    - .planning/phases/44-frontend-integration/44-UI-SPEC.md (required labels and placeholder)
  </read_first>
  <behavior>
    - tests prove the section is device-backed and no longer settings-key-backed
    - tests cover both clearing and setting override values
    - invalid custom values are blocked before the API call
  </behavior>
  <action>
    Extend `frontend/src/components/DeviceConfigPanel.test.tsx` with these exact
    scenarios:

    1. Seed the `mockDevice(...)` helper with:
       ```ts
       poll_class: 'core',
       poll_interval_override: null,
       ```

    2. Add:
       - `renders default cadence context from device poll class`
       - `selecting Use device default sends poll_interval_override null`
       - `selecting 30 seconds sends poll_interval_override 30`
       - `custom override initializes from non-preset device value`
       - `invalid custom override blocks updateDevice and shows validation error`
       - `successful polling save shows Saved and calls onDeviceUpdated`

    3. In the tests, assert the exact copy:
       - `Polling Override`
       - `Default cadence: every 30s (core class)`
       - `Use device default`
       - `Seconds (5-3600)`

    4. Verify API behavior by asserting `updateDevice` receives:
       - `{ poll_interval_override: null }`
       - `{ poll_interval_override: 30 }`
       - `{ poll_interval_override: 123 }` for a valid custom case

    5. For invalid custom values such as `4` and `3601`, assert:
       - the validation error text is rendered
       - `updateDevice` is not called
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia/frontend && rtk npm test -- src/components/DeviceConfigPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n 'Default cadence: every 30s \\(core class\\)' frontend/src/components/DeviceConfigPanel.test.tsx` returns 1 match
    - `rtk rg -n 'poll_interval_override: null|poll_interval_override: 30|poll_interval_override: 123' frontend/src/components/DeviceConfigPanel.test.tsx` returns matches
    - `rtk rg -n 'invalid custom override blocks updateDevice and shows validation error|successful polling save shows Saved and calls onDeviceUpdated' frontend/src/components/DeviceConfigPanel.test.tsx` returns 2 matches
    - `cd /home/azmin/projects/theia/frontend && rtk npm test -- src/components/DeviceConfigPanel.test.tsx` exits 0
  </acceptance_criteria>
  <done>
    DeviceConfigPanel tests now lock the Phase 44 override UX and payload semantics in place.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator input -> `updateDevice()` | Poll override seconds entered in the panel become persisted device configuration |
| device resource -> panel state | Backend poll-class and override fields seed the operator-facing controls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-13 | Tampering | `frontend/src/components/DeviceConfigPanel.tsx` override input | mitigate | Mirror backend validation by blocking custom values outside `5..3600` and only sending `null` or integers through `updateDevice()`. Severity: MEDIUM. |
| T-44-14 | Denial of Service | device polling override UI | mitigate | Prevent invalid or partial custom values from triggering repeated update calls while typing. Severity: MEDIUM. |
| T-44-15 | Repudiation | inline polling saves | mitigate | Tests verify the exact payloads for clear/preset/custom saves so the operator action matches persisted state. Severity: LOW. |
| T-44-16 | Information Disclosure | default cadence context line | accept | The panel displays only already-authorized device polling metadata. Severity: LOW. |

Security gate status: ASVS L1, block_on=high. No unresolved HIGH-severity issue remains because backend validation is established in 44-02 and the panel mirrors it here.
</threat_model>

<verification>
- `cd /home/azmin/projects/theia/frontend && rtk npm test -- src/components/DeviceConfigPanel.test.tsx`
- `rtk rg -n 'Default cadence:|poll_interval_override' frontend/src/components/DeviceConfigPanel.tsx frontend/src/components/DeviceConfigPanel.test.tsx`
</verification>

<success_criteria>
- The polling section shows the assigned default cadence and saves overrides inline through `updateDevice()`.
- The operator can clear an override, choose a preset, or set a valid custom seconds value without refreshing the page.
- The section remains override-first and does not expose direct class-editing controls.
</success_criteria>

<output>
After completion, create `.planning/phases/44-frontend-integration/44-04-SUMMARY.md`
</output>
