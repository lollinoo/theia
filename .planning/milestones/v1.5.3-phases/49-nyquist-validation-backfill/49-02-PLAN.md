---
phase: 49-nyquist-validation-backfill
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/40-collectors/40-VALIDATION.md
  - .planning/phases/42-pipeline-orchestrator-cutover/42-VALIDATION.md
  - .planning/phases/44-frontend-integration/44-VALIDATION.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Phase 40 has a Nyquist validation artifact that maps PIPE-01, PIPE-02, and PIPE-04 to accepted automated evidence plus the finalized HUMAN-UAT record, including the accepted `probe_success` environment limitation."
    - "Phase 42 has a Nyquist validation artifact that maps PIPE-03 to accepted automated evidence plus the finalized HUMAN-UAT record for live cutover, topology ordering, and classified scheduling."
    - "Phase 44 has a Nyquist validation artifact that maps WS-01, WS-03, WS-04, and POLL-06 to accepted automated evidence plus the finalized HUMAN-UAT record for canvas visuals and override flow."
    - "All three new validation artifacts set `nyquist_compliant: true` and preserve the repo's validation-doc structure while reflecting already-closed human/runtime proof instead of reopening it."
  artifacts:
    - path: ".planning/phases/40-collectors/40-VALIDATION.md"
      provides: "Backfilled validation contract for the Phase 40 collector work"
      contains: "PIPE-01"
      contains_2: "accepted environment limitation"
    - path: ".planning/phases/42-pipeline-orchestrator-cutover/42-VALIDATION.md"
      provides: "Backfilled validation contract for the Phase 42 cutover work"
      contains: "PIPE-03"
      contains_2: "Live Cutover Smoke Test"
    - path: ".planning/phases/44-frontend-integration/44-VALIDATION.md"
      provides: "Backfilled validation contract for the Phase 44 frontend integration work"
      contains: "POLL-06"
      contains_2: "Canvas card visual integration"
  key_links:
    - from: ".planning/phases/40-collectors/40-VALIDATION.md"
      to: ".planning/phases/40-collectors/40-HUMAN-UAT.md"
      via: "The validation doc must preserve the finalized live SNMP proof and accepted Prometheus limitation rather than inventing new runtime debt."
      pattern: "accepted environment limitation|probe_success|passed: 1|skipped: 1"
    - from: ".planning/phases/42-pipeline-orchestrator-cutover/42-VALIDATION.md"
      to: ".planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md"
      via: "The validation doc must record that the three live runtime checks are already closed by finalized HUMAN-UAT."
      pattern: "Live Cutover Smoke Test|Topology Ordering Check|Classified Scheduling Smoke Test"
    - from: ".planning/phases/44-frontend-integration/44-VALIDATION.md"
      to: ".planning/phases/44-frontend-integration/44-HUMAN-UAT.md"
      via: "The validation doc must record that the three operator-visible checks are already closed by finalized HUMAN-UAT."
      pattern: "Canvas card visual integration|Polling override interaction flow|Next poll cycle runtime effect"
---

<objective>
Backfill the missing Nyquist validation artifacts for the v1.5.3 phases that
blend automated evidence with already-finalized human/runtime proof: 40, 42,
and 44.

Purpose:
- Convert existing verifier plus HUMAN-UAT evidence into durable
  `*-VALIDATION.md` artifacts without changing product code.
- Preserve closed human/runtime proof exactly as recorded instead of
  re-opening or rewriting it.
- Capture the accepted Phase 40 environment limitation explicitly so Nyquist
  compliance is truthful, not optimistic.

Output:
- `.planning/phases/40-collectors/40-VALIDATION.md`
- `.planning/phases/42-pipeline-orchestrator-cutover/42-VALIDATION.md`
- `.planning/phases/44-frontend-integration/44-VALIDATION.md`
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.5.3-MILESTONE-AUDIT.md
@.planning/phases/38-state-engine/38-VALIDATION.md
@.planning/milestones/v1.5.0-phases/24-backend-api-profiles-assignments-winbox-credentials/24-VALIDATION.md
@.planning/phases/40-collectors/40-01-PLAN.md
@.planning/phases/40-collectors/40-02-PLAN.md
@.planning/phases/40-collectors/40-03-PLAN.md
@.planning/phases/40-collectors/40-04-PLAN.md
@.planning/phases/40-collectors/40-VERIFICATION.md
@.planning/phases/40-collectors/40-HUMAN-UAT.md
@.planning/phases/42-pipeline-orchestrator-cutover/42-01-PLAN.md
@.planning/phases/42-pipeline-orchestrator-cutover/42-02-PLAN.md
@.planning/phases/42-pipeline-orchestrator-cutover/42-03-PLAN.md
@.planning/phases/42-pipeline-orchestrator-cutover/42-04-PLAN.md
@.planning/phases/42-pipeline-orchestrator-cutover/42-VERIFICATION.md
@.planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md
@.planning/phases/44-frontend-integration/44-01-PLAN.md
@.planning/phases/44-frontend-integration/44-02-PLAN.md
@.planning/phases/44-frontend-integration/44-03-PLAN.md
@.planning/phases/44-frontend-integration/44-04-PLAN.md
@.planning/phases/44-frontend-integration/44-VERIFICATION.md
@.planning/phases/44-frontend-integration/44-HUMAN-UAT.md

<interfaces>
Validation docs created in this plan must keep the existing structure and
explicitly record closed human/runtime proof in `## Manual-Only Verifications`
instead of leaving it implicit in separate artifacts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Phase 40 validation artifact without losing the accepted Prometheus limitation</name>
  <files>.planning/phases/40-collectors/40-VALIDATION.md</files>
  <read_first>
    - .planning/phases/40-collectors/40-VALIDATION.md (create if missing)
    - .planning/phases/38-state-engine/38-VALIDATION.md
    - .planning/milestones/v1.5.0-phases/24-backend-api-profiles-assignments-winbox-credentials/24-VALIDATION.md
    - .planning/phases/40-collectors/40-01-PLAN.md
    - .planning/phases/40-collectors/40-02-PLAN.md
    - .planning/phases/40-collectors/40-03-PLAN.md
    - .planning/phases/40-collectors/40-04-PLAN.md
    - .planning/phases/40-collectors/40-VERIFICATION.md
    - .planning/phases/40-collectors/40-HUMAN-UAT.md
  </read_first>
  <action>
    Create `.planning/phases/40-collectors/40-VALIDATION.md` as a completed
    Nyquist validation artifact for the already-shipped Phase 40 collector
    work.

    Use these exact frontmatter values:
    - `phase: 40`
    - `slug: collectors`
    - `status: verified`
    - `nyquist_compliant: true`
    - `wave_0_complete: true`
    - `created:` set to the execution date

    Populate the body from accepted Phase 40 evidence:
    - Reuse the validation-doc structure already used by Phases 24 and 38.
    - Build the automated sections from the exact commands and requirement
      mapping already accepted in `40-VERIFICATION.md`.
    - Cover all three Phase 40 requirement IDs: `PIPE-01`, `PIPE-02`,
      and `PIPE-04`.
    - In `## Manual-Only Verifications`, mirror the two live checks already
      recorded in `40-HUMAN-UAT.md` and preserve the wording that the missing
      Prometheus `probe_success` series is an accepted environment limitation
      in this lab.
    - In the sign-off section, make it explicit that HUMAN-UAT is finalized
      with `passed: 1`, `skipped: 1`, `pending: 0`, so the file remains
      Nyquist-compliant without inventing new automation.
  </action>
  <verify>
    <automated>rtk bash -lc 'test -f .planning/phases/40-collectors/40-VALIDATION.md && rg -n "^phase: 40$|^nyquist_compliant: true$|PIPE-01|PIPE-02|PIPE-04|accepted environment limitation|probe_success" .planning/phases/40-collectors/40-VALIDATION.md'</automated>
  </verify>
  <acceptance_criteria>
    - `rtk test -f .planning/phases/40-collectors/40-VALIDATION.md` exits 0
    - `rtk rg -n '^phase: 40$' .planning/phases/40-collectors/40-VALIDATION.md` returns 1 match
    - `rtk rg -n '^nyquist_compliant: true$' .planning/phases/40-collectors/40-VALIDATION.md` returns 1 match
    - `rtk rg -n 'PIPE-01|PIPE-02|PIPE-04' .planning/phases/40-collectors/40-VALIDATION.md` returns all three requirement IDs
    - `rtk rg -n 'accepted environment limitation|probe_success' .planning/phases/40-collectors/40-VALIDATION.md` returns at least 1 match
    - `rtk rg -n 'passed: 1|skipped: 1|pending: 0' .planning/phases/40-collectors/40-VALIDATION.md` returns the finalized HUMAN-UAT summary values
  </acceptance_criteria>
  <done>The Phase 40 validation artifact exists, is Nyquist-compliant, and preserves the accepted Prometheus environment limitation truthfully.</done>
</task>

<task type="auto">
  <name>Task 2: Create the Phase 42 validation artifact from accepted cutover and HUMAN-UAT evidence</name>
  <files>.planning/phases/42-pipeline-orchestrator-cutover/42-VALIDATION.md</files>
  <read_first>
    - .planning/phases/42-pipeline-orchestrator-cutover/42-VALIDATION.md (create if missing)
    - .planning/phases/38-state-engine/38-VALIDATION.md
    - .planning/phases/42-pipeline-orchestrator-cutover/42-01-PLAN.md
    - .planning/phases/42-pipeline-orchestrator-cutover/42-02-PLAN.md
    - .planning/phases/42-pipeline-orchestrator-cutover/42-03-PLAN.md
    - .planning/phases/42-pipeline-orchestrator-cutover/42-04-PLAN.md
    - .planning/phases/42-pipeline-orchestrator-cutover/42-VERIFICATION.md
    - .planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md
  </read_first>
  <action>
    Create `.planning/phases/42-pipeline-orchestrator-cutover/42-VALIDATION.md`
    as a completed Nyquist validation artifact for the already-shipped Phase 42
    cutover work.

    Use these exact frontmatter values:
    - `phase: 42`
    - `slug: pipeline-orchestrator-cutover`
    - `status: verified`
    - `nyquist_compliant: true`
    - `wave_0_complete: true`
    - `created:` set to the execution date

    Populate the body from accepted Phase 42 evidence:
    - Build the automated sections from the exact commands and package/runtime
      evidence already accepted in `42-VERIFICATION.md`.
    - Cover the single requirement ID `PIPE-03`.
    - In `## Manual-Only Verifications`, mirror the three finalized human checks
      already recorded in `42-HUMAN-UAT.md`:
      `Live Cutover Smoke Test`, `Topology Ordering Check`, and
      `Classified Scheduling Smoke Test`.
    - Make the sign-off explicit that HUMAN-UAT is finalized with `passed: 3`,
      `issues: 0`, `pending: 0`, `skipped: 0`, `blocked: 0`.
    - Do not invent new runtime checks; this file must memorialize the proof
      that already exists.
  </action>
  <verify>
    <automated>rtk bash -lc 'test -f .planning/phases/42-pipeline-orchestrator-cutover/42-VALIDATION.md && rg -n "^phase: 42$|^nyquist_compliant: true$|PIPE-03|Live Cutover Smoke Test|Topology Ordering Check|Classified Scheduling Smoke Test" .planning/phases/42-pipeline-orchestrator-cutover/42-VALIDATION.md'</automated>
  </verify>
  <acceptance_criteria>
    - `rtk test -f .planning/phases/42-pipeline-orchestrator-cutover/42-VALIDATION.md` exits 0
    - `rtk rg -n '^phase: 42$' .planning/phases/42-pipeline-orchestrator-cutover/42-VALIDATION.md` returns 1 match
    - `rtk rg -n '^nyquist_compliant: true$' .planning/phases/42-pipeline-orchestrator-cutover/42-VALIDATION.md` returns 1 match
    - `rtk rg -n 'PIPE-03' .planning/phases/42-pipeline-orchestrator-cutover/42-VALIDATION.md` returns at least 1 match
    - `rtk rg -n 'Live Cutover Smoke Test|Topology Ordering Check|Classified Scheduling Smoke Test' .planning/phases/42-pipeline-orchestrator-cutover/42-VALIDATION.md` returns all three finalized live checks
    - `rtk rg -n 'passed: 3|pending: 0|blocked: 0' .planning/phases/42-pipeline-orchestrator-cutover/42-VALIDATION.md` returns the finalized HUMAN-UAT summary values
  </acceptance_criteria>
  <done>The Phase 42 validation artifact exists, is Nyquist-compliant, and maps PIPE-03 to accepted automated plus finalized human cutover evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Create the Phase 44 validation artifact from accepted frontend and HUMAN-UAT evidence</name>
  <files>.planning/phases/44-frontend-integration/44-VALIDATION.md</files>
  <read_first>
    - .planning/phases/44-frontend-integration/44-VALIDATION.md (create if missing)
    - .planning/phases/38-state-engine/38-VALIDATION.md
    - .planning/phases/44-frontend-integration/44-01-PLAN.md
    - .planning/phases/44-frontend-integration/44-02-PLAN.md
    - .planning/phases/44-frontend-integration/44-03-PLAN.md
    - .planning/phases/44-frontend-integration/44-04-PLAN.md
    - .planning/phases/44-frontend-integration/44-VERIFICATION.md
    - .planning/phases/44-frontend-integration/44-HUMAN-UAT.md
  </read_first>
  <action>
    Create `.planning/phases/44-frontend-integration/44-VALIDATION.md` as a
    completed Nyquist validation artifact for the already-shipped Phase 44
    frontend integration work.

    Use these exact frontmatter values:
    - `phase: 44`
    - `slug: frontend-integration`
    - `status: verified`
    - `nyquist_compliant: true`
    - `wave_0_complete: true`
    - `created:` set to the execution date

    Populate the body from accepted Phase 44 evidence:
    - Build the automated sections from the exact backend/frontend commands and
      requirement mapping already accepted in `44-VERIFICATION.md`.
    - Cover all four requirement IDs: `WS-01`, `WS-03`, `WS-04`, and `POLL-06`.
    - In `## Manual-Only Verifications`, mirror the three finalized human checks
      already recorded in `44-HUMAN-UAT.md`:
      `Canvas card visual integration`, `Polling override interaction flow`,
      and `Next poll cycle runtime effect`.
    - Make the sign-off explicit that HUMAN-UAT is finalized with `passed: 3`,
      `issues: 0`, `pending: 0`, `skipped: 0`, `blocked: 0`.
    - Keep the document focused on accepted evidence only; do not introduce new
      UI or runtime obligations.
  </action>
  <verify>
    <automated>rtk bash -lc 'test -f .planning/phases/44-frontend-integration/44-VALIDATION.md && rg -n "^phase: 44$|^nyquist_compliant: true$|WS-01|WS-03|WS-04|POLL-06|Canvas card visual integration|Polling override interaction flow|Next poll cycle runtime effect" .planning/phases/44-frontend-integration/44-VALIDATION.md'</automated>
  </verify>
  <acceptance_criteria>
    - `rtk test -f .planning/phases/44-frontend-integration/44-VALIDATION.md` exits 0
    - `rtk rg -n '^phase: 44$' .planning/phases/44-frontend-integration/44-VALIDATION.md` returns 1 match
    - `rtk rg -n '^nyquist_compliant: true$' .planning/phases/44-frontend-integration/44-VALIDATION.md` returns 1 match
    - `rtk rg -n 'WS-01|WS-03|WS-04|POLL-06' .planning/phases/44-frontend-integration/44-VALIDATION.md` returns all four requirement IDs
    - `rtk rg -n 'Canvas card visual integration|Polling override interaction flow|Next poll cycle runtime effect' .planning/phases/44-frontend-integration/44-VALIDATION.md` returns all three finalized live checks
    - `rtk rg -n 'passed: 3|pending: 0|blocked: 0' .planning/phases/44-frontend-integration/44-VALIDATION.md` returns the finalized HUMAN-UAT summary values
  </acceptance_criteria>
  <done>The Phase 44 validation artifact exists, is Nyquist-compliant, and maps WS-01/WS-03/WS-04/POLL-06 to accepted automated plus finalized human evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan edits only planning artifacts. The trust boundary is between accepted
verifier/HUMAN-UAT evidence and the newly created validation docs that will be
used to justify milestone Nyquist compliance.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-04 | Tampering | 40/42/44 validation docs | mitigate | Build each validation doc directly from the accepted verification report plus finalized HUMAN-UAT artifact; do not invent new runtime outcomes |
| T-49-05 | Integrity | Phase 40 Prometheus limitation tracking | mitigate | Preserve the accepted `probe_success` environment limitation explicitly so Nyquist compliance is truthful |
| T-49-06 | Repudiation | Human-proof closure claims | mitigate | Copy the exact finalized human-check names and summary counts from HUMAN-UAT into the validation docs and sign-off |
</threat_model>

<verification>
After all three tasks complete:
1. `rtk test -f .planning/phases/40-collectors/40-VALIDATION.md`
2. `rtk test -f .planning/phases/42-pipeline-orchestrator-cutover/42-VALIDATION.md`
3. `rtk test -f .planning/phases/44-frontend-integration/44-VALIDATION.md`
4. `rtk rg -n '^nyquist_compliant: true$' .planning/phases/40-collectors/40-VALIDATION.md .planning/phases/42-pipeline-orchestrator-cutover/42-VALIDATION.md .planning/phases/44-frontend-integration/44-VALIDATION.md` returns 3 matches
5. `rtk rg -n 'accepted environment limitation|probe_success' .planning/phases/40-collectors/40-VALIDATION.md` returns at least 1 match
6. `rtk rg -n 'Live Cutover Smoke Test|Topology Ordering Check|Classified Scheduling Smoke Test' .planning/phases/42-pipeline-orchestrator-cutover/42-VALIDATION.md` returns all 3 matches
7. `rtk rg -n 'Canvas card visual integration|Polling override interaction flow|Next poll cycle runtime effect' .planning/phases/44-frontend-integration/44-VALIDATION.md` returns all 3 matches
</verification>

<success_criteria>
1. Phase 40 has a Nyquist validation artifact that truthfully preserves the accepted Prometheus environment limitation while remaining compliant.
2. Phase 42 has a Nyquist validation artifact that records both PIPE-03 automation and the finalized cutover HUMAN-UAT closure.
3. Phase 44 has a Nyquist validation artifact that records both backend/frontend automation and the finalized canvas/override HUMAN-UAT closure.
4. None of the three docs reopen already-closed human/runtime proof.
</success_criteria>

<output>
After completion, create `.planning/phases/49-nyquist-validation-backfill/49-02-SUMMARY.md`.
</output>
