---
phase: 44-frontend-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/worker/snapshot_builder.go
  - internal/worker/snapshot_builder_test.go
autonomous: true
requirements:
  - WS-01
  - WS-03
  - WS-04
user_setup: []

must_haves:
  truths:
    - "Overview `snapshot` payloads carry backend-owned `health`, freshness timestamps, and polling interval seconds for every canvas device, not only selected detail devices."
    - "A device that has not completed its first successful poll still exposes `health=unknown` plus an expected polling interval so the canvas can render cadence and a waiting/dead freshness state without opening the side panel."
    - "Phase 44 continues to use the existing `snapshot` and `snapshot_delta` transport; no new websocket message family is introduced."
  artifacts:
    - path: "internal/worker/snapshot_builder.go"
      provides: "Overview snapshot enrichment with health, freshness, reachability, and expected poll interval metadata"
      contains: "buildPipelineSnapshot"
    - path: "internal/worker/snapshot_builder_test.go"
      provides: "Regression coverage for overview metadata fields and pre-first-poll cadence fallback"
      contains: "TestBuildPipelineSnapshot"
  key_links:
    - from: "internal/worker/snapshot_builder.go"
      to: "internal/state/store.go"
      via: "Overview DTOs are enriched from `state.DeviceState` fields instead of frontend inference"
      pattern: "Health|Reachability|Stale|LastPolledAt|ExpectedInterval"
    - from: "internal/worker/snapshot_builder.go"
      to: "internal/domain/poll_class.go"
      via: "When no runtime interval exists yet, overview cadence falls back to `PollIntervalOverride` or `PollClass.Interval()`"
      pattern: "PollIntervalOverride|PollClass\\.Interval"
---

<objective>
Extend the existing overview snapshot builder so canvas cards receive the same
backend-owned health, freshness, and cadence metadata that detail updates
already carry.

Purpose:
- Implement D-14 and D-15 by making the shared snapshot atom sufficient for
  card-level status presentation.
- Remove the current Phase 44 blocker where overview cards lack `health`,
  `last_polled_at`, and `expected_poll_interval_seconds`.
- Preserve the Phase 42/43 websocket contract shape instead of inventing a new
  frontend data path.

Output:
- `buildPipelineSnapshot()` enriches overview `DeviceMetricsDTO` entries with
  `health`, `reachability`, `stale`, `last_polled_at`, and
  `expected_poll_interval_seconds`.
- Worker tests prove both the populated-state path and the pre-first-poll
  fallback path.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/44-frontend-integration/44-CONTEXT.md
@.planning/phases/44-frontend-integration/44-UI-SPEC.md
@.planning/phases/43-websocket-detail-on-demand/43-01-SUMMARY.md
@internal/worker/snapshot_builder.go
@internal/worker/snapshot_builder_test.go
@internal/ws/messages.go
@internal/state/store.go
@internal/domain/poll_class.go

<interfaces>
Existing DTO fields already available on the websocket contract:
```go
type DeviceMetricsDTO struct {
    DeviceID                    string   `json:"device_id"`
    CPUPercent                  *float64 `json:"cpu_percent"`
    MemPercent                  *float64 `json:"mem_percent"`
    TempCelsius                 *float64 `json:"temp_celsius"`
    UptimeSecs                  *float64 `json:"uptime_secs"`
    CollectedAt                 string   `json:"collected_at"`
    Health                      string   `json:"health,omitempty"`
    Reachability                string   `json:"reachability,omitempty"`
    Stale                       *bool    `json:"stale,omitempty"`
    LastPolledAt                string   `json:"last_polled_at,omitempty"`
    ExpectedPollIntervalSeconds *int64   `json:"expected_poll_interval_seconds,omitempty"`
}
```

Runtime source of truth for Phase 44 card metadata:
```go
type DeviceState struct {
    Health           HealthStatus
    Reachability     ReachabilityStatus
    Stale            bool
    LastPolledAt     time.Time
    ExpectedInterval time.Duration
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enrich overview snapshot device metrics with backend-owned health, freshness, and cadence fields</name>
  <files>
    internal/worker/snapshot_builder.go
  </files>
  <read_first>
    - .planning/phases/44-frontend-integration/44-CONTEXT.md (D-01, D-02, D-07, D-14, D-15)
    - .planning/phases/44-frontend-integration/44-UI-SPEC.md (freshness and cadence copy contract)
    - .planning/phases/43-websocket-detail-on-demand/43-01-SUMMARY.md (existing detail-field names on DeviceMetricsDTO)
    - internal/worker/snapshot_builder.go (current overview/detail snapshot split)
    - internal/ws/messages.go (exact DTO field names; do not rename them)
    - internal/state/store.go (state.DeviceState source fields)
    - internal/domain/poll_class.go (class interval fallback when ExpectedInterval is zero)
  </read_first>
  <behavior>
    - overview `DeviceMetricsDTO` entries carry the same additive status fields as detail deltas, but still use the existing top-level `device_metrics` map
    - the frontend remains backend-owned for health severity per D-01 and D-14; no raw-metric threshold logic moves client-side
    - a device with no successful poll yet still exposes `health=unknown` plus a cadence fallback so the card can render `Dead · Waiting for first poll`
    - no new websocket message type, map section, or panel-only state channel is introduced
  </behavior>
  <action>
    Update `internal/worker/snapshot_builder.go` so `buildPipelineSnapshot(...)`
    enriches each overview `DeviceMetricsDTO` after `ws.DeviceMetricsToDTOs(...)`
    with the same field names already used by Phase 43 detail deltas:

    1. Set `dto.Health` from `deviceState.Health`. If the state value is the
       zero value, write `string(state.HealthStatusUnknown)` explicitly.

    2. Set `dto.Reachability` from `deviceState.Reachability` when non-empty.

    3. Set `dto.Stale = boolPtr(deviceState.Stale)` for both `true` and `false`
       states so the frontend receives an explicit secondary signal.

    4. Set `dto.LastPolledAt` only when `deviceState.LastPolledAt` is non-zero,
       using `UTC().Format(time.RFC3339)`.

    5. Populate `dto.ExpectedPollIntervalSeconds` from this exact precedence:
       - `deviceState.ExpectedInterval` when `> 0`
       - else `time.Duration(*device.PollIntervalOverride) * time.Second` when
         `device.PollIntervalOverride != nil`
       - else `device.PollClass.Interval()`

    6. Convert the chosen duration to seconds with:
       ```go
       dto.ExpectedPollIntervalSeconds = int64Ptr(int64(expectedInterval / time.Second))
       ```

    7. Keep the existing overview snapshot sections unchanged:
       - `device_metrics`
       - `link_metrics`
       - `alerts`
       - `device_statuses`
       - `device_hostnames`
       - `device_models`

    8. Do not add a new websocket message type and do not move this logic into
       `buildDeviceDetailDelta`; the missing contract is the overview path.
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia && rtk go test ./internal/worker -run 'TestBuildPipelineSnapshot' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n 'HealthStatusUnknown|ExpectedPollIntervalSeconds|LastPolledAt|boolPtr\\(deviceState\\.Stale\\)' internal/worker/snapshot_builder.go` returns matches
    - `rtk rg -n 'PollIntervalOverride|PollClass\\.Interval' internal/worker/snapshot_builder.go` returns matches
    - `rtk rg -n 'MessageTypeSnapshotDelta' internal/worker/snapshot_builder.go` returns 1 or fewer matches from existing code only; no new message family is added
    - `cd /home/azmin/projects/theia && rtk go test ./internal/worker -run 'TestBuildPipelineSnapshot' -count=1 -v` exits 0
  </acceptance_criteria>
  <done>
    Overview websocket snapshots now expose the backend-owned fields the canvas
    cards need for health, freshness, and cadence rendering.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add worker regression tests for overview metadata and pre-first-poll cadence fallback</name>
  <files>
    internal/worker/snapshot_builder_test.go
  </files>
  <read_first>
    - internal/worker/snapshot_builder_test.go (existing overview/detail tests and helper naming)
    - internal/worker/snapshot_builder.go (new enrichment logic from Task 1)
    - internal/state/store.go (zero-value and populated-state semantics)
    - internal/domain/poll_class.go (expected interval fallback values)
  </read_first>
  <behavior>
    - tests prove overview payloads now carry Phase 44 metadata, not only detail deltas
    - tests cover both populated state-store values and the "waiting for first poll" fallback path
    - existing detail-delta and hash tests keep passing so Phase 44 remains additive
  </behavior>
  <action>
    Extend `internal/worker/snapshot_builder_test.go` with these exact cases:

    1. Update `TestBuildPipelineSnapshotPreservesOverviewSections` so the
       overview `device_metrics[deviceID]` assertion also verifies:
       - `Health == "healthy"` / `"warning"` / etc. from the seeded state
       - `Reachability` is present
       - `Stale` is non-nil and matches the seeded state
       - `LastPolledAt` matches the seeded RFC3339 timestamp
       - `ExpectedPollIntervalSeconds` matches the seeded interval

    2. Add:
       `TestBuildPipelineSnapshot_FallsBackToPollOverrideAndUnknownHealthBeforeFirstPoll`
       with a device that has:
       - `PollClass = domain.PollClassCore`
       - `PollIntervalOverride = intPtr(15)`
       - no `state.DeviceState` entry or a zero-value entry

       Assert:
       - `Health == string(state.HealthStatusUnknown)`
       - `ExpectedPollIntervalSeconds == 15`
       - `LastPolledAt == ""`
       - `Stale` is non-nil and `false`

    3. Add:
       `TestBuildPipelineSnapshot_FallsBackToPollClassIntervalWhenOverrideAbsent`
       with a `PollClassLow` device and no override, then assert
       `ExpectedPollIntervalSeconds == 300`.

    4. Leave the existing detail-delta tests intact; the new overview tests
       must be additive and keep `TestBuildDeviceDetailDelta_*` green.
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia && rtk go test ./internal/worker -run 'TestBuildPipelineSnapshot|TestBuildDeviceDetailDelta|TestComputeSnapshotHashes_DeviceMetricHashIncludesDetailFields' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n 'FallsBackToPollOverrideAndUnknownHealthBeforeFirstPoll' internal/worker/snapshot_builder_test.go` returns 1 match
    - `rtk rg -n 'FallsBackToPollClassIntervalWhenOverrideAbsent' internal/worker/snapshot_builder_test.go` returns 1 match
    - `rtk rg -n 'ExpectedPollIntervalSeconds' internal/worker/snapshot_builder_test.go` returns at least 3 matches
    - `cd /home/azmin/projects/theia && rtk go test ./internal/worker -run 'TestBuildPipelineSnapshot|TestBuildDeviceDetailDelta|TestComputeSnapshotHashes_DeviceMetricHashIncludesDetailFields' -count=1 -v` exits 0
  </acceptance_criteria>
  <done>
    Worker tests now lock the Phase 44 overview metadata contract in place.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| state store -> websocket snapshot | Internal runtime state is transformed into client-visible JSON for every canvas card |
| device poll config -> overview DTO | Poll-class and override data become operator-visible cadence metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering | `internal/worker/snapshot_builder.go` | mitigate | Populate `health` from `state.DeviceState.Health` and never from frontend-side raw metric inference. Severity: MEDIUM. |
| T-44-02 | Information Disclosure | overview websocket payload | accept | The new fields expose operational state only (`health`, timestamps, cadence); no credentials or secrets cross the boundary. Severity: LOW. |
| T-44-03 | Denial of Service | websocket contract churn | mitigate | Keep the existing `snapshot` / `snapshot_delta` sections and DTO names so Phase 44 does not create a second message family or duplicate stream. Severity: MEDIUM. |
| T-44-04 | Repudiation | pre-first-poll cadence fallback | mitigate | Regression tests prove the overview payload always includes a deterministic cadence fallback, preventing ambiguous card state before first poll. Severity: LOW. |

Security gate status: ASVS L1, block_on=high. No unresolved HIGH-severity issue remains if the overview builder uses backend-owned state fields and tests pass.
</threat_model>

<verification>
- `rtk go test ./internal/worker -run 'TestBuildPipelineSnapshot|TestBuildDeviceDetailDelta|TestComputeSnapshotHashes_DeviceMetricHashIncludesDetailFields' -count=1 -v`
- `rtk rg -n 'ExpectedPollIntervalSeconds|LastPolledAt|HealthStatusUnknown' internal/worker/snapshot_builder.go internal/worker/snapshot_builder_test.go`
</verification>

<success_criteria>
- Overview websocket snapshots expose `health`, `stale`, `last_polled_at`, and `expected_poll_interval_seconds` for every device card.
- Pre-first-poll devices still expose `health=unknown` and a deterministic cadence fallback.
- Existing detail-delta tests stay green, proving the change is additive to the current transport.
</success_criteria>

<output>
After completion, create `.planning/phases/44-frontend-integration/44-01-SUMMARY.md`
</output>
