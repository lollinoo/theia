---
phase: 06-canvas-token-migration-and-theme-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/__tests__/canvas-token-audit.test.ts
  - frontend/src/components/canvas/canvasHelpers.ts
  - frontend/src/App.tsx
  - frontend/src/components/ReconnectBanner.tsx
  - frontend/src/components/canvas/CanvasPanels.tsx
autonomous: true
requirements: [FOUND-06, THEME-05, COMP-12]

must_haves:
  truths:
    - "Automated test detects stale Tailwind v4 token names and hardcoded hex values in all 6 canvas-scope files"
    - "statusColor() returns CSS variable references instead of hardcoded hex, making MiniMap dots theme-adaptive"
    - "App.tsx root container uses valid bg-bg and text-on-bg tokens instead of stale bg-bg-canvas/text-text-primary"
    - "ReconnectBanner uses semantic warning tokens that are readable in both dark and light themes"
    - "CanvasPanels fallback text uses valid on-bg-secondary token"
  artifacts:
    - path: "frontend/src/components/__tests__/canvas-token-audit.test.ts"
      provides: "Source-scan test for stale tokens and hardcoded hex across 6 target files"
      min_lines: 50
    - path: "frontend/src/components/canvas/canvasHelpers.ts"
      provides: "statusColor() returning CSS variable references"
      contains: "var(--color-status-up)"
    - path: "frontend/src/App.tsx"
      provides: "Root container with valid token classes"
      contains: "bg-bg text-on-bg"
    - path: "frontend/src/components/ReconnectBanner.tsx"
      provides: "Reconnect banner with semantic warning tokens"
      contains: "bg-warning/15"
    - path: "frontend/src/components/canvas/CanvasPanels.tsx"
      provides: "Fallback text with valid token"
      contains: "text-on-bg-secondary"
  key_links:
    - from: "frontend/src/components/__tests__/canvas-token-audit.test.ts"
      to: "all 6 target files"
      via: "fs.readFileSync source scan"
      pattern: "bg-bg-canvas|text-text-primary|border-border-subtle|text-accent|#00c853|#ff1744"
    - from: "frontend/src/components/canvas/canvasHelpers.ts"
      to: "frontend/src/components/Canvas.tsx"
      via: "statusColor() import in MiniMap nodeColor callback"
      pattern: "statusColor\\(n\\.data\\.device\\.status\\)"
---

<objective>
Create the Nyquist validation test for stale canvas tokens and hardcoded hex detection, then migrate 4 smaller files (canvasHelpers.ts, App.tsx, ReconnectBanner.tsx, CanvasPanels.tsx) from stale/hardcoded values to valid theme-adaptive tokens.

Purpose: Establish the automated verification gate (Wave 0) and fix the simpler files first, leaving the two large canvas component files (Canvas.tsx, CanvasOverlays.tsx) for Plan 02.

Output: 1 new test file + 4 migrated source files, all with valid Tailwind v4 `@theme inline` token references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-canvas-token-migration-and-theme-compliance/06-RESEARCH.md

<interfaces>
<!-- Valid token names from @theme inline in frontend/src/index.css -->
<!-- Surface: bg, surface, surface-high, elevated -->
<!-- Text: on-bg, on-bg-secondary, on-bg-muted -->
<!-- Border: outline, outline-subtle -->
<!-- Status: status-up, status-down, status-probing, status-unknown -->
<!-- Accent: primary, secondary, tertiary, warning, critical -->
<!-- Raw CSS vars: --nt-outline, --nt-surface, --color-status-up, etc. -->

From frontend/src/components/canvas/canvasHelpers.ts (statusColor current):
```typescript
export function statusColor(status: Device['status']): string {
  switch (status) {
    case 'up':      return '#00c853';
    case 'down':    return '#ff1744';
    case 'probing': return '#ffc107';
    default:        return '#657786';
  }
}
```

From frontend/src/components/__tests__/no-line-audit.test.ts (existing scan pattern):
```typescript
// Uses readFileSync + readdirSync to scan component source files
// Pattern to follow for canvas-token-audit.test.ts
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create canvas-token-audit.test.ts (Wave 0 Nyquist scaffold)</name>
  <files>frontend/src/components/__tests__/canvas-token-audit.test.ts</files>
  <read_first>
    - frontend/src/components/__tests__/no-line-audit.test.ts (reference pattern for source-scanning tests)
    - frontend/src/index.css (lines 107-153 for valid @theme inline token names)
  </read_first>
  <action>
Create `frontend/src/components/__tests__/canvas-token-audit.test.ts` that source-scans the 6 target files for stale token patterns and hardcoded hex values. Follow the same pattern as `no-line-audit.test.ts` (readFileSync, describe/it, explicit violation reporting).

The test must scan these exact files (relative to `frontend/src/`):
- `App.tsx`
- `components/Canvas.tsx`
- `components/canvas/CanvasOverlays.tsx`
- `components/canvas/CanvasPanels.tsx`
- `components/canvas/canvasHelpers.ts`
- `components/ReconnectBanner.tsx`

**Stale token patterns to detect (regex):**
- `/bg-bg-canvas/` (should be `bg-bg`)
- `/bg-bg-surface/` (should be `bg-surface`)
- `/text-text-primary/` (should be `text-on-bg`)
- `/text-text-secondary/` (should be `text-on-bg-secondary`)
- `/border-border-subtle/` (should be `border-outline-subtle`)
- `/(?<![-\w])text-accent(?![-\w])/` (should be `text-primary`; negative lookbehind/ahead to avoid matching `text-accent-something`)
- `/border-accent/` (should be `border-primary`)
- `/bg-accent/` (should be `bg-primary`)
- `/border-t-accent/` (should be `border-t-primary`)

**Hardcoded hex patterns to detect (regex):**
- `/'#00c853'/` (status up hex)
- `/'#ff1744'/i` (status down hex)
- `/'#ffc107'/` (status probing hex)
- `/'#657786'/` (status unknown hex)
- `/'#4a4a5e'/` (connection line hex)
- `/'#3f3f53'/` (background dots hex)

**Fixed palette patterns to detect in overlays/banner:**
- `/bg-yellow-900/`
- `/text-yellow-200/`
- `/border-yellow-200/`
- `/bg-green-400/`
- `/text-green-300/`
- `/border-green-500/`
- `/bg-yellow-400/`
- `/text-yellow-300/`
- `/border-yellow-500/`
- `/text-yellow-400/`
- `/hover:text-yellow-300/`

Structure as two `describe` blocks:
1. `'FOUND-06 / THEME-05: No stale token names in canvas files'` - tests stale token patterns
2. `'FOUND-06: No hardcoded hex or fixed palette colors in canvas files'` - tests hex and fixed palette patterns

Each test should:
- Read each target file with `readFileSync`
- Scan every line against every pattern
- Collect violations as `{file}:{line}: {trimmed_content}`
- Log all violations to console.error before asserting
- `expect(violations).toHaveLength(0)`

Use `join(__dirname, '../../')` as the base path for `frontend/src/` since the test lives in `frontend/src/components/__tests__/`.
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia/frontend && npx vitest run src/components/__tests__/canvas-token-audit.test.ts -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `frontend/src/components/__tests__/canvas-token-audit.test.ts`
    - File contains `import { describe, it, expect } from 'vitest'`
    - File contains `readFileSync` import from `'fs'`
    - File contains `bg-bg-canvas` as a stale pattern string
    - File contains `#00c853` as a hardcoded hex pattern string
    - File contains `bg-yellow-900` as a fixed palette pattern string
    - File contains `App.tsx` and `Canvas.tsx` and `CanvasOverlays.tsx` and `canvasHelpers.ts` and `ReconnectBanner.tsx` and `CanvasPanels.tsx` in the target file list
    - Test runs and FAILS (expected -- stale tokens still present in source files) with violation count > 0
  </acceptance_criteria>
  <done>Test file created, runs successfully, and correctly detects all stale tokens/hex values in unmodified source files (test FAILs as expected since migration hasn't happened yet)</done>
</task>

<task type="auto">
  <name>Task 2: Migrate canvasHelpers.ts, App.tsx, ReconnectBanner.tsx, CanvasPanels.tsx to valid tokens</name>
  <files>
    frontend/src/components/canvas/canvasHelpers.ts
    frontend/src/App.tsx
    frontend/src/components/ReconnectBanner.tsx
    frontend/src/components/canvas/CanvasPanels.tsx
  </files>
  <read_first>
    - frontend/src/components/canvas/canvasHelpers.ts (current statusColor function with hex values)
    - frontend/src/App.tsx (line 61 with stale bg-bg-canvas text-text-primary)
    - frontend/src/components/ReconnectBanner.tsx (lines 9,14 with yellow-900, yellow-200 palette colors)
    - frontend/src/components/canvas/CanvasPanels.tsx (line 64 with text-text-secondary)
    - frontend/src/index.css (lines 107-153 for valid token names)
  </read_first>
  <action>
Make these exact replacements in each file:

**canvasHelpers.ts** -- `statusColor()` function (lines 81-92):
Replace the 4 hardcoded hex return values:
- `return '#00c853';` -> `return 'var(--color-status-up)';`
- `return '#ff1744';` -> `return 'var(--color-status-down)';`
- `return '#ffc107';` -> `return 'var(--color-status-probing)';`
- `return '#657786';` -> `return 'var(--color-status-unknown)';`

**App.tsx** -- line 61:
- `bg-bg-canvas text-text-primary` -> `bg-bg text-on-bg`
The full className should be: `"h-screen w-screen overflow-hidden bg-bg text-on-bg"`

**ReconnectBanner.tsx** -- line 9:
- `bg-yellow-900/80` -> `bg-warning/15`
- `text-yellow-200` -> `text-warning`

**ReconnectBanner.tsx** -- line 14:
- `border-yellow-200/30` -> `border-warning/30`
- `border-t-yellow-200` -> `border-t-warning`

**CanvasPanels.tsx** -- line 64:
- `text-text-secondary` -> `text-on-bg-secondary`
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia/frontend && npx vitest run src/components/__tests__/canvas-token-audit.test.ts -x 2>&1 | grep -E '(PASS|FAIL|Tests|✓|×|bg-bg-canvas|text-text-primary|#00c853|yellow-900)' | head -20</automated>
  </verify>
  <acceptance_criteria>
    - canvasHelpers.ts contains `return 'var(--color-status-up)';` (not `#00c853`)
    - canvasHelpers.ts contains `return 'var(--color-status-down)';` (not `#ff1744`)
    - canvasHelpers.ts contains `return 'var(--color-status-probing)';` (not `#ffc107`)
    - canvasHelpers.ts contains `return 'var(--color-status-unknown)';` (not `#657786`)
    - App.tsx contains `bg-bg text-on-bg` (not `bg-bg-canvas text-text-primary`)
    - ReconnectBanner.tsx contains `bg-warning/15` (not `bg-yellow-900/80`)
    - ReconnectBanner.tsx contains `text-warning` (not `text-yellow-200`)
    - ReconnectBanner.tsx contains `border-warning/30` (not `border-yellow-200/30`)
    - ReconnectBanner.tsx contains `border-t-warning` (not `border-t-yellow-200`)
    - CanvasPanels.tsx contains `text-on-bg-secondary` (not `text-text-secondary`)
    - No file in this plan contains any of: `#00c853`, `#ff1744`, `#ffc107`, `#657786`, `bg-bg-canvas`, `text-text-primary`, `bg-yellow-900`, `text-yellow-200`, `border-yellow-200`
  </acceptance_criteria>
  <done>All 4 files migrated from stale/hardcoded values to valid theme-adaptive tokens; canvas-token-audit test passes for these files' patterns</done>
</task>

</tasks>

<verification>
- `cd /home/azmin/projects/theia/frontend && npx vitest run src/components/__tests__/canvas-token-audit.test.ts -x` -- audit test passes (may still fail for Canvas.tsx/CanvasOverlays.tsx patterns addressed in Plan 02)
- `cd /home/azmin/projects/theia/frontend && npx vitest run -x` -- full suite passes (no regressions)
- `grep -r '#00c853\|#ff1744\|#ffc107\|#657786' frontend/src/components/canvas/canvasHelpers.ts` -- returns empty
- `grep 'bg-bg-canvas\|text-text-primary' frontend/src/App.tsx` -- returns empty
- `grep 'yellow-900\|yellow-200' frontend/src/components/ReconnectBanner.tsx` -- returns empty
</verification>

<success_criteria>
1. canvas-token-audit.test.ts exists and runs
2. statusColor() returns CSS variable references for all 4 status values
3. App.tsx root div uses `bg-bg text-on-bg`
4. ReconnectBanner uses semantic `warning` tokens, not fixed `yellow-*` palette
5. CanvasPanels uses `text-on-bg-secondary`
6. No hardcoded hex values remain in canvasHelpers.ts
7. Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/06-canvas-token-migration-and-theme-compliance/06-01-SUMMARY.md`
</output>
