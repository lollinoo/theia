---
phase: 02-component-restyling
plan: 05
type: execute
wave: 3
depends_on: [02-02,02-04]
files_modified:
  - frontend/src/components/SettingsPanel.tsx
  - frontend/src/components/SNMPProfileManager.tsx
  - frontend/src/components/SSHProfileManager.tsx
  - frontend/src/components/AddDevicePanel.tsx
  - frontend/src/components/DeviceConfigPanel.tsx
  - frontend/src/components/LinkCreatePanel.tsx
  - frontend/src/components/LinkDetailsPanel.tsx
  - frontend/src/components/InterfaceStatsPanel.tsx
autonomous: true
requirements: [COMP-05, COMP-08, COMP-09, COMP-12]
must_haves:
  truths:
    - "SettingsPanel section separators use spacing and surface tiers, not border-t"
    - "All form inputs across panels use the standard contract: bg-elevated, border-outline-subtle, focus:ring-primary"
    - "LinkDetailsPanel and InterfaceStatsPanel metric values render in JetBrains Mono"
    - "All panels have transition-colors for smooth theme switching"
  artifacts:
    - path: "frontend/src/components/SettingsPanel.tsx"
      provides: "SettingsPanel with no-line section separators and standardized form inputs"
    - path: "frontend/src/components/AddDevicePanel.tsx"
      provides: "AddDevicePanel with standardized form input styling"
    - path: "frontend/src/components/DeviceConfigPanel.tsx"
      provides: "DeviceConfigPanel with standardized form inputs"
    - path: "frontend/src/components/LinkDetailsPanel.tsx"
      provides: "LinkDetailsPanel with JetBrains Mono metrics"
    - path: "frontend/src/components/InterfaceStatsPanel.tsx"
      provides: "InterfaceStatsPanel with JetBrains Mono metrics"
  key_links:
    - from: "frontend/src/components/SettingsPanel.tsx"
      to: "frontend/src/components/SNMPProfileManager.tsx"
      via: "import SNMPProfileManager"
      pattern: "SNMPProfileManager"
    - from: "frontend/src/components/SettingsPanel.tsx"
      to: "frontend/src/components/SSHProfileManager.tsx"
      via: "import SSHProfileManager"
      pattern: "SSHProfileManager"
---

<objective>
Restyle all panel and form components (SettingsPanel, profile managers, AddDevicePanel, DeviceConfigPanel, LinkCreatePanel, LinkDetailsPanel, InterfaceStatsPanel) with standardized form input styling, no-line rule, and JetBrains Mono for metric values.

Purpose: These 8 components share the same form input patterns and panel layout conventions. Standardizing them in one plan ensures consistency and avoids divergent styling.
Output: All panels restyled with standard form contract, no-line rule, JetBrains Mono metrics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-restyling/02-CONTEXT.md
@.planning/phases/02-component-restyling/02-RESEARCH.md
@.planning/phases/02-component-restyling/02-UI-SPEC.md
@.planning/phases/02-component-restyling/02-02-SUMMARY.md
@.planning/phases/02-component-restyling/02-04-SUMMARY.md

<interfaces>
UI-SPEC Form Input Contract (MUST apply to ALL form inputs):
```
Background:    bg-elevated
Border:        border border-outline-subtle   (ghost border, NOT layout separator)
Border radius: rounded-lg  (8px)
Text:          text-sm text-on-bg  (14px, regular weight)
Placeholder:   placeholder-on-bg-muted
Focus:         focus:border-primary focus:ring-1 focus:ring-primary/30 focus:outline-none
Padding:       px-3 py-2  (12px horizontal, 8px vertical)
```

Combined class string for text/url/number inputs:
`w-full rounded-lg border border-outline-subtle bg-elevated px-3 py-2 text-sm text-on-bg placeholder-on-bg-muted focus:border-primary focus:ring-1 focus:ring-primary/30 focus:outline-none`

Combined class string for select elements:
`w-full rounded-lg border border-outline-subtle bg-elevated px-3 py-2 text-sm text-on-bg focus:border-primary focus:ring-1 focus:ring-primary/30 focus:outline-none`

Current form input patterns across components (from grep):
- SettingsPanel: `border border-outline bg-elevated` (missing ring, uses outline not outline-subtle)
- AddDevicePanel: `border border-outline bg-elevated` (same)
- DeviceConfigPanel: `border border-outline bg-elevated` (same)
- SNMPProfileManager: `border border-outline bg-elevated` (same)
- SSHProfileManager: `border border-outline bg-elevated` (same)
- LinkCreatePanel: `border border-outline bg-elevated` (same)

All need: `border-outline` -> `border-outline-subtle`, add `focus:ring-1 focus:ring-primary/30`, add `placeholder-on-bg-muted`.

SettingsPanel section separators to remove:
- Line 262: `border-t border-outline pt-4` (before SNMPProfileManager)
- Line 266: `border-t border-outline pt-4` (before SSHProfileManager)
- Line 271: `border-t border-outline pt-4 space-y-2` (before version info)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle SettingsPanel section separators and form inputs, plus SNMPProfileManager and SSHProfileManager</name>
  <files>frontend/src/components/SettingsPanel.tsx, frontend/src/components/SNMPProfileManager.tsx, frontend/src/components/SSHProfileManager.tsx</files>
  <read_first>
    - frontend/src/components/SettingsPanel.tsx
    - frontend/src/components/SNMPProfileManager.tsx
    - frontend/src/components/SSHProfileManager.tsx
  </read_first>
  <action>
**SettingsPanel.tsx:**

1. Replace all three `border-t border-outline pt-4` section separators with `mt-6` (spacing only, no border per D-12). Specifically:
   - Line ~262: `<div className="border-t border-outline pt-4">` becomes `<div className="mt-6">`
   - Line ~266: Same pattern, becomes `<div className="mt-6">`
   - Line ~271: `<div className="border-t border-outline pt-4 space-y-2">` becomes `<div className="mt-6 space-y-2">`

2. Standardize all form inputs in SettingsPanel. Find every `border border-outline bg-elevated` on input/select elements and replace with the standard contract:
   - `border border-outline` -> `border border-outline-subtle`
   - Add `focus:ring-1 focus:ring-primary/30` after `focus:border-primary`
   - Add `placeholder-on-bg-muted` if element has a placeholder
   - Ensure `focus:outline-none` is present

   The target class for text inputs:
   `w-full rounded-lg border border-outline-subtle bg-elevated px-3 py-2 text-sm text-on-bg placeholder-on-bg-muted focus:border-primary focus:ring-1 focus:ring-primary/30 focus:outline-none`

   The target class for select elements:
   `w-full rounded-lg border border-outline-subtle bg-elevated px-3 py-2 text-sm text-on-bg focus:border-primary focus:ring-1 focus:ring-primary/30 focus:outline-none`

**SNMPProfileManager.tsx:**

1. Find the shared input class constants (lines ~11-13):
   ```typescript
   const inputClassName = 'w-full rounded-lg border border-outline bg-elevated px-3 py-2 text-sm text-on-bg placeholder-on-bg-secondary/40 focus:border-primary focus:outline-none';
   const selectClassName = 'w-full rounded-lg border border-outline bg-elevated px-3 py-2 text-sm text-on-bg focus:border-primary focus:outline-none';
   ```
   Replace with:
   ```typescript
   const inputClassName = 'w-full rounded-lg border border-outline-subtle bg-elevated px-3 py-2 text-sm text-on-bg placeholder-on-bg-muted focus:border-primary focus:ring-1 focus:ring-primary/30 focus:outline-none';
   const selectClassName = 'w-full rounded-lg border border-outline-subtle bg-elevated px-3 py-2 text-sm text-on-bg focus:border-primary focus:ring-1 focus:ring-primary/30 focus:outline-none';
   ```

2. Replace `border border-outline` on section grouping divs (e.g., `space-y-3 rounded-lg border border-outline p-3`) with `bg-surface-high rounded-lg p-3` -- surface tier instead of border for grouping.

3. Replace `border border-outline` on inline action buttons (edit/delete/cancel) with `bg-surface-high hover:bg-elevated` -- surface tier for buttons.

4. Replace `border border-outline bg-elevated p-3` on profile detail cards with `bg-surface-high p-3` -- no border.

**SSHProfileManager.tsx:**

1. Same changes as SNMPProfileManager: update shared class constants for inputs.

2. Replace `border border-outline` on section grouping divs and action buttons with surface tier equivalents, same pattern as SNMP.

3. Replace profile detail card borders with surface tiers.

**All three files:** Add `transition-colors duration-200` to main container/root elements for smooth theme switching.
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia/frontend && npx vite build 2>&1 | tail -5 && npx vitest run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `SettingsPanel.tsx` does NOT contain `border-t border-outline` as a section separator
    - `SettingsPanel.tsx` form inputs contain `border-outline-subtle` (not `border-outline`)
    - `SettingsPanel.tsx` form inputs contain `focus:ring-1 focus:ring-primary/30`
    - `SNMPProfileManager.tsx` inputClassName contains `border-outline-subtle` and `focus:ring-1 focus:ring-primary/30` and `placeholder-on-bg-muted`
    - `SNMPProfileManager.tsx` does NOT contain `border border-outline p-3` on section grouping divs (replaced with `bg-surface-high`)
    - `SSHProfileManager.tsx` inputClassName contains `border-outline-subtle` and `focus:ring-1 focus:ring-primary/30`
    - All three files contain `transition-colors duration-200`
    - `npx vite build` exits 0, all tests pass
  </acceptance_criteria>
  <done>SettingsPanel, SNMPProfileManager, SSHProfileManager restyled with standard form contract, no-line sections, surface tiers</done>
</task>

<task type="auto">
  <name>Task 2: Restyle AddDevicePanel, DeviceConfigPanel, LinkCreatePanel form inputs</name>
  <files>frontend/src/components/AddDevicePanel.tsx, frontend/src/components/DeviceConfigPanel.tsx, frontend/src/components/LinkCreatePanel.tsx</files>
  <read_first>
    - frontend/src/components/AddDevicePanel.tsx
    - frontend/src/components/DeviceConfigPanel.tsx
    - frontend/src/components/LinkCreatePanel.tsx
  </read_first>
  <action>
**AddDevicePanel.tsx:**

1. Read fully. Find the shared input class constants (lines ~127-129):
   ```typescript
   const inputClassName = 'w-full rounded-lg border border-outline bg-elevated px-3 py-2 text-sm text-on-bg placeholder-on-bg-secondary/40 focus:border-primary focus:outline-none';
   const selectClassName = 'w-full rounded-lg border border-outline bg-elevated px-3 py-2 text-sm text-on-bg focus:border-primary focus:outline-none';
   ```
   Replace with standard contract:
   ```typescript
   const inputClassName = 'w-full rounded-lg border border-outline-subtle bg-elevated px-3 py-2 text-sm text-on-bg placeholder-on-bg-muted focus:border-primary focus:ring-1 focus:ring-primary/30 focus:outline-none';
   const selectClassName = 'w-full rounded-lg border border-outline-subtle bg-elevated px-3 py-2 text-sm text-on-bg focus:border-primary focus:ring-1 focus:ring-primary/30 focus:outline-none';
   ```

2. Replace `border border-outline p-3` on section grouping divs (e.g., SNMP credential section, SSH credential section around lines 185, 215) with `bg-surface-high rounded-lg p-3`.

**DeviceConfigPanel.tsx:**

1. Read fully. This is a large file with many form inputs. Apply the standard contract to ALL input/select/textarea elements. The pattern to find-and-replace:
   - `border border-outline bg-elevated` -> `border border-outline-subtle bg-elevated`
   - Add `focus:ring-1 focus:ring-primary/30` after every `focus:border-primary`
   - `placeholder-on-bg-secondary/40` -> `placeholder-on-bg-muted`

2. Find `border-t border-outline pt-4` section separator (line ~532). Replace with `mt-6` (spacing, no border).

3. Find `border border-outline p-3` on section grouping divs (SSH credential sections, around lines 390, 484). Replace with `bg-surface-high rounded-lg p-3`.

4. Find `border border-outline bg-elevated/40 px-3 py-2` on read-only display divs (line ~297). Replace with `bg-surface-high px-3 py-2 rounded-lg`.

**LinkCreatePanel.tsx:**

1. Read fully. Apply the standard contract to all input/select elements:
   - `border border-outline bg-elevated` -> `border border-outline-subtle bg-elevated`
   - Add `focus:ring-1 focus:ring-primary/30`
   - `placeholder-on-bg-secondary/40` -> `placeholder-on-bg-muted`

2. Find `border-t border-outline` separator (line ~347). Replace with `my-4` (spacing only).

3. Find `border border-outline bg-elevated` on dropdown containers (line ~90). Replace with `border border-outline-subtle bg-elevated`.

4. Find inline action buttons with `border border-outline bg-elevated`. Replace border with `border border-outline-subtle`.

**All three files:** Add `transition-colors duration-200` to form container root elements.
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia/frontend && npx vite build 2>&1 | tail -5 && npx vitest run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `AddDevicePanel.tsx` inputClassName contains `border-outline-subtle`, `focus:ring-1 focus:ring-primary/30`, `placeholder-on-bg-muted`
    - `AddDevicePanel.tsx` does NOT contain `border border-outline p-3` on section grouping divs (uses `bg-surface-high`)
    - `DeviceConfigPanel.tsx` does NOT contain `border-t border-outline` as section separator
    - `DeviceConfigPanel.tsx` all inputs have `border-outline-subtle` (not `border-outline` alone)
    - `DeviceConfigPanel.tsx` all inputs have `focus:ring-1 focus:ring-primary/30`
    - `LinkCreatePanel.tsx` does NOT contain `<div className="border-t border-outline" />`
    - `LinkCreatePanel.tsx` inputs have `border-outline-subtle`
    - `npx vite build` exits 0, all tests pass
  </acceptance_criteria>
  <done>AddDevicePanel, DeviceConfigPanel, LinkCreatePanel form inputs standardized with new contract, no-line section separators, surface tier groupings</done>
</task>

<task type="auto">
  <name>Task 3: Restyle LinkDetailsPanel and InterfaceStatsPanel with JetBrains Mono metrics</name>
  <files>frontend/src/components/LinkDetailsPanel.tsx, frontend/src/components/InterfaceStatsPanel.tsx</files>
  <read_first>
    - frontend/src/components/LinkDetailsPanel.tsx
    - frontend/src/components/InterfaceStatsPanel.tsx
  </read_first>
  <action>
**LinkDetailsPanel.tsx:**

1. Read fully. Apply standard form input contract to all input/select elements (same pattern as Task 2): `border-outline` -> `border-outline-subtle`, add `focus:ring-1 focus:ring-primary/30`.

2. Find metric display values (bandwidth, throughput, utilization numbers). Ensure they use `font-mono` class for JetBrains Mono rendering. Look for inline styles with `fontFamily` or classes without `font-mono` on numeric values. Add `font-mono` where metric values are displayed.

3. Replace `border border-outline bg-elevated p-3` on detail cards (line ~166) with `bg-surface-high rounded-lg p-3`.

4. Replace `border-t border-outline pt-4` section separator (line ~265) with `mt-6`.

5. Replace `border border-outline bg-elevated` on action buttons with `border border-outline-subtle bg-elevated`.

6. Add `transition-colors duration-200` to panel root.

**InterfaceStatsPanel.tsx:**

1. Read fully. This component displays interface metrics (speed, errors, discards, octets).

2. Replace `border border-outline bg-elevated p-4` on the container (line ~31) with `bg-surface-high rounded-xl p-4`.

3. Replace `border-t border-outline pt-3` separator (line ~66) with `mt-3 pt-3` (spacing only).

4. Ensure all numeric metric values (speed in Mbps/Gbps, error counts, octet counts) use `font-mono` class. Find text elements displaying numbers and add `font-mono text-[11px] font-semibold` where appropriate per UI-SPEC Metric typography role.

5. Ensure label text (column headers like "In Errors", "Out Errors") uses `text-[12px] uppercase tracking-[0.16em] text-on-bg-secondary` per UI-SPEC Label typography role.

6. Add `transition-colors duration-200` to the component root.
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia/frontend && npx vite build 2>&1 | tail -5 && npx vitest run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `LinkDetailsPanel.tsx` does NOT contain `border-t border-outline` as section separator
    - `LinkDetailsPanel.tsx` detail cards use `bg-surface-high` (not `border border-outline bg-elevated`)
    - `LinkDetailsPanel.tsx` metric values have `font-mono` class
    - `InterfaceStatsPanel.tsx` does NOT contain `border-t border-outline` or `border border-outline` as layout separators
    - `InterfaceStatsPanel.tsx` container uses `bg-surface-high` (not `border border-outline bg-elevated`)
    - `InterfaceStatsPanel.tsx` numeric metric values have `font-mono` class
    - Both files contain `transition-colors duration-200`
    - `npx vite build` exits 0, all tests pass
  </acceptance_criteria>
  <done>LinkDetailsPanel and InterfaceStatsPanel restyled with JetBrains Mono metrics, surface tier cards, no border separators</done>
</task>

</tasks>

<verification>
- All 8 panel/form components use standard form input contract
- No border-t section separators in SettingsPanel, DeviceConfigPanel, LinkDetailsPanel
- Section grouping divs use bg-surface-high instead of border
- LinkDetailsPanel and InterfaceStatsPanel metric values use font-mono
- All components have transition-colors for theme switching
- All tests pass, Vite build succeeds
</verification>

<success_criteria>
1. Form inputs across all 8 components use `border-outline-subtle`, `focus:ring-1 focus:ring-primary/30`, `placeholder-on-bg-muted`
2. No `border-t border-outline` layout separators in any panel
3. Metric values in LinkDetailsPanel and InterfaceStatsPanel render in JetBrains Mono
4. `cd frontend && npx vitest run` all pass, `npx vite build` clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-restyling/02-05-SUMMARY.md`
</output>
