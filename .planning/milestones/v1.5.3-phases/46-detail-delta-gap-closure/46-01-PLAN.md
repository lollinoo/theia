---
phase: 46-detail-delta-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/worker/snapshot_builder.go
  - internal/worker/snapshot_builder_test.go
  - internal/worker/pipeline_test.go
  - frontend/src/types/metrics.test.ts
  - frontend/src/hooks/useWebSocket.test.ts
autonomous: true
requirements:
  - WS-02
user_setup: []

must_haves:
  truths:
    - "A subscribed device-scoped interface panel receives a targeted `snapshot_delta` that includes `link_metrics[device.id]` immediately after the selected device's performance poll completes."
    - "The targeted detail payload remains limited to the selected device's `device_metrics`, `device_statuses`, and `link_metrics[device.id]`; unsubscribed clients do not receive it."
    - "The frontend's single shared snapshot atom merges the selected device's targeted `link_metrics` update without clearing other devices or introducing a second websocket/cache path."
  artifacts:
    - path: "internal/worker/snapshot_builder.go"
      provides: "Device-scoped targeted detail payload composition with `link_metrics[device.id]` on the existing `snapshot_delta` contract"
      contains: "func buildDeviceDetailDelta"
    - path: "internal/worker/pipeline_test.go"
      provides: "Runtime websocket regressions proving selected-device link-metric delivery and no unsubscribed leakage"
      contains: "TestPipelineOrchestratorRunTask_PerformancePollSendsOnlySelectedDeviceLinkMetricsToSubscribedClient"
    - path: "frontend/src/hooks/useWebSocket.test.ts"
      provides: "Shared snapshot-atom merge regression for targeted `link_metrics` deltas"
      contains: "targeted link_metrics"
  key_links:
    - from: "internal/worker/snapshot_builder.go"
      to: "frontend/src/components/InterfaceStatsPanel.tsx"
      via: "Targeted payload now fills `snapshot.link_metrics[device.id]`, which the device-scoped interface panel reads by `if_name`"
      pattern: "LinkMetricsToDTOs|snapshot\\?\\.link_metrics\\[device\\.id\\]"
    - from: "internal/worker/pipeline.go"
      to: "internal/ws/hub.go"
      via: "Existing `publishSubscribedDetailDelta()` recipient filtering remains the only delivery path"
      pattern: "DetailSubscribers|SendTo"
    - from: "frontend/src/hooks/useWebSocket.ts"
      to: "frontend/src/types/metrics.ts"
      via: "The existing `snapshot_delta` merge path updates the shared snapshot atom in place"
      pattern: "mergeSnapshotDelta|snapshot_delta"
---

<objective>
Close `INT-03` by adding selected-device `link_metrics` to the existing targeted
detail delta, then lock the runtime and frontend merge behavior so subscribed
device interface panels refresh immediately after the device's performance poll
without widening overview broadcasts or leaking targeted payloads.

Purpose:
- Satisfy Phase 46 and `WS-02` using the existing `snapshot` / `snapshot_delta`
  contract and the existing subscription ownership rules from Phases 43-45.
- Preserve the locked Phase 46 UI contract: device-scoped interface stats stay
  subscribed by device ID, link-scoped interface panels remain unsubscribed and
  overview-driven, and no second websocket family or panel-local cache is
  introduced.
- Keep the fix surgical by changing only targeted payload composition and
  regression coverage around the already-existing backend send path and
  frontend merge path.

Output:
- `buildDeviceDetailDelta()` emits selected-device `link_metrics[device.id]`
  alongside the existing `device_metrics` and `device_statuses`.
- Worker runtime tests prove subscribed delivery, same-device-only payload
  scope, and no unsubscribed leakage.
- Frontend merge tests prove targeted link-metric deltas update the shared
  snapshot atom without clearing unrelated device data.
</objective>

<execution_context>
@/home/azmin/.codex/get-shit-done/workflows/execute-plan.md
@/home/azmin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.5.3-MILESTONE-AUDIT.md
@.planning/phases/43-websocket-detail-on-demand/43-02-PLAN.md
@.planning/phases/43-websocket-detail-on-demand/43-02-SUMMARY.md
@.planning/phases/45-polling-cadence-gap-closure/45-02-SUMMARY.md
@.planning/phases/46-detail-delta-gap-closure/46-UI-SPEC.md
@internal/ws/messages.go
@internal/worker/snapshot_builder.go
@internal/worker/snapshot_builder_test.go
@internal/worker/pipeline.go
@internal/worker/pipeline_test.go
@frontend/src/components/InterfaceStatsPanel.tsx
@frontend/src/hooks/useWebSocket.ts
@frontend/src/hooks/useWebSocket.test.ts
@frontend/src/types/metrics.ts
@frontend/src/types/metrics.test.ts
@frontend/src/components/canvas/detailSubscription.ts

<interfaces>
Existing targeted payload contract:
```go
type SnapshotPayload struct {
    DeviceMetrics   map[string]DeviceMetricsDTO `json:"device_metrics"`
    LinkMetrics     map[string][]LinkMetricsDTO `json:"link_metrics"`
    Alerts          []AlertDTO                  `json:"alerts"`
    DeviceStatuses  map[string]string           `json:"device_statuses"`
    DeviceHostnames map[string]string           `json:"device_hostnames"`
    DeviceModels    map[string]string           `json:"device_models"`
}

func buildDeviceDetailDelta(device domain.Device, deviceState state.DeviceState) *ws.SnapshotPayload
```

Frontend merge seam that must remain unchanged:
```ts
export function mergeSnapshotDelta(
  existing: SnapshotPayload,
  delta: SnapshotPayload,
): SnapshotPayload {
  return {
    device_metrics: { ...existing.device_metrics, ...delta.device_metrics },
    link_metrics: { ...existing.link_metrics, ...delta.link_metrics },
    device_statuses: { ...existing.device_statuses, ...delta.device_statuses },
    device_hostnames: { ...existing.device_hostnames, ...delta.device_hostnames },
    device_models: { ...existing.device_models, ...delta.device_models },
    alerts: delta.alerts.length > 0 ? delta.alerts : existing.alerts,
  };
}
```

Locked Phase 46 UI contract:
- Device-scoped `interfaceStats` stays subscribed by device ID.
- Link-scoped `interfaceStats` stays unsubscribed and overview-driven.
- Targeted detail may update only the selected device's `device_metrics`,
  `device_statuses`, and `link_metrics[device.id]`.
- No new websocket message family, no REST polling loop, no panel-local cache,
  and no new UI chrome.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add selected-device `link_metrics` to the targeted detail builder without widening the payload</name>
  <files>
    internal/worker/snapshot_builder.go
    internal/worker/snapshot_builder_test.go
  </files>
  <read_first>
    - .planning/v1.5.3-MILESTONE-AUDIT.md (INT-03 evidence and Phase 46 success criteria)
    - .planning/phases/43-websocket-detail-on-demand/43-02-PLAN.md (existing targeted detail seam and its original omission)
    - .planning/phases/46-detail-delta-gap-closure/46-UI-SPEC.md (locked device-scoped detail contract)
    - internal/worker/snapshot_builder.go (current `buildDeviceDetailDelta` implementation and DTO helpers)
    - internal/worker/snapshot_builder_test.go (existing detail-builder regressions that currently enforce link-metric omission)
    - internal/ws/messages.go (`SnapshotPayload`, `LinkMetricsDTO`, and `DeviceMetricsDTO`)
    - frontend/src/components/InterfaceStatsPanel.tsx (device-scoped panel reads `snapshot.link_metrics[device.id]` by `if_name`)
    - frontend/src/types/metrics.ts (frontend JSON contract for `link_metrics`)
  </read_first>
  <behavior>
    - targeted detail stays on the existing `snapshot_delta` payload family; no new message type or payload branch is added
    - `buildDeviceDetailDelta()` emits only one `link_metrics` map key: the selected device ID
    - the targeted payload continues to leave `alerts`, `device_hostnames`, and `device_models` empty
    - the change is scoped to targeted detail payload composition only; overview `computeSnapshotHashes()` and `buildDelta()` stay unchanged
  </behavior>
  <action>
    Update `internal/worker/snapshot_builder.go` and its tests exactly this way:

    1. Keep the existing `buildDeviceDetailDelta(device, deviceState)` signature
       and the existing `device_metrics` / `device_statuses` population.

    2. Before returning from `buildDeviceDetailDelta()`, populate only the
       selected device's `link_metrics` entry from `deviceState.LinkMetrics`:
       - create a copied slice so the builder does not mutate store-owned
         `deviceState.LinkMetrics`
       - for each copied `domain.LinkMetrics`, set `DeviceID = device.ID`
       - convert the copied slice with:
         ```go
         ws.LinkMetricsToDTOs(map[string][]domain.LinkMetrics{
             deviceID: copiedLinkMetrics,
         })
         ```
       - assign only `delta.LinkMetrics[deviceID]`
       - leave `delta.LinkMetrics` empty when `deviceState.LinkMetrics` is empty

    3. Do not populate any other top-level sections:
       - `delta.Alerts` must stay empty
       - `delta.DeviceHostnames` must stay empty
       - `delta.DeviceModels` must stay empty
       - no `link_metrics` key other than the selected `deviceID` may be added

    4. Do not modify `computeSnapshotHashes()` or `buildDelta()`. Phase 46 is a
       targeted-send closure, not an overview hash-contract change.

    5. Replace the current link-metric omission test with selected-device
       inclusion coverage:
       - rename/replace `TestBuildDeviceDetailDelta_OmitsLinkMetricsAlertsAndModels`
         with `TestBuildDeviceDetailDelta_IncludesSelectedDeviceLinkMetricsOnly`
       - assert `delta.LinkMetrics` has exactly one key equal to the selected
         device ID
       - assert the emitted DTO keeps:
         - `device_id == device.ID.String()`
         - `if_name == "ether1"` for the fixture
         - non-nil `tx_bps`, `rx_bps`, and `utilization`
         - the original RFC3339 `collected_at`
       - keep a second assertion in that test that `Alerts`,
         `DeviceHostnames`, and `DeviceModels` remain empty

    6. Keep `TestBuildDeviceDetailDelta_EmbedsOptionalDetailFieldsInDeviceMetrics`
       and update it only if the new link-metric assertions require fixture
       expansion. The existing device-detail fields from Phase 43 must still be
       present.
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia && rtk go test ./internal/worker -run 'TestBuildDeviceDetailDelta_(EmbedsOptionalDetailFieldsInDeviceMetrics|IncludesSelectedDeviceLinkMetricsOnly)' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n 'func buildDeviceDetailDelta' internal/worker/snapshot_builder.go` returns 1 match
    - `rtk rg -n 'LinkMetricsToDTOs|delta\\.LinkMetrics' internal/worker/snapshot_builder.go` returns matches inside `buildDeviceDetailDelta`
    - `rtk rg -n 'TestBuildDeviceDetailDelta_IncludesSelectedDeviceLinkMetricsOnly' internal/worker/snapshot_builder_test.go` returns 1 match
    - `rtk rg -n 'DeviceHostnames|DeviceModels|Alerts' internal/worker/snapshot_builder_test.go` returns matches inside the selected-device inclusion regression
    - `cd /home/azmin/projects/theia && rtk go test ./internal/worker -run 'TestBuildDeviceDetailDelta_(EmbedsOptionalDetailFieldsInDeviceMetrics|IncludesSelectedDeviceLinkMetricsOnly)' -count=1 -v` exits 0
  </acceptance_criteria>
  <done>
    `buildDeviceDetailDelta()` now emits selected-device `link_metrics` on the
    existing targeted detail contract while leaving unrelated payload sections
    untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the worker regressions that currently lock link-metric omission and prove subscriber-only delivery</name>
  <files>
    internal/worker/pipeline_test.go
  </files>
  <read_first>
    - .planning/v1.5.3-MILESTONE-AUDIT.md (INT-03 and `WS-02` closure criteria)
    - .planning/phases/43-websocket-detail-on-demand/43-02-SUMMARY.md (existing runtime seam and current omission)
    - .planning/phases/45-polling-cadence-gap-closure/45-02-SUMMARY.md (performance-owned freshness metadata that must stay intact)
    - .planning/phases/46-detail-delta-gap-closure/46-UI-SPEC.md (selected-device-only targeted update rules)
    - internal/worker/pipeline.go (`runTask()` already calls `publishSubscribedDetailDelta()` after each poll class)
    - internal/worker/pipeline_test.go (existing websocket subscription/runtime patterns)
    - internal/worker/snapshot_builder.go (Task 1 payload shape)
    - frontend/src/components/canvas/detailSubscription.ts (device-scoped vs link-scoped subscription ownership)
  </read_first>
  <behavior>
    - the existing runtime seam stays intact: `pipeline.go` continues using `publishSubscribedDetailDelta()` after each poll class
    - subscribed clients receive selected-device `link_metrics` after the selected device's performance poll
    - unsubscribed clients still receive nothing
    - mixed-tier freshness semantics from Phase 45 stay intact while targeted detail may continue to carry the selected device's current `link_metrics`
  </behavior>
  <action>
    Leave `internal/worker/pipeline.go` unchanged unless Task 1 exposes a real
    recipient-path bug. The implementation seam already exists. Update
    `internal/worker/pipeline_test.go` exactly as follows:

    1. Replace `TestPipelineOrchestratorRunTask_DetailDeltaOmitsLinkMetrics`
       with:
       `TestPipelineOrchestratorRunTask_PerformancePollSendsOnlySelectedDeviceLinkMetricsToSubscribedClient`

    2. In that replacement test:
       - keep the existing real websocket server + subscriber setup
       - run the selected device's performance task
       - read the targeted `snapshot_delta`
       - assert:
         - `len(message.Payload.LinkMetrics) == 1`
         - `message.Payload.LinkMetrics[device.ID.String()]` exists
         - the selected-device slice length is `1` for the fixture
         - the emitted DTO has:
           - `device_id == device.ID.String()`
           - `if_name == "ether1"`
           - non-nil `tx_bps`
           - non-nil `rx_bps`
         - `DeviceMetrics` still contains the selected device
         - no overview broadcast messages were emitted during the targeted send

    3. Strengthen `TestPipelineOrchestratorRunTask_DetailDeltaDoesNotReachUnsubscribedClient`
       so the subscribed client-side assertion also checks
       `message.Payload.LinkMetrics[device.ID.String()]` exists before the
       unsubscribed client timeout assertion.

    4. Extend
       `TestPipelineOrchestratorRunTask_DetailDeltaKeepsPerformanceFreshnessMetadataAfterOperationalPoll`
       so the operationally-triggered targeted delta still:
       - preserves `LastPolledAt` from the earlier performance poll
       - preserves `ExpectedPollIntervalSeconds == 30`
       - carries `LinkMetrics[device.ID.String()]` with the selected device's
         existing interface throughput slice, proving the panel does not lose
         current throughput data on later same-device detail sends

    5. Keep the existing no-leak/no-new-family guarantees:
       - message type remains `snapshot_delta`
       - no extra websocket family is introduced
       - no test may assert or require a broadcast-widened payload
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia && rtk go test ./internal/worker -run 'TestPipelineOrchestratorRunTask_(PerformancePollSendsOnlySelectedDeviceLinkMetricsToSubscribedClient|DetailDeltaKeepsPerformanceFreshnessMetadataAfterOperationalPoll|DetailDeltaDoesNotReachUnsubscribedClient)' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n 'TestPipelineOrchestratorRunTask_PerformancePollSendsOnlySelectedDeviceLinkMetricsToSubscribedClient' internal/worker/pipeline_test.go` returns 1 match
    - `rtk rg -n 'LinkMetrics\\[device\\.ID\\.String\\(\\)\\]|len\\(message\\.Payload\\.LinkMetrics\\) == 1' internal/worker/pipeline_test.go` returns matches in the targeted-detail regressions
    - `rtk rg -n 'snapshot_delta' internal/worker/pipeline_test.go` returns matches in the selected-device link-metric tests
    - `cd /home/azmin/projects/theia && rtk go test ./internal/worker -run 'TestPipelineOrchestratorRunTask_(PerformancePollSendsOnlySelectedDeviceLinkMetricsToSubscribedClient|DetailDeltaKeepsPerformanceFreshnessMetadataAfterOperationalPoll|DetailDeltaDoesNotReachUnsubscribedClient)' -count=1 -v` exits 0
  </acceptance_criteria>
  <done>
    Worker/runtime coverage now proves the targeted detail path sends
    selected-device `link_metrics` only to subscribed clients while preserving
    Phase 45 freshness ownership semantics.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock the existing frontend shared-snapshot merge behavior for targeted `link_metrics` deltas</name>
  <files>
    frontend/src/types/metrics.test.ts
    frontend/src/hooks/useWebSocket.test.ts
  </files>
  <read_first>
    - .planning/phases/46-detail-delta-gap-closure/46-UI-SPEC.md (shared snapshot atom is locked; no panel-local cache)
    - frontend/src/types/metrics.ts (`mergeSnapshotDelta()` is the single merge seam)
    - frontend/src/types/metrics.test.ts (existing sparse-delta merge coverage)
    - frontend/src/hooks/useWebSocket.ts (existing `snapshot_delta` handler)
    - frontend/src/hooks/useWebSocket.test.ts (current websocket merge patterns)
    - frontend/src/components/InterfaceStatsPanel.tsx (device-scoped panel reads `snapshot.link_metrics[device.id]`)
    - frontend/src/components/canvas/detailSubscription.ts (device-scoped interface panel is the only `interfaceStats` subscriber)
  </read_first>
  <behavior>
    - the frontend continues using one websocket and one shared snapshot atom
    - targeted `link_metrics` deltas update only the selected device key while preserving other devices' entries
    - no production frontend code changes are introduced unless these tests expose a real merge bug
  </behavior>
  <action>
    Leave `frontend/src/types/metrics.ts` and `frontend/src/hooks/useWebSocket.ts`
    unchanged unless the new regressions expose an actual merge bug. Add test
    coverage only:

    1. In `frontend/src/types/metrics.test.ts`, add:
       `it('merges targeted link_metrics for one device without clearing other devices', ...)`
       with this exact setup:
       - `existing.link_metrics` contains:
         - `dev-1` with one interface row for `ether1`
         - `dev-2` with one interface row for `ether2`
       - `delta.link_metrics` contains only `dev-1` with updated `tx_bps`,
         `rx_bps`, `utilization`, and `collected_at`
       - assert the merged snapshot updates only `dev-1` and preserves the
         existing `dev-2` slice untouched

    2. In `frontend/src/hooks/useWebSocket.test.ts`, add:
       `it('handles snapshot_delta message with targeted link_metrics by merging into existing snapshot', ...)`
       with this exact message sequence:
       - open the mocked websocket
       - send a full `snapshot` containing `link_metrics.dev-1` and
         `link_metrics.dev-2`
       - send a sparse `snapshot_delta` whose only changed section is
         `link_metrics.dev-1`
       - assert `result.current.snapshot!.link_metrics['dev-1']` reflects the
         updated throughput
       - assert `result.current.snapshot!.link_metrics['dev-2']` is still present
         and unchanged

    3. Do not add a second snapshot store, panel-local cache, or REST polling
       helper in frontend tests or implementation. The purpose of this task is
       to prove the existing shared merge seam is already sufficient for Phase 46.
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia/frontend && rtk npm test -- src/types/metrics.test.ts src/hooks/useWebSocket.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "merges targeted link_metrics for one device without clearing other devices" frontend/src/types/metrics.test.ts` returns 1 match
    - `rtk rg -n "handles snapshot_delta message with targeted link_metrics by merging into existing snapshot" frontend/src/hooks/useWebSocket.test.ts` returns 1 match
    - `rtk rg -n "link_metrics\\['dev-1'\\]|link_metrics\\['dev-2'\\]" frontend/src/hooks/useWebSocket.test.ts frontend/src/types/metrics.test.ts` returns matches in both new regressions
    - `cd /home/azmin/projects/theia/frontend && rtk npm test -- src/types/metrics.test.ts src/hooks/useWebSocket.test.ts` exits 0
  </acceptance_criteria>
  <done>
    Frontend regression coverage proves the existing shared snapshot merge path
    is ready for targeted selected-device `link_metrics` updates without any new
    transport or state-management surface.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker runtime -> websocket client | Device-scoped targeted payload crosses the per-client subscription boundary and must not leak to unsubscribed clients |
| websocket message -> shared frontend snapshot atom | Sparse `snapshot_delta` payload mutates client-visible state for multiple panels off one shared cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | I | `publishSubscribedDetailDelta` / `buildDeviceDetailDelta` | mitigate | Keep the existing `DetailSubscribers(device.ID)` recipient filter, emit only `link_metrics[device.id]`, and add runtime tests proving the unsubscribed client times out while the subscribed client receives only the selected device key |
| T-46-02 | T/I | `mergeSnapshotDelta` shared snapshot atom | mitigate | Add frontend regressions proving a targeted `link_metrics.dev-1` delta updates only `dev-1` and preserves `dev-2`, preventing sparse-delta corruption or accidental state clearing |
| T-46-03 | D | targeted detail send path | accept | Payload growth is limited to one selected device's `link_metrics` slice on the existing `snapshot_delta` path; no new polling loop, broadcast fanout, or message family is introduced in this phase |
</threat_model>

<verification>
- `cd /home/azmin/projects/theia && rtk go test ./internal/worker -count=1`
- `cd /home/azmin/projects/theia/frontend && rtk npm test -- src/types/metrics.test.ts src/hooks/useWebSocket.test.ts`
- `rtk rg -n 'LinkMetricsToDTOs|PerformancePollSendsOnlySelectedDeviceLinkMetricsToSubscribedClient|targeted link_metrics' internal/worker/snapshot_builder.go internal/worker/pipeline_test.go frontend/src/types/metrics.test.ts frontend/src/hooks/useWebSocket.test.ts`
</verification>

<success_criteria>
- `buildDeviceDetailDelta()` emits `link_metrics[device.id]` for the selected device on the targeted detail path without populating alerts, hostnames, or models.
- Backend runtime tests prove subscribed clients receive selected-device link metrics immediately after the device's performance poll and unsubscribed clients still receive nothing.
- Frontend merge tests prove the shared snapshot atom updates the selected device's `link_metrics` entry without clearing other devices or requiring a new websocket/cache path.
</success_criteria>

<output>
After completion, create `.planning/phases/46-detail-delta-gap-closure/46-01-SUMMARY.md`
</output>
