---
phase: 42-pipeline-orchestrator-cutover
plan: 04
type: execute
wave: 3
depends_on: ["42-03"]
files_modified:
  - cmd/theia/main.go
  - internal/api/router.go
  - internal/api/health_handler.go
  - internal/api/health_handler_test.go
  - cmd/theia/main_test.go
autonomous: true
requirements:
  - PIPE-03
user_setup: []

must_haves:
  truths:
    - "`cmd/theia/main.go` wires only PipelineOrchestrator for live polling and metrics delivery; it does not start `worker.Poller` or `worker.MetricsCollector`"
    - "WebSocket bootstrap now reads `GetSnapshot` and `IsPromAvailable` from PipelineOrchestrator"
    - "Health/status plumbing no longer depends on the concrete `*worker.Poller` type; it accepts any provider with `Status() string`, allowing PipelineOrchestrator to report `running` / `stopped`"
    - "The topology notify channel remains shared between DeviceService and PipelineOrchestrator so legacy reprobes and scheduled static polls both preserve snapshot-then-topology ordering"
  artifacts:
    - path: "cmd/theia/main.go"
      provides: "Atomic runtime cutover from Poller + MetricsCollector to PipelineOrchestrator"
      contains: "worker.NewPipelineOrchestrator"
    - path: "internal/api/health_handler.go"
      provides: "Interface-based status provider compatible with PipelineOrchestrator"
      contains: "Status() string"
    - path: "internal/api/health_handler_test.go"
      provides: "Regression coverage for health output using the new status-provider abstraction"
  key_links:
    - from: "cmd/theia/main.go"
      to: "internal/worker/pipeline.go"
      via: "PipelineOrchestrator becomes the sole polling runtime"
      pattern: "NewPipelineOrchestrator|GetSnapshot|IsPromAvailable"
    - from: "internal/api/health_handler.go"
      to: "internal/api/router.go"
      via: "Router and health endpoint accept a generic status provider instead of a concrete Poller"
      pattern: "Status() string"
---

<objective>
Perform the atomic cutover in startup wiring and API status plumbing so the new
PipelineOrchestrator is the only live polling runtime.

Purpose:
- Finish the Phase 42 replacement by removing dual-runtime startup.
- Keep health and WebSocket bootstrap behavior intact.
- Prove the cutover in the app entrypoint, not only in isolated worker tests.

Output:
- `cmd/theia/main.go` starts and stops only the PipelineOrchestrator.
- health/router status plumbing accepts the new runtime's `Status()` method.
- tests and grep-verifiable checks prove the old workers are no longer wired.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-pipeline-orchestrator-cutover/42-CONTEXT.md
@cmd/theia/main.go
@cmd/theia/main_test.go
@internal/api/router.go
@internal/api/health_handler.go
@internal/api/health_handler_test.go
@internal/worker/pipeline.go
@internal/worker/poller.go
@internal/worker/metrics_collector.go

<interfaces>
The cutover must leave these runtime integration points:
```go
wsHandler := ws.NewHandler(hub, pipeline.GetSnapshot, pipeline.IsPromAvailable)
router := api.NewRouter(..., pipeline, ..., wsHandler)
```

Health plumbing must accept a generic status provider:
```go
type statusProvider interface {
    Status() string
}
```

The final `main.go` must not call:
- `worker.NewPoller(...)`
- `poller.Start(...)`
- `worker.NewMetricsCollector(...)`
- `collector.Start(...)`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace startup wiring with PipelineOrchestrator and shared collector dependencies</name>
  <files>
    cmd/theia/main.go
    cmd/theia/main_test.go
  </files>
  <read_first>
    - cmd/theia/main.go (current Poller and MetricsCollector wiring)
    - internal/worker/pipeline.go (new runtime constructor and methods)
    - .planning/phases/42-pipeline-orchestrator-cutover/42-CONTEXT.md (atomic replacement rules)
  </read_first>
  <behavior>
    - `main.go` creates the scheduler, state store, volatility collectors, Prometheus collector, and PipelineOrchestrator
    - `main.go` starts and stops only the PipelineOrchestrator for live polling
    - the same buffered `topologyNotify` channel is shared by `DeviceService` and PipelineOrchestrator
    - WebSocket bootstrap uses PipelineOrchestrator getters
    - old worker files may remain in the tree, but they are not constructed or started
  </behavior>
  <action>
    Update `cmd/theia/main.go` exactly as follows:

    1. Add the imports required for:
       - `internal/collector`
       - `internal/scheduler`
       - `internal/state`
    2. Add one shared helper in `main.go`:
       ```go
       func newCollectorSNMPClientFunc(settingsRepo domain.SettingsRepository) collector.NewSNMPClientFunc
       ```
       The helper must:
       - read `domain.SettingSNMPTimeout` with fallback `5 * time.Second`
       - read `domain.SettingSNMPRetries` with fallback `1`
       - return `snmp.NewClient(target, creds, timeout, retries)` without connecting; collectors own `Connect()` / `Close()`
    2. Replace the old live-runtime block with:
       - `stateStore := state.NewStore()`
       - `sched := scheduler.NewScheduler(deviceLinkCache, settingsRepo)`
       - `snmpClientFactory := newCollectorSNMPClientFunc(settingsRepo)`
       - `performanceCollector := collector.NewPerformanceCollector(vendorRegistry, snmpClientFactory)`
       - `operationalCollector := collector.NewOperationalCollector(vendorRegistry, snmpClientFactory)`
       - `staticCollector := collector.NewStaticCollector(vendorRegistry, snmpClientFactory)`
       - `promCollector := collector.NewPrometheusCollector(promClient)`
       - `pipeline := worker.NewPipelineOrchestrator(sched, stateStore, deviceLinkCache, hub, performanceCollector, operationalCollector, staticCollector, promCollector, deviceService, settingsRepo, topologyNotify)`
       - `pipeline.Start(ctx)`
    3. Replace:
       - `ws.NewHandler(hub, collector.GetSnapshot, collector.IsPromAvailable)`
       with
       - `ws.NewHandler(hub, pipeline.GetSnapshot, pipeline.IsPromAvailable)`
    4. Replace the router call so the status-provider argument is `pipeline`, not `poller`.
    4. In shutdown, call `pipeline.Stop()` and remove the old `poller.Stop()` / `collector.Stop()` calls.

    Add a small entrypoint regression test to `cmd/theia/main_test.go`:
    - `TestMainSNMPRuntimeHelpersRemainConstructibleAfterPipelineCutover`

    The test must at least exercise any new SNMP client factory helper added to
    `main.go` so the new collector wiring stays build-covered, including
    fallback timeout / retry parsing.
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia && go test ./cmd/theia -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'worker.NewPipelineOrchestrator' cmd/theia/main.go`
    - `grep -q 'ws.NewHandler(hub, pipeline.GetSnapshot, pipeline.IsPromAvailable)' cmd/theia/main.go`
    - `! rg -n 'NewPoller\\(|poller\\.Start\\(|poller\\.Stop\\(' cmd/theia/main.go`
    - `! rg -n 'NewMetricsCollector\\(|collector\\.Start\\(|collector\\.Stop\\(' cmd/theia/main.go`
    - `grep -c 'TestMainSNMPRuntimeHelpersRemainConstructibleAfterPipelineCutover' cmd/theia/main_test.go` returns 1
    - `cd /home/azmin/projects/theia && go test ./cmd/theia -count=1` exits 0
  </acceptance_criteria>
  <done>
    The app entrypoint now boots only the new pipeline runtime.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generalize health/router status plumbing from concrete Poller to status provider</name>
  <files>
    internal/api/router.go
    internal/api/health_handler.go
    internal/api/health_handler_test.go
  </files>
  <read_first>
    - internal/api/router.go (current `*worker.Poller` dependency)
    - internal/api/health_handler.go (current poller-specific field)
    - internal/api/health_handler_test.go (existing expectations around `snmp_poller`)
    - internal/worker/pipeline.go (`Status() string` runtime contract)
  </read_first>
  <behavior>
    - `HealthHandler` depends on a small interface with `Status() string`, not on `*worker.Poller`
    - `api.NewRouter` accepts the same small interface and forwards it to `NewHealthHandler`
    - the health JSON key remains `snmp_poller`
    - tests still verify `running` / `stopped` behavior, now through the abstract status provider
  </behavior>
  <action>
    Update `internal/api/health_handler.go` to:

    ```go
    type statusProvider interface {
        Status() string
    }

    type HealthHandler struct {
        db      *sql.DB
        poller  statusProvider
    }

    func NewHealthHandler(db *sql.DB, poller statusProvider) *HealthHandler
    ```

    Update `internal/api/router.go` so `NewRouter(...)` accepts the same
    `statusProvider`-compatible argument instead of `*worker.Poller`.

    Update `internal/api/health_handler_test.go` by introducing a tiny fake:
    ```go
    type fakeStatusProvider struct { status string }
    func (f fakeStatusProvider) Status() string { return f.status }
    ```

    Add or update tests so they still cover:
    - running status
    - stopped status
    - nil provider fallback to `stopped`
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia && go test ./internal/api -run 'TestHealthHandler' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'type statusProvider interface' internal/api/health_handler.go`
    - `grep -q 'Status() string' internal/api/health_handler.go`
    - `! rg -n '\\*worker\\.Poller' internal/api/router.go internal/api/health_handler.go`
    - `grep -q 'fakeStatusProvider' internal/api/health_handler_test.go`
    - `cd /home/azmin/projects/theia && go test ./internal/api -count=1` exits 0
  </acceptance_criteria>
  <done>
    Health and router wiring now accept the new runtime without concrete Poller coupling.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App entrypoint -> runtime | `main.go` chooses which live polling runtime is actually active |
| Health handler -> status provider | API health reports runtime state through a narrow interface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-13 | Denial of Service | dual-runtime startup | mitigate | Remove all `Poller` and `MetricsCollector` startup calls so duplicate polling loops cannot run in parallel after the cutover. Severity: HIGH. |
| T-42-14 | Tampering | health/router concrete coupling | mitigate | Status plumbing is narrowed to `Status() string`, preventing stale concrete dependencies from forcing the old Poller back into startup. Severity: HIGH. |
| T-42-15 | Repudiation | runtime status reporting | mitigate | Health tests verify `snmp_poller` still reports `running` / `stopped` correctly through the new provider abstraction. Severity: MEDIUM. |
| T-42-16 | Information Disclosure | health JSON surface | accept | The health endpoint surface is unchanged; only its backing provider changes. Severity: LOW. |

Security gate status: ASVS L1, block_on=high. No HIGH-severity issue remains once `main.go` no longer starts the legacy workers and health tests pass.
</threat_model>

<verification>
After both tasks complete, the following must hold:

1. `cd /home/azmin/projects/theia && go test ./cmd/theia ./internal/api -count=1` exits 0.
2. `grep -q 'worker.NewPipelineOrchestrator' cmd/theia/main.go`
3. `! rg -n 'NewPoller\\(|NewMetricsCollector\\(' cmd/theia/main.go`
4. `grep -q 'type statusProvider interface' internal/api/health_handler.go`
5. `grep -q 'ws.NewHandler(hub, pipeline.GetSnapshot, pipeline.IsPromAvailable)' cmd/theia/main.go`
</verification>

<success_criteria>
- `main.go` wires the new PipelineOrchestrator as the sole live polling and
  metrics-delivery runtime.
- WebSocket bootstrap and health reporting continue to work without retaining
  concrete Poller coupling.
- The codebase no longer has a dual-pipeline startup path for live telemetry.
</success_criteria>

<output>
After completion, create `.planning/phases/42-pipeline-orchestrator-cutover/42-04-SUMMARY.md`
following the standard summary template, including:
- exact startup wiring changes in `main.go`
- how health/router status plumbing was generalized
- proof that old Poller and MetricsCollector startup calls were removed
- any deviations from this plan
</output>
