---
phase: 30-gap-closure-verification-docs-dead-code
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - frontend/src/api/client.ts
autonomous: true
requirements:
  - WINBOX-03
  - WINBOX-04
  - CRED-03
  - CRED-05
  - BRIDGE-01
  - BRIDGE-02
gap_closure: true

must_haves:
  truths:
    - "REQUIREMENTS.md checkboxes for CRED-03, CRED-05, BRIDGE-01, BRIDGE-02 are marked [x]"
    - "REQUIREMENTS.md traceability table shows WINBOX-03, WINBOX-04, CRED-03, CRED-05, BRIDGE-01, BRIDGE-02 as Complete with correct phase attribution"
    - "testSSHProfile function does not exist in frontend/src/api/client.ts"
    - "Frontend TypeScript compiles cleanly after dead code removal"
    - "All frontend tests pass after dead code removal"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected checkboxes and traceability table"
      contains: "[x] **CRED-03**"
    - path: "frontend/src/api/client.ts"
      provides: "Clean API client without dead testSSHProfile function"
  key_links: []
---

<objective>
Close v1.5.0 audit blockers: mark stale REQUIREMENTS.md checkboxes as complete (CRED-03, CRED-05, BRIDGE-01, BRIDGE-02 were implemented in Phase 24 but never checked off), fix traceability table entries for WINBOX-03/WINBOX-04, and remove the dead `testSSHProfile` function from the frontend API client.

Purpose: Bring project documentation into accurate state and remove dead code before milestone closure.
Output: Updated REQUIREMENTS.md with correct checkbox and traceability states; clean client.ts with no orphaned SSH profile functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@frontend/src/api/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix REQUIREMENTS.md checkboxes and traceability table</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state of all checkboxes and traceability table)
    - .planning/phases/24-backend-api-profiles-assignments-winbox-credentials/24-VERIFICATION.md (evidence that CRED-03, CRED-05, BRIDGE-01, BRIDGE-02 are satisfied)
    - .planning/phases/25-frontend-credential-profile-manager-winbox-actions/25-VERIFICATION.md (evidence that WINBOX-03 is satisfied)
    - .planning/phases/27-schema-cleanup-drop-legacy-fk/27-VERIFICATION.md (evidence that WINBOX-04 is satisfied)
  </read_first>
  <action>
Edit `.planning/REQUIREMENTS.md` to make 10 specific changes:

**Checkbox fixes (4 changes):** Change `[ ]` to `[x]` for these 4 lines:
- Line 14: `- [ ] **CRED-03**` becomes `- [x] **CRED-03**`
- Line 16: `- [ ] **CRED-05**` becomes `- [x] **CRED-05**`
- Line 20: `- [ ] **BRIDGE-01**` becomes `- [x] **BRIDGE-01**`
- Line 21: `- [ ] **BRIDGE-02**` becomes `- [x] **BRIDGE-02**`

**Traceability table fixes (6 changes):** Update phase attribution and status for these rows:
- Line 78: `| CRED-03 | Phase 30 | Pending |` becomes `| CRED-03 | Phase 24 | Complete |`
- Line 79: `| CRED-05 | Phase 30 | Pending |` becomes `| CRED-05 | Phase 24 | Complete |`
- Line 80: `| BRIDGE-01 | Phase 30 | Pending |` becomes `| BRIDGE-01 | Phase 24 | Complete |`
- Line 81: `| BRIDGE-02 | Phase 30 | Pending |` becomes `| BRIDGE-02 | Phase 24 | Complete |`
- Line 84: `| WINBOX-03 | Phase 30 | Pending |` becomes `| WINBOX-03 | Phase 25 | Complete |`
- Line 88: `| WINBOX-04 | Phase 30 | Pending |` becomes `| WINBOX-04 | Phase 27 | Complete |`

Do NOT change any other lines. The WINBOX-03 and WINBOX-04 checkboxes (lines 30-31) are already `[x]` and must remain so.
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia && grep -c "\- \[ \]" .planning/REQUIREMENTS.md | grep -q "^4$" && grep "\[x\] \*\*CRED-03\*\*" .planning/REQUIREMENTS.md && grep "\[x\] \*\*CRED-05\*\*" .planning/REQUIREMENTS.md && grep "\[x\] \*\*BRIDGE-01\*\*" .planning/REQUIREMENTS.md && grep "\[x\] \*\*BRIDGE-02\*\*" .planning/REQUIREMENTS.md && grep "CRED-03 | Phase 24 | Complete" .planning/REQUIREMENTS.md && grep "WINBOX-03 | Phase 25 | Complete" .planning/REQUIREMENTS.md && grep "WINBOX-04 | Phase 27 | Complete" .planning/REQUIREMENTS.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "[x] **CRED-03**" .planning/REQUIREMENTS.md returns a match
    - grep "[x] **CRED-05**" .planning/REQUIREMENTS.md returns a match
    - grep "[x] **BRIDGE-01**" .planning/REQUIREMENTS.md returns a match
    - grep "[x] **BRIDGE-02**" .planning/REQUIREMENTS.md returns a match
    - grep "CRED-03 | Phase 24 | Complete" .planning/REQUIREMENTS.md returns a match
    - grep "CRED-05 | Phase 24 | Complete" .planning/REQUIREMENTS.md returns a match
    - grep "BRIDGE-01 | Phase 24 | Complete" .planning/REQUIREMENTS.md returns a match
    - grep "BRIDGE-02 | Phase 24 | Complete" .planning/REQUIREMENTS.md returns a match
    - grep "WINBOX-03 | Phase 25 | Complete" .planning/REQUIREMENTS.md returns a match
    - grep "WINBOX-04 | Phase 27 | Complete" .planning/REQUIREMENTS.md returns a match
    - Total unchecked requirements in file is exactly 4 (WINBOX-01, WINBOX-02, BRIDGE-05, TRAY-04 — all deferred to Phase 31)
  </acceptance_criteria>
  <done>CRED-03, CRED-05, BRIDGE-01, BRIDGE-02 checkboxes are marked [x] and all 6 traceability table rows show the correct implementing phase and "Complete" status</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead testSSHProfile function from client.ts</name>
  <files>frontend/src/api/client.ts</files>
  <read_first>
    - frontend/src/api/client.ts (current state — testSSHProfile is at lines 415-426, between deleteCredentialProfile and the Device Credential Profile Assignments section comment)
  </read_first>
  <action>
Delete the `testSSHProfile` function (lines 415-426 of `frontend/src/api/client.ts`) and the blank line before it (line 414). The function is:

```typescript
export async function testSSHProfile(id: string, targetIP: string): Promise<{ success: boolean; error?: string }> {
  const response = await requestJSONWithBody(
    `/api/v1/ssh-profiles/${encodeURIComponent(id)}/test`,
    'POST',
    { target_ip: targetIP },
  );
  const data = response as Record<string, unknown>;
  return {
    success: data.success === true,
    error: typeof data.error === 'string' ? data.error : undefined,
  };
}
```

This function references the defunct `/api/v1/ssh-profiles/` endpoint (renamed to `/api/v1/credential-profiles/` in Phase 23) and is not imported or called anywhere in the codebase. After deletion, ensure line 413 (`}` closing deleteCredentialProfile) is followed by a single blank line, then the `// --- Device Credential Profile Assignments ---` comment.

After editing, run TypeScript compilation and the full test suite to confirm no regressions.
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia/frontend && grep -c "testSSHProfile" src/api/client.ts | grep -q "^0$" && grep -c "ssh-profiles" src/api/client.ts | grep -q "^0$" && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - grep "testSSHProfile" frontend/src/api/client.ts returns 0 matches
    - grep "ssh-profiles" frontend/src/api/client.ts returns 0 matches
    - npx tsc --noEmit exits with code 0
    - npx vitest run exits with code 0 (all tests pass)
    - The line "// --- Device Credential Profile Assignments ---" still exists in client.ts (immediately after deleteCredentialProfile function)
  </acceptance_criteria>
  <done>The testSSHProfile function is completely removed from client.ts, TypeScript compiles cleanly, and all frontend tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan modifies only planning documentation and removes dead code.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Information Disclosure | testSSHProfile dead code | mitigate | Remove the function entirely — it references a defunct endpoint path (`/api/v1/ssh-profiles/`) that could confuse future developers into re-exposing a removed API surface |
| T-30-02 | Tampering | REQUIREMENTS.md edits | accept | Documentation-only changes; no runtime impact; changes reflect already-verified implementation state from Phase 24/25/27 VERIFICATION.md files |
</threat_model>

<verification>
After both tasks complete:
1. `grep -c "\- \[ \]" .planning/REQUIREMENTS.md` returns exactly 4 (the Phase 31 requirements: WINBOX-01, WINBOX-02, BRIDGE-05, TRAY-04)
2. `grep "testSSHProfile" frontend/src/api/client.ts` returns no matches
3. `grep "ssh-profiles" frontend/src/api/client.ts` returns no matches
4. `cd frontend && npx tsc --noEmit` exits 0
5. `cd frontend && npx vitest run` — all tests pass
</verification>

<success_criteria>
1. Phase 25 has a VERIFICATION.md confirming all 5 success criteria pass — ALREADY EXISTS (verified 2026-04-08, score 5/5)
2. Phase 27 has a VERIFICATION.md confirming all 4 success criteria pass — ALREADY EXISTS (verified 2026-04-08, score 11/11)
3. REQUIREMENTS.md checkboxes for CRED-03, CRED-05, BRIDGE-01, BRIDGE-02 are marked [x] — Task 1
4. `testSSHProfile` is removed from `frontend/src/api/client.ts` — Task 2
</success_criteria>

<output>
After completion, create `.planning/phases/30-gap-closure-verification-docs-dead-code/30-01-SUMMARY.md`
</output>