---
phase: 02-component-restyling
plan: 04
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - frontend/src/components/Toolbar.tsx
  - frontend/src/components/SidePanel.tsx
  - frontend/src/components/ZoomControls.tsx
  - frontend/src/components/AlertsPanel.tsx
  - frontend/src/components/ShortcutHelp.tsx
  - frontend/src/components/ReconnectBanner.tsx
autonomous: true
requirements: [COMP-04, COMP-06, COMP-12]
must_haves:
  truths:
    - "Toolbar uses Material Symbols icons instead of inline SVGs"
    - "Toolbar has no border-b separators between buttons"
    - "SidePanel has no border-l or border-b layout separators"
    - "ZoomControls uses Material Symbols zoom_in/zoom_out/fit_screen icons"
    - "ZoomControls has no border-b separators between buttons"
    - "AlertsPanel firing alert dots have glow effect"
    - "AlertsPanel alert cards use surface tiers instead of border"
  artifacts:
    - path: "frontend/src/components/Toolbar.tsx"
      provides: "Toolbar with Material Symbols icons and no-line rule"
    - path: "frontend/src/components/SidePanel.tsx"
      provides: "SidePanel with no border separators"
    - path: "frontend/src/components/ZoomControls.tsx"
      provides: "ZoomControls with Material Symbols icons and no-line rule"
    - path: "frontend/src/components/AlertsPanel.tsx"
      provides: "AlertsPanel with glow status dots and surface tier cards"
  key_links:
    - from: "frontend/src/components/Toolbar.tsx"
      to: "frontend/src/components/MaterialIcon.tsx"
      via: "import MaterialIcon"
      pattern: "MaterialIcon"
    - from: "frontend/src/components/ZoomControls.tsx"
      to: "frontend/src/components/MaterialIcon.tsx"
      via: "import MaterialIcon"
      pattern: "MaterialIcon"
---

<objective>
Restyle navigation controls (Toolbar, SidePanel, ZoomControls) with Material Symbols icons and no-line rule. Restyle AlertsPanel with glow dots and surface tiers. Restyle ShortcutHelp and ReconnectBanner for visual consistency.

Purpose: These six components complete the control surface restyling and the alert visual treatment, covering COMP-04 (navigation controls) and COMP-06 (AlertsPanel).
Output: All six components restyled with Material Symbols, no-line rule, surface tiers, glow effects.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-restyling/02-CONTEXT.md
@.planning/phases/02-component-restyling/02-RESEARCH.md
@.planning/phases/02-component-restyling/02-UI-SPEC.md
@.planning/phases/02-component-restyling/02-01-SUMMARY.md

<interfaces>
From frontend/src/components/MaterialIcon.tsx (Plan 01 output):
```typescript
export function MaterialIcon({ name, className = '', size = 18 }: MaterialIconProps) { ... }
```

From frontend/src/components/Toolbar.tsx (current -- 6 inline SVGs, border-b separators):
```typescript
interface ToolbarProps {
  onSearch: () => void; onAddDevice: () => void; onCreateLink: () => void;
  onAlerts: () => void; onSettings: () => void; onToggleEditMode: () => void;
  editMode: boolean; alertCount?: number;
}
// Buttons array: each has label, onClick, active (optional), icon (SVG JSX)
// Container: `border border-outline bg-surface/90 shadow-canvas backdrop-blur-xl`
// Button: `border-b border-outline` between each (except last)
```

From frontend/src/components/ZoomControls.tsx (current -- text +/-/Fit, border-b separators):
```typescript
interface ZoomControlsProps {
  onZoomIn: () => void; onZoomOut: () => void; onFitView: () => void;
}
// Button: `border-b border-outline bg-surface/90` between each
// Container: `border border-outline shadow-canvas backdrop-blur-xl`
```

From frontend/src/components/SidePanel.tsx (current borders):
```typescript
// Container: `bg-surface border-l border-outline`
// Header: `border-b border-outline`
// Close button: inline SVG (X icon)
```

From frontend/src/components/AlertsPanel.tsx (current):
```typescript
// stateBadge('firing'): `h-2 w-2 rounded-full bg-status-down animate-pulse`
// Alert cards: `rounded-lg border border-outline bg-elevated p-3`
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Toolbar with Material Symbols icons and no-line rule</name>
  <files>frontend/src/components/Toolbar.tsx</files>
  <read_first>
    - frontend/src/components/Toolbar.tsx
    - frontend/src/components/MaterialIcon.tsx
  </read_first>
  <action>
Update `frontend/src/components/Toolbar.tsx`:

**1. Add import:**
```typescript
import { MaterialIcon } from './MaterialIcon';
```

**2. Replace all 6 inline SVG icons with MaterialIcon in the buttons array.** Replace each `icon:` value:

- Edit Mode button: `icon: <MaterialIcon name="edit" />`
- Search button: `icon: <MaterialIcon name="search" />`
- Add Device button: `icon: <MaterialIcon name="add" />`
- Create Link button: `icon: <MaterialIcon name="link" />`
- Alerts button: The icon is wrapped in a `<div className="relative">` for the badge. Keep the wrapper, replace only the SVG inside:
  ```tsx
  icon: (
    <div className="relative">
      <MaterialIcon name="notifications" />
      {alertCount > 0 && (
        <span className="absolute -top-1.5 -right-1.5 flex h-3.5 min-w-[14px] items-center justify-center rounded-full bg-status-down px-0.5 text-[9px] font-bold text-white">
          {alertCount > 99 ? '99+' : alertCount}
        </span>
      )}
    </div>
  )
  ```
  Note: The `alertCount` badge must remain. Move it from inside the button definition to the `icon` field. Since `alertCount` is a prop, the buttons array needs to be inside the component body (it already is).
- Settings button: `icon: <MaterialIcon name="settings" />`

**3. Remove border-b separators between buttons (no-line rule, COMP-12):**
Line 94: Remove `${i !== buttons.length - 1 ? 'border-b border-outline' : ''}` from the button className. Buttons are separated by their background shifts on hover -- no border needed.

**4. Remove container border (no-line rule):**
Line 90: Change `border border-outline bg-surface/90` to just `bg-surface/90`. The `shadow-canvas` already provides depth. Keep `rounded-xl` and `overflow-hidden`.

**5. Apply dark-only backdrop-blur:**
Line 90: Change `backdrop-blur-xl` to `dark:backdrop-blur-xl`.

**6. Add transition-colors:**
Add `transition-colors duration-200` to the container div.

**7. Update button active state:** The existing active state class `bg-primary/15 text-primary` is fine. Ensure the MaterialIcon inherits color via `currentColor` (it does by default since the icon renders as text).
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia/frontend && npx vite build 2>&1 | tail -5 && npx vitest run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `Toolbar.tsx` imports `MaterialIcon` from `'./MaterialIcon'`
    - `Toolbar.tsx` does NOT contain any `<svg` elements
    - `Toolbar.tsx` does NOT contain `border-b border-outline` between buttons
    - `Toolbar.tsx` does NOT contain `border border-outline` on container (only `bg-surface/90`)
    - `Toolbar.tsx` contains `dark:backdrop-blur-xl` (NOT unconditional `backdrop-blur-xl`)
    - `Toolbar.tsx` contains MaterialIcon with names: `edit`, `search`, `add`, `link`, `notifications`, `settings`
    - Alert count badge is retained with `bg-status-down` class
    - `npx vite build` exits 0
  </acceptance_criteria>
  <done>Toolbar uses Material Symbols icons with no border separators and dark-only backdrop-blur</done>
</task>

<task type="auto">
  <name>Task 2a: Restyle SidePanel and ZoomControls</name>
  <files>frontend/src/components/SidePanel.tsx, frontend/src/components/ZoomControls.tsx</files>
  <read_first>
    - frontend/src/components/SidePanel.tsx
    - frontend/src/components/ZoomControls.tsx
    - frontend/src/components/MaterialIcon.tsx
  </read_first>
  <action>
**SidePanel.tsx:**

1. Add import: `import { MaterialIcon } from './MaterialIcon';`

2. Remove `border-l border-outline` from container (line 24). The panel already has `shadow-2xl` for depth. Change `shadow-2xl` to `shadow-panel` for consistency with token system.

3. Remove `border-b border-outline` from header (line 27). Use a surface tier shift instead: change header div to `bg-surface-high` (it sits on top of `bg-surface` container, providing visual separation without a border).

4. Replace close button SVG (lines 34-36) with:
```tsx
<MaterialIcon name="close" size={20} />
```

5. Add `transition-colors duration-200` to the container div.

**ZoomControls.tsx:**

1. Add import: `import { MaterialIcon } from './MaterialIcon';`

2. Remove `border-b border-outline` from buttonClassName (line 13). Remove `last:border-b-0` too. Buttons separate by hover state contrast, not borders.

3. Remove `border border-outline` from container div (line 17). Keep `shadow-canvas`.

4. Apply dark-only backdrop-blur: Change `backdrop-blur-xl` to `dark:backdrop-blur-xl` on container.

5. Replace the three button text contents:
- `+` becomes `<MaterialIcon name="zoom_in" />`
- `-` becomes `<MaterialIcon name="zoom_out" />`
- `Fit` becomes `<MaterialIcon name="fit_screen" />`

6. Remove `text-lg` and `text-sm font-semibold` from button classes since MaterialIcon handles its own sizing.

7. Add `transition-colors duration-200` to the container div.
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia/frontend && npx vite build 2>&1 | tail -5 && npx vitest run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `SidePanel.tsx` does NOT contain `border-l border-outline` or `border-b border-outline`
    - `SidePanel.tsx` imports `MaterialIcon` and does NOT contain `<svg`
    - `SidePanel.tsx` contains `shadow-panel` (not `shadow-2xl`)
    - `SidePanel.tsx` header div contains `bg-surface-high`
    - `ZoomControls.tsx` does NOT contain `border-b border-outline` between buttons
    - `ZoomControls.tsx` does NOT contain `border border-outline` on container
    - `ZoomControls.tsx` imports `MaterialIcon` and contains names `zoom_in`, `zoom_out`, `fit_screen`
    - `ZoomControls.tsx` does NOT contain `<svg` or text characters `+`, `-`, `Fit` as button content
    - `ZoomControls.tsx` contains `dark:backdrop-blur-xl`
    - `npx vite build` exits 0, all tests pass
  </acceptance_criteria>
  <done>SidePanel and ZoomControls restyled with Material Symbols icons, no-line rule, surface tiers, dark-only backdrop-blur</done>
</task>

<task type="auto">
  <name>Task 2b: Restyle ShortcutHelp and ReconnectBanner</name>
  <files>frontend/src/components/ShortcutHelp.tsx, frontend/src/components/ReconnectBanner.tsx</files>
  <read_first>
    - frontend/src/components/ShortcutHelp.tsx
    - frontend/src/components/ReconnectBanner.tsx
  </read_first>
  <action>
**ShortcutHelp.tsx:**

1. Read the file fully. Remove `border border-outline` from the modal container (line 44 area). Keep `shadow-2xl`, change to `shadow-panel`.

2. Remove `border-b border-outline` from the keyboard shortcut rows (line 61 area). Use spacing instead (`space-y-1` or padding).

3. Remove `border border-outline` from the `<kbd>` elements (line 63 area). Change to `bg-surface-high` with no border.

4. Add `transition-colors duration-200` to the modal container.

**ReconnectBanner.tsx:**

1. Read the file fully. Add `transition-colors duration-200` to the banner container.

2. Ensure the banner uses token classes for colors (bg-surface, text-on-bg, etc.). Add a subtle glow if the status is reconnecting: `shadow-[0_0_12px_rgba(255,234,0,0.15)]` for a warm warning glow.
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia/frontend && npx vite build 2>&1 | tail -5 && npx vitest run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `ShortcutHelp.tsx` does NOT contain `border-b border-outline` on shortcut rows
    - `ShortcutHelp.tsx` kbd elements use `bg-surface-high` (not `border border-outline`)
    - `ShortcutHelp.tsx` contains `shadow-panel` (not `shadow-2xl`)
    - `ReconnectBanner.tsx` contains `transition-colors duration-200`
    - `npx vite build` exits 0, all tests pass
  </acceptance_criteria>
  <done>ShortcutHelp and ReconnectBanner restyled with no-line rule, surface tiers, transition-colors</done>
</task>

<task type="auto">
  <name>Task 3: Restyle AlertsPanel with glow status dots and surface tier cards</name>
  <files>frontend/src/components/AlertsPanel.tsx</files>
  <read_first>
    - frontend/src/components/AlertsPanel.tsx
    - frontend/src/components/StatusDot.tsx
    - frontend/src/index.css
  </read_first>
  <action>
Read `frontend/src/components/AlertsPanel.tsx` fully. Make these changes:

**1. Add glow to the `stateBadge` function's firing state.**
Current (line 34): `bg-status-down animate-pulse`
Change to: `bg-status-down animate-pulse shadow-[0_0_10px_rgba(255,23,68,var(--nt-glow-shadow-opacity))] motion-reduce:animate-none`

Also add glow to the resolved state dot:
Current (line 39): `bg-status-up`
Change to: `bg-status-up shadow-[0_0_6px_rgba(0,230,118,var(--nt-glow-shadow-opacity))]`

**2. Replace alert card borders with surface tiers (no-line rule, COMP-12):**
Find `border border-outline bg-elevated` on alert cards (around line 128). Change to just `bg-elevated shadow-panel` -- remove the border, add shadow for depth.

Find `border border-outline/50 bg-elevated/50` on resolved cards (around line 163). Change to `bg-surface-high` -- remove border, use surface tier for the dimmed appearance.

**3. Add transition-colors for theme switching:**
Add `transition-colors duration-200` to alert card containers.

**4. Check empty state:** If there's an empty state message, ensure it uses correct typography tokens. Update empty state icon (if using `check_circle`) to use MaterialIcon if present. Read the full file to check.

**5. Verify severity badges use token classes.** The `severityBadge` function (lines 10-30) should already use `bg-status-down/15 text-status-down` etc. from Phase 1 migration. Confirm and fix any remaining non-token colors.
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia/frontend && npx vite build 2>&1 | tail -5 && npx vitest run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `AlertsPanel.tsx` firing stateBadge contains `shadow-[0_0_10px_rgba(255,23,68,var(--nt-glow-shadow-opacity))]`
    - `AlertsPanel.tsx` resolved stateBadge contains `shadow-[0_0_6px_rgba(0,230,118,var(--nt-glow-shadow-opacity))]`
    - `AlertsPanel.tsx` alert cards do NOT contain `border border-outline` (removed per no-line rule)
    - `AlertsPanel.tsx` alert cards contain `shadow-panel` or `bg-elevated` without border
    - `AlertsPanel.tsx` contains `motion-reduce:animate-none` on pulse elements
    - `AlertsPanel.tsx` contains `transition-colors duration-200`
    - `npx vite build` exits 0, all tests pass
  </acceptance_criteria>
  <done>AlertsPanel has glow status dots, surface tier alert cards, no border separators</done>
</task>

</tasks>

<verification>
- Toolbar has 6 Material Symbols icons, no inline SVGs, no border separators
- SidePanel has no border-l or border-b, close button is MaterialIcon
- ZoomControls has Material Symbols icons, no border separators
- AlertsPanel has glow status dots and borderless alert cards
- ShortcutHelp has no border separators in shortcut rows
- All components have transition-colors duration-200
- Dark-only backdrop-blur on Toolbar and ZoomControls
- All tests pass, Vite build succeeds
</verification>

<success_criteria>
1. No inline SVGs remain in Toolbar, SidePanel, ZoomControls
2. No layout border separators in any of the 6 components per COMP-12
3. AlertsPanel glow dots use theme-aware opacity variable
4. `cd frontend && npx vitest run` all pass, `npx vite build` clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-restyling/02-04-SUMMARY.md`
</output>
