---
phase: 23-credential-profile-schema-domain
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - internal/repository/sqlite/credential_profile_repo.go
  - internal/repository/sqlite/ssh_profile_repo.go
  - internal/service/backup_service.go
  - internal/api/credential_profile_handler.go
  - internal/api/ssh_profile_handler.go
  - internal/api/router.go
  - internal/api/device_handler.go
  - cmd/theia/main.go
  - internal/service/backup_service_test.go
  - internal/api/ssh_profile_handler_test.go
  - internal/api/credential_profile_handler_test.go
  - internal/api/device_handler_test.go
  - internal/api/oversized_body_test.go
  - internal/api/backup_handler_test.go
  - internal/worker/device_backup_scheduler_test.go
autonomous: true
requirements: [CRED-01, CRED-02, CRED-04]

must_haves:
  truths:
    - "All Go code compiles with zero errors after the SSHProfile -> CredentialProfile rename"
    - "All existing Go tests pass after the rename (no behavior changes)"
    - "BackupService resolves credentials via device.SSHProfileID and renamed repository methods (logic unchanged per D-10)"
    - "API routes at /api/v1/ssh-profiles continue to work (paths preserved for frontend compatibility)"
    - "The credential_profile_repo.go queries reference the credential_profiles table (not ssh_profiles)"
    - "The Role field is read from and written to the database in credential_profile_repo.go"
  artifacts:
    - path: "internal/repository/sqlite/credential_profile_repo.go"
      provides: "CredentialProfileRepo implementing CredentialProfileRepository"
      exports: ["CredentialProfileRepo", "NewCredentialProfileRepo"]
    - path: "internal/api/credential_profile_handler.go"
      provides: "CredentialProfileHandler with renamed types but same API behavior"
      exports: ["CredentialProfileHandler", "NewCredentialProfileHandler"]
    - path: "internal/service/backup_service.go"
      provides: "BackupService with credentialProfileRepo field (renamed from sshProfileRepo)"
    - path: "internal/api/router.go"
      provides: "Router wiring CredentialProfileRepo and CredentialProfileHandler"
  key_links:
    - from: "internal/repository/sqlite/credential_profile_repo.go"
      to: "internal/domain/credential_profile.go"
      via: "implements domain.CredentialProfileRepository interface"
      pattern: "domain\\.CredentialProfile"
    - from: "internal/service/backup_service.go"
      to: "internal/domain/credential_profile.go"
      via: "credentialProfileRepo field typed as domain.CredentialProfileRepository"
      pattern: "credentialProfileRepo.*domain\\.CredentialProfileRepository"
    - from: "internal/api/credential_profile_handler.go"
      to: "internal/service/backup_service.go"
      via: "calls service methods (renamed from SSHProfile to CredentialProfile)"
      pattern: "svc\\.Create.*CredentialProfile\\|svc\\.Get.*CredentialProfile"
    - from: "cmd/theia/main.go"
      to: "internal/repository/sqlite/credential_profile_repo.go"
      via: "NewCredentialProfileRepo(db) constructor call"
      pattern: "sqlite\\.NewCredentialProfileRepo"
---

<objective>
Rename all Go consumers of the old SSHProfile types to use CredentialProfile: repository implementation, service layer, API handler, router wiring, main.go, and all test files. The rename is purely cosmetic on the Go side — no behavioral changes. API URL paths stay as /api/v1/ssh-profiles for frontend compatibility (path rename deferred).

Purpose: Complete the SSHProfile -> CredentialProfile rename across the entire Go codebase so all downstream phases (24-27) work with clean canonical names.
Output: Renamed Go files, updated imports, passing tests, compiling codebase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-credential-profile-schema-domain/23-CONTEXT.md
@.planning/phases/23-credential-profile-schema-domain/23-01-SUMMARY.md

@internal/domain/credential_profile.go
@internal/domain/backup.go
@internal/repository/sqlite/ssh_profile_repo.go
@internal/service/backup_service.go
@internal/api/ssh_profile_handler.go
@internal/api/router.go
@internal/api/device_handler.go
@cmd/theia/main.go

<interfaces>
<!-- New domain types from Plan 01 that this plan implements against -->

From internal/domain/credential_profile.go (created by Plan 01):
```go
type CredentialProfile struct {
    ID              uuid.UUID     `json:"id"`
    Name            string        `json:"name"`
    Description     string        `json:"description"`
    Username        string        `json:"username"`
    Port            int           `json:"port"`
    AuthMethod      SSHAuthMethod `json:"auth_method"`
    EncryptedSecret string        `json:"-"`
    Role            string        `json:"role"`
    CreatedAt       time.Time     `json:"created_at"`
    UpdatedAt       time.Time     `json:"updated_at"`
}

type CredentialProfileRepository interface {
    Create(profile *CredentialProfile) error
    GetByID(id uuid.UUID) (*CredentialProfile, error)
    GetAll() ([]CredentialProfile, error)
    Update(profile *CredentialProfile) error
    Delete(id uuid.UUID) error
}
```

From internal/domain/backup.go (UNCHANGED):
```go
type SSHAuthMethod string
const (
    SSHAuthPassword SSHAuthMethod = "password"
    SSHAuthKey      SSHAuthMethod = "key"
)
```

From internal/domain/device.go (UNCHANGED — SSHProfileID stays):
```go
type Device struct {
    // ... other fields ...
    SSHProfileID *uuid.UUID `json:"ssh_profile_id,omitempty"`
    // ... other fields ...
}
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API handler -> JSON response | CredentialProfile must never leak EncryptedSecret in responses |
| Repository -> SQLite | SQL queries must reference credential_profiles table (not ssh_profiles) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04 | Information Disclosure | credential_profile_handler.go response mapping | mitigate | The `credentialProfileToResponse` function maps only safe fields (id, name, description, username, port, auth_method, role, created_at, updated_at). EncryptedSecret is excluded from the response struct. Verify response struct has no EncryptedSecret field. |
| T-23-05 | Tampering | credential_profile_repo.go SQL queries | mitigate | All SQL queries use parameterized `?` placeholders (no string interpolation). Table name is `credential_profiles` (hardcoded string, not variable). Same pattern as existing ssh_profile_repo.go. |
| T-23-06 | Information Disclosure | backup_service.go CredentialProfile handling | accept | BackupService already handles EncryptedSecret correctly — decrypts only for SSH connection, never returns it. Type rename does not alter this flow. Low risk. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Rename repository and API handler files with updated types and SQL</name>
  <files>
    internal/repository/sqlite/credential_profile_repo.go
    internal/repository/sqlite/ssh_profile_repo.go
    internal/api/credential_profile_handler.go
    internal/api/ssh_profile_handler.go
  </files>
  <read_first>
    internal/repository/sqlite/ssh_profile_repo.go
    internal/api/ssh_profile_handler.go
    internal/domain/credential_profile.go
  </read_first>
  <action>
**Repository rename (per D-03):**

Create `internal/repository/sqlite/credential_profile_repo.go` based on the existing `ssh_profile_repo.go` with these changes:

1. Rename struct: `SSHProfileRepo` -> `CredentialProfileRepo`
2. Rename constructor: `NewSSHProfileRepo` -> `NewCredentialProfileRepo`
3. All SQL queries change table name from `ssh_profiles` to `credential_profiles`
4. All SELECT statements add `role` to the column list (after `encrypted_secret`, before `created_at`)
5. INSERT adds `role` column and value: `profile.Role`
6. UPDATE adds `role=?` to SET clause with `profile.Role` in params
7. All `domain.SSHProfile` references become `domain.CredentialProfile`
8. Scan helpers `scanSSHProfile`/`scanSSHProfileRow` become `scanCredentialProfile`/`scanCredentialProfileRow` and include `&p.Role` in the scan
9. Error messages change from "ssh profile" to "credential profile"
10. `IsInUse` method stays and continues to query `devices WHERE ssh_profile_id = ?` (legacy FK still exists per D-06)
11. Doc comments updated from "SSH profile" to "credential profile"

Concrete SQL changes:
- INSERT: `INSERT INTO credential_profiles (id, name, description, username, port, auth_method, encrypted_secret, role, created_at, updated_at) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`
- SELECT (GetByID): `SELECT id, name, description, username, port, auth_method, encrypted_secret, role, created_at, updated_at FROM credential_profiles WHERE id = ?`
- SELECT (GetAll): `SELECT id, name, description, username, port, auth_method, encrypted_secret, role, created_at, updated_at FROM credential_profiles ORDER BY name ASC`
- UPDATE: `UPDATE credential_profiles SET name=?, description=?, username=?, port=?, auth_method=?, encrypted_secret=?, role=?, updated_at=? WHERE id=?`
- DELETE: `DELETE FROM credential_profiles WHERE id = ?`
- IsInUse: `SELECT COUNT(*) FROM devices WHERE ssh_profile_id = ?` (unchanged — legacy FK)

Scan order in helpers: `&idStr, &p.Name, &p.Description, &p.Username, &p.Port, &authMethod, &p.EncryptedSecret, &p.Role, &p.CreatedAt, &p.UpdatedAt`

Delete `internal/repository/sqlite/ssh_profile_repo.go` after creating the new file.

**API handler rename (per D-03):**

Create `internal/api/credential_profile_handler.go` based on `ssh_profile_handler.go` with these changes:

1. Struct: `SSHProfileHandler` -> `CredentialProfileHandler`
2. Constructor: `NewSSHProfileHandler(svc, sshProfileRepo)` -> `NewCredentialProfileHandler(svc, credentialProfileRepo)` where the repo param type changes from `*sqlite.SSHProfileRepo` to `*sqlite.CredentialProfileRepo`
3. Field: `sshProfileRepo` -> `credentialProfileRepo`
4. Request struct: `sshProfileRequest` -> `credentialProfileRequest` — add `Role string `json:"role"`` field
5. Response struct: `sshProfileResponse` -> `credentialProfileResponse` — add `Role string `json:"role"`` field
6. Response mapper: `sshProfileToResponse` -> `credentialProfileToResponse` — include `Role: p.Role`
7. All method signatures stay the same: `HandleList`, `HandleCreate`, `HandleGet`, `HandleUpdate`, `HandleDelete`, `HandleTest`
8. `HandleCreate`: Pass `req.Role` through to service. If `req.Role == ""`, default to `"Admin"`
9. `HandleUpdate`: Pass `req.Role` through to service
10. `extractIDFromPath` calls keep the same prefix `/api/v1/ssh-profiles/` (API paths NOT renamed in this phase)
11. Service method calls change from `svc.CreateSSHProfile` -> `svc.CreateCredentialProfile`, etc. (see Task 2)
12. The `HandleDelete` method calls `h.credentialProfileRepo.IsInUse(id)` instead of `h.sshProfileRepo.IsInUse(id)`

IMPORTANT: The response struct `credentialProfileResponse` must NOT include an `EncryptedSecret` field (T-23-04 mitigation). Only include: ID, Name, Description, Username, Port, AuthMethod, Role, CreatedAt, UpdatedAt.

Delete `internal/api/ssh_profile_handler.go` after creating the new file.
  </action>
  <verify>
    <automated>test -f internal/repository/sqlite/credential_profile_repo.go && ! test -f internal/repository/sqlite/ssh_profile_repo.go && test -f internal/api/credential_profile_handler.go && ! test -f internal/api/ssh_profile_handler.go && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `credential_profile_repo.go` exists and `ssh_profile_repo.go` does NOT exist
    - `credential_profile_repo.go` contains `type CredentialProfileRepo struct`
    - `credential_profile_repo.go` contains `func NewCredentialProfileRepo`
    - `credential_profile_repo.go` contains `INSERT INTO credential_profiles` (not `ssh_profiles`)
    - `credential_profile_repo.go` contains `FROM credential_profiles` in SELECT queries
    - `credential_profile_repo.go` contains `role` in INSERT and SELECT column lists
    - `credential_profile_repo.go` scan helpers include `&p.Role`
    - `credential_profile_repo.go` contains `IsInUse` method with `FROM devices WHERE ssh_profile_id`
    - `credential_profile_handler.go` exists and `ssh_profile_handler.go` does NOT exist
    - `credential_profile_handler.go` contains `type CredentialProfileHandler struct`
    - `credential_profile_handler.go` response struct contains `Role string` field
    - `credential_profile_handler.go` response struct does NOT contain `EncryptedSecret`
    - `credential_profile_handler.go` contains `extractIDFromPath(r.URL.Path, "/api/v1/ssh-profiles/")` (paths unchanged)
  </acceptance_criteria>
  <done>Repository and handler files renamed with all type references updated. SQL queries reference credential_profiles table. Role field included in all CRUD operations. Old files deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Rename service methods, update router wiring, and update main.go</name>
  <files>
    internal/service/backup_service.go
    internal/api/router.go
    internal/api/device_handler.go
    cmd/theia/main.go
  </files>
  <read_first>
    internal/service/backup_service.go
    internal/api/router.go
    internal/api/device_handler.go
    cmd/theia/main.go
    internal/domain/credential_profile.go
  </read_first>
  <action>
**backup_service.go changes (per D-10 — type rename only, logic unchanged):**

1. Field rename: `sshProfileRepo domain.SSHProfileRepository` -> `credentialProfileRepo domain.CredentialProfileRepository`
2. Constructor param: `sshProfileRepo domain.SSHProfileRepository` -> `credentialProfileRepo domain.CredentialProfileRepository`
3. Constructor body: `sshProfileRepo: sshProfileRepo` -> `credentialProfileRepo: credentialProfileRepo`
4. All `s.sshProfileRepo.` calls become `s.credentialProfileRepo.`
5. `runFullBackup` param type: `profile *domain.SSHProfile` -> `profile *domain.CredentialProfile`
6. Method renames:
   - `CreateSSHProfile` -> `CreateCredentialProfile` — signature changes `*domain.SSHProfile` to `*domain.CredentialProfile`, adds `role string` parameter. Body creates `&domain.CredentialProfile{..., Role: role}`. If role is empty, default to `"Admin"`.
   - `GetSSHProfile` -> `GetCredentialProfile` — return type `*domain.SSHProfile` -> `*domain.CredentialProfile`
   - `GetAllSSHProfiles` -> `GetAllCredentialProfiles` — return type `[]domain.SSHProfile` -> `[]domain.CredentialProfile`
   - `UpdateSSHProfile` -> `UpdateCredentialProfile` — adds `role string` parameter, sets `profile.Role = role`
   - `DeleteSSHProfile` -> `DeleteCredentialProfile`
   - `TestSSHProfile` -> `TestCredentialProfile`
   - `TestSSHConnection` stays as `TestSSHConnection` (it tests SSH connectivity, not a "credential profile" concept)
7. Logic in `TriggerBackup`, `TriggerBulkBackup`, `TestSSHConnection` is UNCHANGED — still reads `device.SSHProfileID`, still calls `s.credentialProfileRepo.GetByID()`. Only the field/type names change.

**router.go changes:**

1. `NewRouter` signature: `sshProfileRepo *sqlite.SSHProfileRepo` -> `credentialProfileRepo *sqlite.CredentialProfileRepo`
2. Import: `sqlite` package already imported — just the type name changes
3. Handler creation: `sshProfileHandler := NewSSHProfileHandler(backupService, sshProfileRepo)` -> `credentialProfileHandler := NewCredentialProfileHandler(backupService, credentialProfileRepo)`
4. `deviceHandler` creation: `NewDeviceHandler(deviceService, sshProfileRepo, vendorRegistry)` -> `NewDeviceHandler(deviceService, credentialProfileRepo, vendorRegistry)` (but see device_handler.go below)
5. Route handler references: `sshProfileHandler.HandleList` -> `credentialProfileHandler.HandleList`, etc.
6. API URL paths stay as `/api/v1/ssh-profiles` and `/api/v1/ssh-profiles/` (NOT renamed — frontend compatibility)

**device_handler.go changes:**

`device_handler.go` stores `sshProfileRepo domain.SSHProfileRepository` and calls `h.sshProfileRepo.GetByID()` in **three places** to validate that a provided `ssh_profile_id` exists before creating/updating a device:
1. Device create (validates `ssh_profile_id` in request payload)
2. Device update (validates `ssh_profile_id` in request payload)
3. Batch-add (validates `ssh_profile_id` in request payload)

Rename the field and all three call sites:
- Struct field: `sshProfileRepo domain.SSHProfileRepository` -> `credentialProfileRepo domain.CredentialProfileRepository`
- Constructor `NewDeviceHandler` param: `sshProfileRepo domain.SSHProfileRepository` -> `credentialProfileRepo domain.CredentialProfileRepository`
- All 3 call sites: `h.sshProfileRepo.GetByID` -> `h.credentialProfileRepo.GetByID`
- Error messages at those call sites: `"ssh profile not found"` -> `"credential profile not found"`

Note: The `DeviceHandler` still references `SSHProfileID` in device create/update request structs and attribute mapping — these are `device.SSHProfileID` field references which stay (the Device struct field is NOT being renamed in this phase).

**main.go changes:**

1. `sshProfileRepo := sqlite.NewSSHProfileRepo(db)` -> `credentialProfileRepo := sqlite.NewCredentialProfileRepo(db)`
2. `service.NewBackupService(backupJobRepo, backupFileRepo, sshProfileRepo, ...)` -> `service.NewBackupService(backupJobRepo, backupFileRepo, credentialProfileRepo, ...)`
3. `api.NewRouter(db, deviceService, linkRepo, positionRepo, settingsRepo, snmpProfileRepo, sshProfileRepo, ...)` -> `api.NewRouter(db, deviceService, linkRepo, positionRepo, settingsRepo, snmpProfileRepo, credentialProfileRepo, ...)`
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia && grep -r 'SSHProfileRepo\|SSHProfileRepository\|SSHProfile[^I]' --include='*.go' internal/ cmd/ | grep -v '_test.go' | grep -v 'SSHAuthMethod\|SSHAuthPassword\|SSHAuthKey\|SSHProfileID\|ssh_profile_id' | head -20 || echo "No stale SSHProfile references found (PASS)"</automated>
  </verify>
  <acceptance_criteria>
    - `backup_service.go` contains `credentialProfileRepo domain.CredentialProfileRepository` (not sshProfileRepo)
    - `backup_service.go` contains `func (s *BackupService) CreateCredentialProfile`
    - `backup_service.go` contains `func (s *BackupService) GetCredentialProfile`
    - `backup_service.go` contains `func (s *BackupService) GetAllCredentialProfiles`
    - `backup_service.go` contains `func (s *BackupService) UpdateCredentialProfile`
    - `backup_service.go` contains `func (s *BackupService) DeleteCredentialProfile`
    - `backup_service.go` contains `func (s *BackupService) TestCredentialProfile`
    - `backup_service.go` still contains `device.SSHProfileID` in TriggerBackup/TriggerBulkBackup (logic unchanged per D-10)
    - `router.go` contains `credentialProfileRepo *sqlite.CredentialProfileRepo` in NewRouter params
    - `router.go` contains `credentialProfileHandler := NewCredentialProfileHandler`
    - `router.go` still contains `/api/v1/ssh-profiles` URL paths (NOT renamed)
    - `device_handler.go` contains `credentialProfileRepo domain.CredentialProfileRepository`
    - `device_handler.go` contains `h.credentialProfileRepo.GetByID` (3 occurrences — device create, device update, batch-add)
    - `device_handler.go` does NOT contain `h.sshProfileRepo.GetByID`
    - `device_handler.go` does NOT contain `"ssh profile not found"` (updated to `"credential profile not found"`)
    - `main.go` contains `credentialProfileRepo := sqlite.NewCredentialProfileRepo(db)`
    - No remaining `SSHProfileRepo`, `SSHProfileRepository`, or `domain.SSHProfile` references in non-test Go files (excluding SSHAuthMethod, SSHAuthPassword, SSHAuthKey, SSHProfileID, ssh_profile_id)
  </acceptance_criteria>
  <done>All service methods renamed from SSHProfile to CredentialProfile. Router wired with new types. main.go uses NewCredentialProfileRepo. API URL paths preserved. BackupService logic unchanged (still uses device.SSHProfileID).</done>
</task>

<task type="auto">
  <name>Task 3: Update all test files and verify full compilation + test pass</name>
  <files>
    internal/service/backup_service_test.go
    internal/api/credential_profile_handler_test.go
    internal/api/ssh_profile_handler_test.go
    internal/api/device_handler_test.go
    internal/api/oversized_body_test.go
    internal/api/backup_handler_test.go
    internal/worker/device_backup_scheduler_test.go
  </files>
  <read_first>
    internal/service/backup_service_test.go
    internal/api/ssh_profile_handler_test.go
    internal/api/device_handler_test.go
    internal/api/oversized_body_test.go
    internal/api/backup_handler_test.go
    internal/worker/device_backup_scheduler_test.go
    internal/domain/credential_profile.go
  </read_first>
  <action>
**backup_service_test.go changes:**

1. Mock struct: `mockSSHProfileRepo` -> `mockCredentialProfileRepo`
2. Mock constructor: `newMockSSHProfileRepo` -> `newMockCredentialProfileRepo`
3. All `domain.SSHProfile` references -> `domain.CredentialProfile`
4. All mock method signatures updated to match `CredentialProfileRepository` interface
5. Mock map type: `map[uuid.UUID]*domain.SSHProfile` -> `map[uuid.UUID]*domain.CredentialProfile`
6. Test calls: `sshProfileRepo.Create(&domain.SSHProfile{...})` -> `credentialProfileRepo.Create(&domain.CredentialProfile{..., Role: "Admin"})`
7. Service constructor calls: update `sshProfileRepo` variable to `credentialProfileRepo`
8. All `&domain.SSHProfile{` -> `&domain.CredentialProfile{` with `Role: "Admin"` added

**ssh_profile_handler_test.go -> credential_profile_handler_test.go:**

1. Rename file: `ssh_profile_handler_test.go` -> `credential_profile_handler_test.go`
2. Setup function: `setupSSHProfileTest` -> `setupCredentialProfileTest`, returns `(*CredentialProfileHandler, *sqlite.CredentialProfileRepo, *sql.DB)`
3. All `sqlite.NewSSHProfileRepo` -> `sqlite.NewCredentialProfileRepo`
4. All `NewSSHProfileHandler` -> `NewCredentialProfileHandler`
5. All `domain.SSHProfile{` -> `domain.CredentialProfile{` with `Role: "Admin"` added
6. Test function names: `TestSSHProfileHandler*` -> `TestCredentialProfileHandler*` and `TestSSHProfile_*` -> `TestCredentialProfile_*`
7. All `SSHProfileHandler` references -> `CredentialProfileHandler`
8. Delete `ssh_profile_handler_test.go`

**device_handler_test.go changes:**

1. Mock: `mockSSHProfileRepo` -> `mockCredentialProfileRepo`
2. Constructor: `newMockSSHProfileRepo` -> `newMockCredentialProfileRepo`
3. All `domain.SSHProfile` -> `domain.CredentialProfile`
4. All mock methods match `CredentialProfileRepository` interface
5. `sshProfileRepo := newMockSSHProfileRepo()` -> `credentialProfileRepo := newMockCredentialProfileRepo()`
6. Constructor calls to `NewDeviceHandler` pass `credentialProfileRepo`

**oversized_body_test.go changes:**

1. All `SSHProfileHandler` references -> `CredentialProfileHandler`
2. Test case names: `"SSHProfileHandler.HandleCreate"` -> `"CredentialProfileHandler.HandleCreate"`, etc.
3. `&SSHProfileHandler{}` -> `&CredentialProfileHandler{}`

**backup_handler_test.go changes:**

1. Mock: `newMockSSHProfileRepo()` -> `newMockCredentialProfileRepo()`
2. All `domain.SSHProfile` -> `domain.CredentialProfile`
3. Mock references updated

**device_backup_scheduler_test.go changes:**

1. `stubSSHProfileRepo` -> `stubCredentialProfileRepo`
2. All `domain.SSHProfile` -> `domain.CredentialProfile`
3. Method signatures match `CredentialProfileRepository` interface
4. Constructor usage: `&stubSSHProfileRepo{}` -> `&stubCredentialProfileRepo{}`

After all file changes, run:
```bash
cd /home/azmin/projects/theia && go build ./...
cd /home/azmin/projects/theia && go test ./internal/... ./cmd/...
```

Both commands must succeed with zero errors. If compilation fails, fix remaining references. If tests fail, debug and fix — there should be zero behavior changes (per D-11).
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia && go build ./... && go test ./internal/... ./cmd/... -count=1 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits with code 0 (zero compilation errors)
    - `go test ./internal/... ./cmd/...` exits with code 0 (all tests pass)
    - `internal/api/ssh_profile_handler_test.go` does NOT exist (renamed to credential_profile_handler_test.go)
    - `internal/api/credential_profile_handler_test.go` exists
    - No remaining `mockSSHProfileRepo` or `stubSSHProfileRepo` in any test file
    - No remaining `domain.SSHProfile{` in any test file (all should be `domain.CredentialProfile{`)
    - `grep -r 'SSHProfileRepo\|SSHProfileRepository\|domain\.SSHProfile[^I]' --include='*.go' internal/ cmd/` returns empty (excluding SSHAuthMethod, SSHProfileID patterns)
  </acceptance_criteria>
  <done>All test files updated with CredentialProfile types. Full codebase compiles. All tests pass. No stale SSHProfile type references remain anywhere in Go files (only SSHAuthMethod, SSHAuthPassword, SSHAuthKey, and SSHProfileID survive as intended).</done>
</task>

</tasks>

<verification>
1. Full compilation: `cd /home/azmin/projects/theia && go build ./...`
2. Full test suite: `cd /home/azmin/projects/theia && go test ./internal/... ./cmd/... -count=1`
3. No stale SSHProfile references: `grep -r 'SSHProfileRepo\|SSHProfileRepository' --include='*.go' internal/ cmd/` returns empty
4. No stale file references: `! test -f internal/repository/sqlite/ssh_profile_repo.go && ! test -f internal/api/ssh_profile_handler.go && ! test -f internal/api/ssh_profile_handler_test.go`
5. Role field in repo: `grep 'role' internal/repository/sqlite/credential_profile_repo.go`
6. API paths preserved: `grep 'ssh-profiles' internal/api/router.go` confirms paths NOT changed
</verification>

<success_criteria>
- `go build ./...` succeeds with zero errors
- `go test ./internal/... ./cmd/...` succeeds with all tests passing
- All SSHProfile type/struct/interface references renamed to CredentialProfile (except SSHAuthMethod, SSHProfileID)
- Old files (ssh_profile_repo.go, ssh_profile_handler.go, ssh_profile_handler_test.go) deleted
- New files (credential_profile_repo.go, credential_profile_handler.go, credential_profile_handler_test.go) exist
- Service methods renamed: CreateCredentialProfile, GetCredentialProfile, etc.
- BackupService logic unchanged — still uses device.SSHProfileID for credential resolution
- API URL paths preserved as /api/v1/ssh-profiles (no frontend breakage)
- Role field included in repo CRUD operations and handler request/response
</success_criteria>

<output>
After completion, create `.planning/phases/23-credential-profile-schema-domain/23-02-SUMMARY.md`
</output>
