---
phase: 07-settings-panel-verification-and-phase-2-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/SettingsPanel.tsx
  - frontend/src/components/SettingsPanel.test.tsx
  - .planning/phases/02-component-restyling/02-VERIFICATION.md
autonomous: true
requirements: [COMP-05]
must_haves:
  truths:
    - "SettingsPanel dev badge uses semantic warning tokens instead of stale yellow-500 values"
    - "All 5 settings components (SettingsPanel, SNMPProfileManager, SSHProfileManager, VendorSettingsPanel, AreaManager) confirmed using valid Neon Topography tokens"
    - "Phase 2 VERIFICATION.md exists documenting all 13 Phase 2 requirements as satisfied"
  artifacts:
    - path: "frontend/src/components/SettingsPanel.tsx"
      provides: "Dev badge with semantic warning tokens"
      contains: "bg-warning"
    - path: "frontend/src/components/SettingsPanel.test.tsx"
      provides: "COMP-05 test with dev badge token check"
      contains: "dev badge uses semantic warning token"
    - path: ".planning/phases/02-component-restyling/02-VERIFICATION.md"
      provides: "Phase 2 verification document"
      contains: "COMP-05"
  key_links:
    - from: "frontend/src/components/SettingsPanel.tsx"
      to: "frontend/src/index.css"
      via: "Tailwind token classes"
      pattern: "bg-warning|text-warning"
---

<objective>
Fix the one remaining stale token in SettingsPanel (dev badge), update the COMP-05 test to verify semantic tokens, and create Phase 2 VERIFICATION.md documenting all 13 Phase 2 requirements as satisfied.

Purpose: Close COMP-05 (SettingsPanel restyling) and produce the Phase 2 verification document that confirms the v1.3.0 milestone's component restyling is complete.
Output: Fixed SettingsPanel.tsx, updated test, and Phase 2 VERIFICATION.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-settings-panel-verification-and-phase-2-closure/07-CONTEXT.md
@.planning/phases/02-component-restyling/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SettingsPanel dev badge stale token and update COMP-05 test</name>
  <files>frontend/src/components/SettingsPanel.tsx, frontend/src/components/SettingsPanel.test.tsx</files>
  <read_first>
    - frontend/src/components/SettingsPanel.tsx (current state -- stale token on line 287)
    - frontend/src/components/SettingsPanel.test.tsx (current COMP-05 tests)
    - frontend/src/index.css (token definitions -- verify warning token exists as --color-warning)
  </read_first>
  <action>
In SettingsPanel.tsx, replace the dev badge stale tokens on the conditional className around line 285-289.

Current (stale):
```
import.meta.env.DEV
  ? 'bg-yellow-500/15 text-yellow-400'
  : 'bg-status-up/15 text-status-up'
```

Replace with (semantic):
```
import.meta.env.DEV
  ? 'bg-warning/15 text-warning'
  : 'bg-status-up/15 text-status-up'
```

The `warning` token is defined in the CSS token system (index.css defines `--color-warning: var(--nt-warning)` which Tailwind exposes as `bg-warning` and `text-warning`). This is consistent with existing usage in ReconnectBanner.tsx, CanvasOverlays.tsx, AreaHub.tsx, and metrics.ts. This replaces the hardcoded Tailwind v3 `yellow-500` / `yellow-400` with the semantic Neon Topography token.

Then in SettingsPanel.test.tsx, add a fourth test to the existing `describe('SettingsPanel (COMP-05)')` block:

```typescript
it('dev badge uses semantic warning token (not hardcoded yellow)', () => {
  const { container } = render(<SettingsPanel />);
  const html = container.innerHTML;
  expect(html).not.toContain('yellow-500');
  expect(html).not.toContain('yellow-400');
});
```

This test reads the rendered HTML and confirms no stale yellow-500 or yellow-400 classes appear.
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia/frontend && npx vitest run src/components/SettingsPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SettingsPanel.tsx contains `bg-warning/15 text-warning` (grep confirms)
    - SettingsPanel.tsx does NOT contain `bg-yellow-500` or `text-yellow-400` (grep confirms zero matches)
    - SettingsPanel.test.tsx contains the string `yellow-500` only inside a test assertion (not in production code)
    - All 4 tests in SettingsPanel.test.tsx pass (vitest exits 0)
  </acceptance_criteria>
  <done>SettingsPanel dev badge renders with semantic warning token in dev mode; no stale yellow-* Tailwind classes remain; test verifies this</done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 2 VERIFICATION.md documenting all 13 requirements</name>
  <files>.planning/phases/02-component-restyling/02-VERIFICATION.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (all COMP-01 through COMP-12 and THEME-05 definitions)
    - .planning/phases/02-component-restyling/02-VALIDATION.md (existing test map with per-requirement test files and status)
    - .planning/phases/02-component-restyling/02-01-SUMMARY.md (what Phase 2 Plan 01 did)
    - .planning/phases/02-component-restyling/02-02-SUMMARY.md (what Phase 2 Plan 02 did)
    - .planning/phases/02-component-restyling/02-03-SUMMARY.md (what Phase 2 Plan 03 did)
    - .planning/phases/02-component-restyling/02-04-SUMMARY.md (what Phase 2 Plan 04 did)
    - .planning/phases/02-component-restyling/02-05-SUMMARY.md (what Phase 2 Plan 05 did)
    - .planning/phases/02-component-restyling/02-06-SUMMARY.md (what Phase 2 Plan 06 did)
    - frontend/src/components/SettingsPanel.tsx (verify COMP-05 after Task 1 fix)
    - frontend/src/components/SNMPProfileManager.tsx (verify tokens)
    - frontend/src/components/SSHProfileManager.tsx (verify tokens)
    - frontend/src/components/dashboard/VendorSettingsPanel.tsx (verify tokens)
    - frontend/src/components/AreaManager.tsx (verify tokens)
  </read_first>
  <action>
Create `.planning/phases/02-component-restyling/02-VERIFICATION.md` as a YAML-frontmatter markdown file.

The frontmatter must contain these exact fields:
  phase: 02-component-restyling
  status: verified
  verified_date: "2026-03-27"
  requirement_count: 13
  satisfied_count: 13

The body must contain:

**Title:** `# Phase 2 Verification: Component Restyling`

**Summary section:** State that all 13 Phase 2 requirements are verified as satisfied. Phase 2 restyled all 25+ frontend components to the Neon Topography design language with dual-theme support.

**Requirements Verification section:** For each of the 13 requirements (COMP-01 through COMP-12, THEME-05), create a subsection with:
- Requirement ID and title (copied from REQUIREMENTS.md)
- `Status: Satisfied`
- Evidence: Which plan/summary implemented it, which test file verifies it, and a brief description of the implementation
- Automated test command from 02-VALIDATION.md

Evidence per requirement:

COMP-01 -- Implemented in 02-02 -- Test: DeviceCard.test.tsx -- Glow node, Outfit labels, JetBrains Mono values, no top border, no bottom ports
COMP-02 -- Implemented in 02-03 -- Test: ContextMenu.test.tsx -- Material Symbols icons, separators, glassmorphism, danger styling
COMP-03 -- Implemented in 02-03 -- Test: NavigationPill.nav.test.tsx -- NavBar replaced by NavigationPill with Material Symbols
COMP-04 -- Implemented in 02-04 -- Test: Toolbar.test.tsx -- SidePanel/Toolbar/ZoomControls with surface hierarchy, ghost borders
COMP-05 -- Implemented in 02-05 + 07-01 -- Test: SettingsPanel.test.tsx -- SettingsPanel + SNMPProfileManager + SSHProfileManager standardized inputs, surface tiers; dev badge fixed in Phase 7
COMP-06 -- Implemented in 02-03, 02-04 -- Test: AlertsPanel.test.tsx -- AlertsPanel glow dots, SearchOverlay glassmorphism
COMP-07 -- Implemented in 02-06 -- Test: Dashboard.test.tsx -- Dashboard, DeviceTable, DeviceRow with token colors
COMP-08 -- Implemented in 02-05 -- Test: form-input-audit.test.ts -- AddDevicePanel, DeviceConfigPanel, LinkCreatePanel form standardization
COMP-09 -- Implemented in 02-05 -- Test: font-mono-metrics.test.ts -- LinkDetailsPanel and InterfaceStatsPanel with JetBrains Mono
COMP-10 -- Implemented in 02-02 -- Test: LinkEdge.test.tsx -- LinkEdge label pills with theme-responsive colors
COMP-11 -- Implemented in 02-02, 02-04 -- Test: StatusDot.test.tsx -- Severity-scaled box-shadow glow, animate-pulse on critical
COMP-12 -- Implemented in 02-06 -- Test: no-line-audit.test.ts -- Zero layout border separators, surface tiers for depth
THEME-05 -- Implemented in 02-06 -- Test: theme05-smoke.test.tsx -- All components use token system, zero hardcoded hex

**Automated Verification section:** List all 13 test commands:
  cd frontend && npx vitest run src/components/DeviceCard.test.tsx
  cd frontend && npx vitest run src/components/ContextMenu.test.tsx
  cd frontend && npx vitest run src/components/NavigationPill.nav.test.tsx
  cd frontend && npx vitest run src/components/Toolbar.test.tsx
  cd frontend && npx vitest run src/components/SettingsPanel.test.tsx
  cd frontend && npx vitest run src/components/AlertsPanel.test.tsx
  cd frontend && npx vitest run src/components/Dashboard.test.tsx
  cd frontend && npx vitest run src/components/__tests__/form-input-audit.test.ts
  cd frontend && npx vitest run src/components/__tests__/font-mono-metrics.test.ts
  cd frontend && npx vitest run src/components/LinkEdge.test.tsx
  cd frontend && npx vitest run src/components/StatusDot.test.tsx
  cd frontend && npx vitest run src/components/__tests__/no-line-audit.test.ts
  cd frontend && npx vitest run src/components/__tests__/theme05-smoke.test.tsx

Also include full-suite command: `cd frontend && npx vitest run`

**Cross-Phase Notes section:** Note that COMP-05 required a minor fix in Phase 7 (stale dev badge token) but the component was substantively restyled in Phase 2 Plan 05. VendorSettingsPanel was restyled in Phase 2 Plan 06. AreaManager was created in Phase 3 following the same Neon Topography patterns.

After creating the file, run `cd /home/azmin/projects/theia/frontend && npx vitest run` to confirm the full test suite still passes (verifies no regressions from Task 1).
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia/frontend && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-component-restyling/02-VERIFICATION.md` exists
    - File contains frontmatter with `status: verified` and `satisfied_count: 13`
    - File contains section headers for all 13 requirements: COMP-01, COMP-02, COMP-03, COMP-04, COMP-05, COMP-06, COMP-07, COMP-08, COMP-09, COMP-10, COMP-11, COMP-12, THEME-05
    - File contains `Status: Satisfied` for each requirement (grep returns 13 matches)
    - File contains the full-suite test command `npx vitest run`
    - Full test suite passes (vitest exits 0)
  </acceptance_criteria>
  <done>Phase 2 VERIFICATION.md documents all 13 requirements as satisfied with evidence, test commands, and cross-phase notes; full test suite green</done>
</task>

</tasks>

<verification>
1. `grep -c 'yellow-500\|yellow-400' frontend/src/components/SettingsPanel.tsx` returns 0
2. `grep 'bg-warning' frontend/src/components/SettingsPanel.tsx` returns matches
3. `test -f .planning/phases/02-component-restyling/02-VERIFICATION.md` succeeds
4. `grep -c 'Status: Satisfied' .planning/phases/02-component-restyling/02-VERIFICATION.md` returns 13
5. `cd frontend && npx vitest run` exits 0
</verification>

<success_criteria>
- COMP-05 satisfied: SettingsPanel and all sub-panels use Neon Topography tokens (no stale values)
- Phase 2 VERIFICATION.md exists with all 13 requirements documented as satisfied
- Full frontend test suite passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/07-settings-panel-verification-and-phase-2-closure/07-01-PLAN.md`
</output>
