---
phase: 06-canvas-token-migration-and-theme-compliance
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - frontend/src/components/Canvas.tsx
  - frontend/src/components/canvas/CanvasOverlays.tsx
autonomous: true
requirements: [THEME-05, FOUND-06, COMP-12]

must_haves:
  truths:
    - "Canvas loading spinner, error modal, and retry button use valid theme-adaptive tokens in both themes"
    - "Canvas connection line color adapts to theme via CSS variable"
    - "Canvas Background dot color adapts to theme via CSS variable"
    - "MiniMap fully uses CSS variable statusColor() for all status values (no hex fallback)"
    - "Edit mode banner uses primary tokens instead of stale accent tokens"
    - "Prometheus reconnected toast uses semantic status-up tokens instead of fixed green palette"
    - "Prometheus unreachable toast uses semantic warning tokens instead of fixed yellow palette"
    - "No stale border-border-subtle tokens remain -- replaced with border-outline-subtle (COMP-12)"
  artifacts:
    - path: "frontend/src/components/Canvas.tsx"
      provides: "Canvas component with all valid tokens and no hardcoded hex"
      contains: "bg-bg"
    - path: "frontend/src/components/canvas/CanvasOverlays.tsx"
      provides: "Canvas overlays with semantic status/warning tokens"
      contains: "text-primary"
  key_links:
    - from: "frontend/src/components/Canvas.tsx"
      to: "ReactFlow connectionLineStyle"
      via: "inline style prop"
      pattern: "var\\(--nt-outline\\)"
    - from: "frontend/src/components/Canvas.tsx"
      to: "ReactFlow Background"
      via: "color prop"
      pattern: "var\\(--nt-outline\\)"
    - from: "frontend/src/components/canvas/CanvasOverlays.tsx"
      to: "theme token system"
      via: "Tailwind class names"
      pattern: "text-primary|bg-surface|text-on-bg-secondary"
---

<objective>
Migrate Canvas.tsx (7 stale tokens + 2 hardcoded hex) and CanvasOverlays.tsx (9 stale tokens + 3 fixed palette colors) to valid Tailwind v4 `@theme inline` token names and CSS variable references.

Purpose: Complete the canvas token migration so all canvas-related elements render correctly in both dark and light themes, closing THEME-05, FOUND-06, and COMP-12 for the canvas scope.

Output: 2 migrated component files with zero stale tokens, zero hardcoded hex, and zero fixed palette colors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-canvas-token-migration-and-theme-compliance/06-RESEARCH.md
@.planning/phases/06-canvas-token-migration-and-theme-compliance/06-01-SUMMARY.md

<interfaces>
<!-- Valid token mapping from @theme inline (frontend/src/index.css lines 107-153) -->
<!-- Stale -> Valid class name replacements: -->
<!--   bg-bg-canvas -> bg-bg -->
<!--   bg-bg-surface/XX -> bg-surface/XX -->
<!--   text-text-primary -> text-on-bg -->
<!--   text-text-secondary -> text-on-bg-secondary -->
<!--   border-border-subtle -> border-outline-subtle -->
<!--   text-accent -> text-primary -->
<!--   border-accent/XX -> border-primary/XX -->
<!--   bg-accent/XX -> bg-primary/XX -->
<!--   border-t-accent -> border-t-primary -->

<!-- For ReactFlow inline style props, use raw CSS variables: -->
<!--   '#4a4a5e' -> 'var(--nt-outline)' -->
<!--   '#3f3f53' -> 'var(--nt-outline)' -->

<!-- Fixed palette -> semantic token replacements: -->
<!--   bg-green-400 -> bg-status-up -->
<!--   text-green-300 -> text-status-up -->
<!--   border-green-500/30 -> border-status-up/30 -->
<!--   bg-yellow-400 -> bg-warning -->
<!--   text-yellow-300 -> text-warning -->
<!--   border-yellow-500/30 -> border-warning/30 -->
<!--   text-yellow-400 -> text-warning -->
<!--   hover:text-yellow-300 -> hover:text-warning/80 -->

From frontend/src/components/canvas/canvasHelpers.ts (after Plan 01 migration):
```typescript
export function statusColor(status: Device['status']): string {
  switch (status) {
    case 'up':      return 'var(--color-status-up)';
    case 'down':    return 'var(--color-status-down)';
    case 'probing': return 'var(--color-status-probing)';
    default:        return 'var(--color-status-unknown)';
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Canvas.tsx stale tokens and hardcoded hex values</name>
  <files>frontend/src/components/Canvas.tsx</files>
  <read_first>
    - frontend/src/components/Canvas.tsx (full file -- need to see all stale tokens in loading, error, main return sections)
    - frontend/src/index.css (lines 107-172 for valid token names and ReactFlow CSS variable overrides)
    - .planning/phases/06-canvas-token-migration-and-theme-compliance/06-RESEARCH.md (Canvas.tsx stale token inventory table)
  </read_first>
  <action>
Make these exact replacements in Canvas.tsx. Line numbers are approximate -- match by surrounding context.

**Loading state (around line 246):**
- `bg-bg-canvas` -> `bg-bg`
- `border border-border-subtle bg-bg-surface/85` -> `border border-outline-subtle bg-surface/85`
- `border-2 border-border-subtle border-t-accent` -> `border-2 border-outline-subtle border-t-primary`
- `text-text-secondary` -> `text-on-bg-secondary`

**Error state (around line 257):**
- `bg-bg-canvas` -> `bg-bg`
- `border border-border-subtle bg-bg-surface/85` -> `border border-outline-subtle bg-surface/85`
- `text-text-primary` -> `text-on-bg`
- `text-text-secondary` -> `text-on-bg-secondary`
- `border border-accent/40 bg-accent/10 px-5 py-2 text-sm font-medium text-accent transition-colors duration-150 hover:bg-accent/20` -> `border border-primary/40 bg-primary/10 px-5 py-2 text-sm font-medium text-primary transition-colors duration-150 hover:bg-primary/20`

**Main return wrapper (around line 272):**
- `bg-bg-canvas` -> `bg-bg`

**ReactFlow props (around line 339):**
- `connectionLineStyle={{ stroke: '#4a4a5e', strokeWidth: 2 }}` -> `connectionLineStyle={{ stroke: 'var(--nt-outline)', strokeWidth: 2 }}`
- `className="bg-bg-canvas"` -> `className="bg-bg"`

**Background component (around line 340):**
- `<Background color="#3f3f53"` -> `<Background color="var(--nt-outline)"`

Total: 7 stale class name replacements + 2 hardcoded hex replacements + 1 ReactFlow className fix.
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia/frontend && grep -n 'bg-bg-canvas\|bg-bg-surface\|text-text-primary\|text-text-secondary\|border-border-subtle\|border-t-accent\|text-accent\|border-accent\|bg-accent\|#4a4a5e\|#3f3f53' src/components/Canvas.tsx; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - Canvas.tsx does NOT contain `bg-bg-canvas` (0 matches via grep)
    - Canvas.tsx does NOT contain `bg-bg-surface` (0 matches)
    - Canvas.tsx does NOT contain `text-text-primary` (0 matches)
    - Canvas.tsx does NOT contain `text-text-secondary` (0 matches)
    - Canvas.tsx does NOT contain `border-border-subtle` (0 matches)
    - Canvas.tsx does NOT contain `border-t-accent` (0 matches)
    - Canvas.tsx does NOT contain `text-accent` (0 matches for standalone usage)
    - Canvas.tsx does NOT contain `border-accent` (0 matches)
    - Canvas.tsx does NOT contain `bg-accent` (0 matches)
    - Canvas.tsx does NOT contain `#4a4a5e` (0 matches)
    - Canvas.tsx does NOT contain `#3f3f53` (0 matches)
    - Canvas.tsx DOES contain `bg-bg` (the valid replacement)
    - Canvas.tsx DOES contain `bg-surface/85` (the valid replacement)
    - Canvas.tsx DOES contain `border-outline-subtle` (the valid replacement)
    - Canvas.tsx DOES contain `border-t-primary` (the valid replacement)
    - Canvas.tsx DOES contain `text-on-bg` (the valid replacement)
    - Canvas.tsx DOES contain `text-on-bg-secondary` (the valid replacement)
    - Canvas.tsx DOES contain `border-primary/40` (the valid replacement)
    - Canvas.tsx DOES contain `var(--nt-outline)` in connectionLineStyle and Background color props
    - Existing valid CSS variable references preserved: `var(--nt-on-bg-muted)`, `var(--color-status-down)`, `var(--color-status-probing)` in MiniMap nodeColor
  </acceptance_criteria>
  <done>Canvas.tsx has zero stale token names, zero hardcoded hex values; all replaced with valid @theme inline tokens and CSS variable references</done>
</task>

<task type="auto">
  <name>Task 2: Migrate CanvasOverlays.tsx stale tokens and fixed palette colors</name>
  <files>frontend/src/components/canvas/CanvasOverlays.tsx</files>
  <read_first>
    - frontend/src/components/canvas/CanvasOverlays.tsx (full file -- need to see all 3 overlay sections)
    - frontend/src/index.css (lines 107-153 for valid token names)
    - .planning/phases/06-canvas-token-migration-and-theme-compliance/06-RESEARCH.md (CanvasOverlays.tsx stale token inventory)
  </read_first>
  <action>
Make these exact replacements in CanvasOverlays.tsx:

**Edit mode banner (around lines 28-33):**
- `border-accent/30 bg-bg-surface/95` -> `border-primary/30 bg-surface/95`
- `text-accent` (svg icon, line 29) -> `text-primary`
- `text-accent` (edit mode text, line 32) -> `text-primary`
- `text-text-secondary` (press E hint, line 33) -> `text-on-bg-secondary`

**Prometheus reconnected toast (around lines 37-50):**
- `border-green-500/30 bg-bg-surface/95` -> `border-status-up/30 bg-surface/95`
- `bg-green-400` (dot) -> `bg-status-up`
- `text-green-300` (message text) -> `text-status-up`
- `text-text-secondary hover:text-text-primary` (dismiss button) -> `text-on-bg-secondary hover:text-on-bg`

**Prometheus unreachable toast (around lines 53-77):**
- `border-yellow-500/30 bg-bg-surface/95` -> `border-warning/30 bg-surface/95`
- `bg-yellow-400` (dot) -> `bg-warning`
- `text-yellow-300` (message text) -> `text-warning`
- `text-yellow-400 hover:text-yellow-300` (Details button) -> `text-warning hover:text-warning/80`
- `text-text-secondary hover:text-text-primary` (dismiss button) -> `text-on-bg-secondary hover:text-on-bg`

Total: 9 stale token replacements + 3 fixed-palette-to-semantic replacements.
  </action>
  <verify>
    <automated>cd /home/azmin/projects/theia/frontend && npx vitest run src/components/__tests__/canvas-token-audit.test.ts -x 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - CanvasOverlays.tsx does NOT contain `border-accent` (0 matches)
    - CanvasOverlays.tsx does NOT contain `bg-bg-surface` (0 matches)
    - CanvasOverlays.tsx does NOT contain `text-accent` (0 matches)
    - CanvasOverlays.tsx does NOT contain `text-text-secondary` (0 matches)
    - CanvasOverlays.tsx does NOT contain `text-text-primary` (0 matches)
    - CanvasOverlays.tsx does NOT contain `bg-green-400` (0 matches)
    - CanvasOverlays.tsx does NOT contain `text-green-300` (0 matches)
    - CanvasOverlays.tsx does NOT contain `border-green-500` (0 matches)
    - CanvasOverlays.tsx does NOT contain `bg-yellow-400` (0 matches)
    - CanvasOverlays.tsx does NOT contain `text-yellow-300` (0 matches)
    - CanvasOverlays.tsx does NOT contain `border-yellow-500` (0 matches)
    - CanvasOverlays.tsx does NOT contain `text-yellow-400` (0 matches)
    - CanvasOverlays.tsx DOES contain `border-primary/30 bg-surface/95` (edit mode)
    - CanvasOverlays.tsx DOES contain `text-primary` (edit mode icon + label)
    - CanvasOverlays.tsx DOES contain `text-on-bg-secondary` (hint text + dismiss buttons)
    - CanvasOverlays.tsx DOES contain `border-status-up/30 bg-surface/95` (reconnected toast)
    - CanvasOverlays.tsx DOES contain `bg-status-up` (reconnected dot)
    - CanvasOverlays.tsx DOES contain `text-status-up` (reconnected message)
    - CanvasOverlays.tsx DOES contain `border-warning/30 bg-surface/95` (unreachable toast)
    - CanvasOverlays.tsx DOES contain `bg-warning` (unreachable dot)
    - CanvasOverlays.tsx DOES contain `text-warning` (unreachable message + details button)
    - canvas-token-audit.test.ts passes (all patterns cleared across all 6 files)
    - Full test suite passes: `cd /home/azmin/projects/theia/frontend && npx vitest run -x`
  </acceptance_criteria>
  <done>CanvasOverlays.tsx has zero stale tokens, zero fixed palette colors; all 3 overlay sections use semantic theme-adaptive tokens; full canvas-token-audit test passes across all 6 files</done>
</task>

</tasks>

<verification>
- `cd /home/azmin/projects/theia/frontend && npx vitest run src/components/__tests__/canvas-token-audit.test.ts -x` -- all assertions pass (zero violations)
- `cd /home/azmin/projects/theia/frontend && npx vitest run src/components/__tests__/no-line-audit.test.ts -x` -- still passes (COMP-12)
- `cd /home/azmin/projects/theia/frontend && npx vitest run -x` -- full suite green
- `grep -rn 'bg-bg-canvas\|bg-bg-surface\|text-text-primary\|text-text-secondary\|border-border-subtle\|text-accent\|border-accent\|bg-accent\|border-t-accent' frontend/src/components/Canvas.tsx frontend/src/components/canvas/CanvasOverlays.tsx` -- returns empty
- `grep -rn '#4a4a5e\|#3f3f53' frontend/src/components/Canvas.tsx` -- returns empty
- `grep -rn 'green-300\|green-400\|green-500\|yellow-300\|yellow-400\|yellow-500\|yellow-900' frontend/src/components/canvas/CanvasOverlays.tsx` -- returns empty
</verification>

<success_criteria>
1. Canvas.tsx contains zero stale Tailwind v4 token names and zero hardcoded hex values
2. CanvasOverlays.tsx contains zero stale tokens and zero fixed palette colors
3. Canvas connection line and Background dots use `var(--nt-outline)` for theme-adaptive rendering
4. All 3 overlay toasts (edit mode, reconnected, unreachable) use semantic tokens
5. canvas-token-audit.test.ts passes with zero violations across all 6 target files
6. no-line-audit.test.ts continues to pass (COMP-12 compliant)
7. Full Vitest suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/06-canvas-token-migration-and-theme-compliance/06-02-SUMMARY.md`
</output>
