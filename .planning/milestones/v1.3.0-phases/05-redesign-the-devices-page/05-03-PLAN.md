---
phase: 05-redesign-the-devices-page
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/SidePanel.tsx
  - frontend/src/components/dashboard/SSHCredentialForm.tsx
  - frontend/src/components/dashboard/BackupPanel.tsx
  - frontend/src/components/dashboard/BackupHistoryTable.tsx
  - frontend/src/components/dashboard/ConfigViewer.tsx
  - frontend/src/components/dashboard/BulkBackupPanel.tsx
  - frontend/src/components/dashboard/VendorSettingsPanel.tsx
autonomous: true
requirements: [COMP-08, COMP-09]
must_haves:
  truths:
    - "SidePanel header uses Neon Topography surface tiers with Material Symbols close icon"
    - "All form inputs in sub-panels use consistent styling with no-line rule"
    - "Metric values in sub-panels render in JetBrains Mono (font-mono class)"
    - "No hardcoded hex colors remain in any sub-panel file"
  artifacts:
    - path: "frontend/src/components/SidePanel.tsx"
      provides: "Restyled panel chrome with surface tier header and MaterialIcon close button"
      min_lines: 25
    - path: "frontend/src/components/dashboard/SSHCredentialForm.tsx"
      provides: "Form inputs restyled with Neon Topography tokens"
    - path: "frontend/src/components/dashboard/BackupPanel.tsx"
      provides: "Backup panel with consistent token-based styling"
    - path: "frontend/src/components/dashboard/BackupHistoryTable.tsx"
      provides: "Backup history with font-mono timestamps and file sizes"
    - path: "frontend/src/components/dashboard/ConfigViewer.tsx"
      provides: "Config viewer with font-mono code content"
    - path: "frontend/src/components/dashboard/BulkBackupPanel.tsx"
      provides: "Bulk backup panel with consistent token-based styling"
    - path: "frontend/src/components/dashboard/VendorSettingsPanel.tsx"
      provides: "Vendor settings form with consistent token-based styling"
  key_links:
    - from: "frontend/src/components/SidePanel.tsx"
      to: "frontend/src/components/MaterialIcon.tsx"
      via: "import MaterialIcon for close button"
      pattern: "import.*MaterialIcon"
---

<objective>
Restyle the SidePanel chrome and all dashboard sub-panels (SSH, Backup, BackupHistory, ConfigViewer, BulkBackup, VendorSettings) to match Neon Topography design tokens with consistent form input styling, JetBrains Mono metric values, and no-line rule enforcement.

Purpose: Delivers COMP-08 (form panel restyling) and COMP-09 (metric panel restyling). The SidePanel is the container for all device-action sub-panels, so restyling it plus its children completes the devices page visual overhaul.
Output: All 7 files updated with consistent Neon Topography styling.
</objective>

<execution_context>
@/home/azmin/.claude/get-shit-done/workflows/execute-plan.md
@/home/azmin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-redesign-the-devices-page/05-CONTEXT.md
@.planning/phases/05-redesign-the-devices-page/05-RESEARCH.md

@frontend/src/components/SidePanel.tsx
@frontend/src/components/dashboard/SSHCredentialForm.tsx
@frontend/src/components/dashboard/BackupPanel.tsx
@frontend/src/components/dashboard/BackupHistoryTable.tsx
@frontend/src/components/dashboard/ConfigViewer.tsx
@frontend/src/components/dashboard/BulkBackupPanel.tsx
@frontend/src/components/dashboard/VendorSettingsPanel.tsx

<interfaces>
<!-- Key types and contracts -->

From frontend/src/components/MaterialIcon.tsx:
```typescript
export function MaterialIcon({ name, className, size }: MaterialIconProps): JSX.Element;
```

Standardized input classes already in SSHCredentialForm.tsx:
```typescript
const inputClass =
  'w-full rounded-md border border-outline-subtle bg-elevated px-3 py-2 text-sm text-on-bg placeholder-on-bg-muted outline-none focus:border-primary focus:ring-1 focus:ring-primary/30';
const selectClass =
  'w-full rounded-md border border-outline-subtle bg-elevated px-3 py-2 text-sm text-on-bg outline-none focus:border-primary focus:ring-1 focus:ring-primary/30';
```

Current SidePanel structure:
```typescript
export function SidePanel({ open, onClose, title, children }: SidePanelProps) {
  // Fixed right panel, w-[320px], z-40, bg-surface, shadow-panel
  // Header: bg-surface-high, MaterialIcon close button
  // Body: overflow-y-auto p-4
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle SidePanel chrome</name>
  <files>frontend/src/components/SidePanel.tsx</files>
  <read_first>
    - frontend/src/components/SidePanel.tsx (full file -- current panel chrome)
    - frontend/src/components/MaterialIcon.tsx (MaterialIcon component)
    - .planning/phases/05-redesign-the-devices-page/05-CONTEXT.md (D-18, D-19)
  </read_first>
  <action>
Update SidePanel.tsx header and close button styling per D-18. The SidePanel already has reasonable styling from Phase 2 (bg-surface, shadow-panel, MaterialIcon close). Make these targeted refinements:

1. **Header section** -- update from `bg-surface-high` to a cleaner hierarchy:
   ```jsx
   <div className="flex items-center justify-between px-4 py-3 bg-surface-high/80 transition-colors duration-200">
     <h2 className="text-sm font-semibold text-on-bg tracking-wide">{title}</h2>
     <button
       onClick={onClose}
       className="p-1 text-on-bg-secondary hover:text-on-bg hover:bg-elevated rounded-md transition-colors"
       title="Close"
     >
       <MaterialIcon name="close" size={18} />
     </button>
   </div>
   ```
   Key changes: `text-lg` -> `text-sm` with `tracking-wide` for a tighter professional look, `py-4` -> `py-3` for less vertical padding, add `transition-colors duration-200` to the header div, reduce close icon from `size={20}` to `size={18}`.

2. **Body section** -- add transition:
   ```jsx
   <div className="flex-1 overflow-y-auto p-4 transition-colors duration-200">
     {children}
   </div>
   ```

3. **Outer container** -- ensure clean class string (no duplicate `transition-colors`). The outer div should have:
   ```
   className={`fixed top-0 right-0 h-full w-[320px] z-40 transform transition-transform duration-200 ease-in-out bg-surface flex flex-col shadow-panel ${open ? 'translate-x-0' : 'translate-x-full'}`}
   ```
   Note: remove the duplicate `transition-colors` that currently exists in the class string.

D-19 is already satisfied: panel is `fixed` overlay, no push layout.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SidePanel.tsx header h2 has `text-sm font-semibold` (not text-lg)
    - SidePanel.tsx header div has `py-3` (not py-4)
    - SidePanel.tsx header div contains `transition-colors duration-200`
    - SidePanel.tsx close button uses `MaterialIcon name="close" size={18}`
    - SidePanel.tsx outer container does NOT have duplicate `transition-colors` strings
    - SidePanel.tsx body div contains `transition-colors duration-200`
    - All existing tests pass
  </acceptance_criteria>
  <done>SidePanel chrome restyled with tighter header, consistent transitions, and proper Material Symbols close button</done>
</task>

<task type="auto">
  <name>Task 2: Restyle sub-panel forms and metric displays</name>
  <files>frontend/src/components/dashboard/SSHCredentialForm.tsx, frontend/src/components/dashboard/BackupPanel.tsx, frontend/src/components/dashboard/BackupHistoryTable.tsx, frontend/src/components/dashboard/ConfigViewer.tsx, frontend/src/components/dashboard/BulkBackupPanel.tsx, frontend/src/components/dashboard/VendorSettingsPanel.tsx</files>
  <read_first>
    - frontend/src/components/dashboard/SSHCredentialForm.tsx (full file -- standardized input classes already defined)
    - frontend/src/components/dashboard/BackupPanel.tsx (full file -- backup trigger UI; check whether outermost element is a React Fragment or a container div, needed for transition-colors placement)
    - frontend/src/components/dashboard/BackupHistoryTable.tsx (full file -- history table with status colors)
    - frontend/src/components/dashboard/ConfigViewer.tsx (full file -- config viewer with tabs)
    - frontend/src/components/dashboard/BulkBackupPanel.tsx (full file -- bulk backup progress; check whether outermost element is a React Fragment or a container div, needed for transition-colors placement)
    - frontend/src/components/dashboard/VendorSettingsPanel.tsx (full file -- vendor settings form)
    - .planning/phases/05-redesign-the-devices-page/05-CONTEXT.md (D-18 SidePanel sub-panels)
  </read_first>
  <action>
Apply consistent Neon Topography styling across all 6 sub-panel files. These files already use token-based classes from Phase 2, so changes are refinements, not rewrites. Focus on:

**A. Standardized form input classes (ensure consistency across all files):**

The inputClass and selectClass in SSHCredentialForm.tsx are the reference. Ensure all files that have form inputs use the same pattern:
```typescript
const inputClass = 'w-full rounded-md border border-outline-subtle bg-elevated px-3 py-2 text-sm text-on-bg placeholder-on-bg-muted outline-none focus:border-primary focus:ring-1 focus:ring-primary/30 transition-colors';
const selectClass = 'w-full rounded-md border border-outline-subtle bg-elevated px-3 py-2 text-sm text-on-bg outline-none focus:border-primary focus:ring-1 focus:ring-primary/30 transition-colors';
```
Add `transition-colors` to the end of both classes if not already present.

**B. JetBrains Mono for metric/technical values (COMP-09):**

In each file, ensure technical readout values use `font-mono` class:

1. **BackupHistoryTable.tsx:**
   - Timestamps (created_at) should use `font-mono text-[11px]`
   - File sizes (size_bytes) should use `font-mono text-[11px]`
   - File names should use `font-mono text-[11px]`
   - Status text already uses token-based status colors -- keep those

2. **ConfigViewer.tsx:**
   - The config content `<pre>` block should use `font-mono text-xs` (it likely already does via `<pre>`)
   - Tab labels can stay in `font-sans`
   - File hash/name values should use `font-mono text-[11px]`

3. **BackupPanel.tsx:**
   - Latest backup timestamp should use `font-mono text-[11px]`
   - Job status text should use token-based status colors

4. **BulkBackupPanel.tsx:**
   - Device names in progress list should stay font-sans
   - Status text should use token-based status colors matching BackupHistoryTable pattern

5. **VendorSettingsPanel.tsx:**
   - PromQL query values in inputs should use `font-mono` on the input elements
   - SNMP OID values should use `font-mono` on the input elements
   - Apply `font-mono` to inputs that contain technical values: add `font-mono` to the class of promQL and OID input fields

6. **SSHCredentialForm.tsx:**
   - Port number input should use `font-mono`
   - Password fields stay font-sans (they're masked)

**C. No-line rule enforcement:**

Scan each file for any `border-b`, `border-t`, or `divide-y` classes used as section separators. Replace with surface tier depth changes:
- Replace `border-b border-outline` with `bg-surface-high` on section headers
- Replace `divide-y` on lists with alternating `[&:nth-child(even)]:bg-surface-high/30`

If a file already has no border separators, leave it as-is.

**D. Section headers in sub-panels:**

Where sub-panels have section labels (e.g., "Create New Profile" in SSHCredentialForm, "Prometheus Queries" in VendorSettings), ensure they use:
```
className="text-xs font-medium text-on-bg-secondary uppercase tracking-[0.12em] mb-2"
```

**E. Button styling consistency:**

Primary action buttons (Save, Test, Trigger Backup) should use:
```
className="rounded-md bg-primary px-3 py-2 text-sm font-medium text-on-primary hover:bg-primary/90 transition-colors disabled:opacity-50"
```

Secondary/cancel buttons should use:
```
className="rounded-md bg-surface-high px-3 py-2 text-sm text-on-bg-secondary hover:text-on-bg hover:bg-elevated transition-colors"
```

Danger buttons (delete) should use:
```
className="rounded-md bg-critical/10 px-3 py-2 text-sm text-critical hover:bg-critical/20 transition-colors"
```

**F. Add `transition-colors duration-200` to outermost container div** in each sub-panel if not already present, ensuring smooth theme switching. IMPORTANT: If BackupPanel.tsx or BulkBackupPanel.tsx uses a React Fragment (`<>...</>`) as the outermost element instead of a `<div>`, wrap the content in a `<div className="transition-colors duration-200">` or add the class to the first meaningful container div inside the Fragment.

NOTE: Do NOT rewrite entire files. Make targeted class string updates. Preserve all existing logic, state management, and API calls unchanged. Only modify className strings and add font-mono where specified.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SSHCredentialForm.tsx inputClass contains `transition-colors`
    - SSHCredentialForm.tsx port input element has `font-mono` in its className
    - BackupHistoryTable.tsx timestamp cells have `font-mono` class
    - BackupHistoryTable.tsx file size cells have `font-mono` class
    - ConfigViewer.tsx config content element has `font-mono` class
    - VendorSettingsPanel.tsx PromQL input fields have `font-mono` class
    - VendorSettingsPanel.tsx SNMP OID input fields have `font-mono` class
    - BulkBackupPanel.tsx outermost div has `transition-colors`
    - BackupPanel.tsx outermost div has `transition-colors`
    - No file in dashboard/ directory contains `border-b border-outline` as section separator (grep returns empty)
    - All existing tests pass (including ConfigViewer.test.tsx)
  </acceptance_criteria>
  <done>All 6 sub-panels have consistent Neon Topography form styling, JetBrains Mono for technical values, transition-colors for theme switching, and no border separators</done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run --reporter=verbose` -- all tests pass
- `grep -r "font-mono" frontend/src/components/dashboard/BackupHistoryTable.tsx` returns matches (COMP-09)
- `grep -r "font-mono" frontend/src/components/dashboard/ConfigViewer.tsx` returns matches (COMP-09)
- `grep -r "font-mono" frontend/src/components/dashboard/VendorSettingsPanel.tsx` returns matches for PromQL/OID inputs
- `grep -rn "border-b border-outline" frontend/src/components/dashboard/` returns NO matches (no-line rule)
- `grep "transition-colors" frontend/src/components/SidePanel.tsx` returns matches
</verification>

<success_criteria>
1. SidePanel header is tighter (text-sm, py-3) with transition-colors
2. All form inputs across sub-panels use consistent inputClass/selectClass patterns
3. Technical values (timestamps, file sizes, OIDs, PromQL, config content) render in JetBrains Mono (font-mono class)
4. No border separator classes remain in any sub-panel
5. All sub-panels have transition-colors on their outermost container
6. Primary/secondary/danger buttons use consistent styling patterns
7. All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-redesign-the-devices-page/05-03-SUMMARY.md`
</output>
