/**
 * Installs shared test-environment shims used by Vitest and React Testing Library.
 * Keeps browser API mocks consistent across frontend unit tests.
 */
import '@testing-library/jest-dom/vitest';
