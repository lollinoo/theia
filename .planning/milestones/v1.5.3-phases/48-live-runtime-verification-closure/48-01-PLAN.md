---
phase: 48-live-runtime-verification-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md
  - .planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "Phase 42 human UAT records outcomes for live cutover, topology ordering, and mixed-cadence behavior with no `pending` results left."
    - "Phase 45 has a persisted `45-HUMAN-UAT.md` artifact recording the live canvas/websocket cadence smoke test outcome."
    - "Both runtime-proof artifacts preserve the established HUMAN-UAT structure and include concrete device/override notes from the live session."
  artifacts:
    - path: ".planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md"
      provides: "Persisted live cutover, topology ordering, and mixed-cadence runtime evidence"
      contains: "### 3. Classified Scheduling Smoke Test"
    - path: ".planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md"
      provides: "Persisted live cadence/websocket smoke evidence"
      contains: "### 1. Live Canvas Cadence + Mixed-Tier Stability"
  key_links:
    - from: ".planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md"
      to: ".planning/phases/42-pipeline-orchestrator-cutover/42-VERIFICATION.md"
      via: "The artifact mirrors the exact three human-only runtime checks that Phase 42 verification left pending."
      pattern: "Live Cutover Smoke Test|Topology Ordering Check|Classified Scheduling Smoke Test"
    - from: ".planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md"
      to: ".planning/phases/45-polling-cadence-gap-closure/45-VERIFICATION.md"
      via: "The new artifact mirrors the exact live cadence/websocket smoke test required by Phase 45 verification."
      pattern: "Live Canvas Cadence \\+ Mixed-Tier Stability|poll_interval_override"
    - from: ".planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md"
      to: ".planning/phases/44-frontend-integration/44-HUMAN-UAT.md"
      via: "The new Phase 45 artifact must match the established HUMAN-UAT frontmatter, section ordering, and summary-count format."
      pattern: "## Current Test|## Tests|## Summary|pending: 0"
---

<objective>
Close the remaining live browser/runtime proof debt for Phase 42 and Phase 45
by staging the exact HUMAN-UAT artifacts, running the missing live checks, and
persisting the observed outcomes with no `pending` entries left.

Purpose:
- Clear the milestone's open live-runtime debt for cutover, topology ordering,
  cadence ownership, and websocket/browser smoke behavior.
- Keep the work on `.planning` artifacts only; no product/code changes belong in
  this phase unless the live check reveals a real regression.
- Give the executor a concrete, reproducible live-check flow instead of vague
  follow-up notes.

Output:
- `.planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md` updated
  with final outcomes for all three Phase 42 live checks.
- `.planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md` created and
  finalized with the live canvas/websocket cadence smoke result.
</objective>

<execution_context>
@/home/azmin/.codex/get-shit-done/workflows/execute-plan.md
@/home/azmin/.codex/get-shit-done/templates/summary.md
@/home/azmin/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.5.3-MILESTONE-AUDIT.md
@.planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md
@.planning/phases/42-pipeline-orchestrator-cutover/42-VERIFICATION.md
@.planning/phases/45-polling-cadence-gap-closure/45-VERIFICATION.md
@.planning/phases/44-frontend-integration/44-HUMAN-UAT.md
@.planning/phases/46-detail-delta-gap-closure/46-HUMAN-UAT.md
@internal/api/device_handler.go

<interfaces>
Canonical HUMAN-UAT artifact shape to mirror from completed phases:
```md
[frontmatter]
status: partial
phase: 44-frontend-integration
source: [44-VERIFICATION.md]
started: 2026-04-13T17:50:38Z
updated: 2026-04-13T18:51:34Z

[body]
## Current Test
...

## Tests
### 1. ...
expected: ...
result: ...

## Summary
total: N
passed: N
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps
None.
```

Ground-truth live checks to close:
```md
Phase 42:
1. Live Cutover Smoke Test
2. Topology Ordering Check
3. Classified Scheduling Smoke Test

Phase 45:
1. Live Canvas Cadence + Mixed-Tier Stability
```

Runtime seam available for the topology-ordering check:
```http
POST /api/v1/devices/{id}/probe
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage the Phase 42 and Phase 45 HUMAN-UAT artifacts and ready the live runtime</name>
  <files>
    .planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md
    .planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md
  </files>
  <read_first>
    - .planning/v1.5.3-MILESTONE-AUDIT.md
    - .planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md
    - .planning/phases/42-pipeline-orchestrator-cutover/42-VERIFICATION.md
    - .planning/phases/45-polling-cadence-gap-closure/45-VERIFICATION.md
    - .planning/phases/44-frontend-integration/44-HUMAN-UAT.md
    - .planning/phases/46-detail-delta-gap-closure/46-HUMAN-UAT.md
    - SETUP.md
    - Makefile
    - internal/api/device_handler.go
    - docker-compose.yml
  </read_first>
  <action>
    Update the Phase 42 and Phase 45 HUMAN-UAT artifacts into a checkpoint-ready
    state before any live observation begins.

    1. Rewrite `.planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md`
       so it keeps the existing frontmatter and section order but uses exact
       checkpoint-ready wording:
       - keep the three existing test headings exactly:
         - `### 1. Live Cutover Smoke Test`
         - `### 2. Topology Ordering Check`
         - `### 3. Classified Scheduling Smoke Test`
       - keep each `expected:` line aligned to `42-VERIFICATION.md`
       - replace each `result:` line with:
         `pending — execute the Phase 48 live checkpoint and replace this with passed/failed plus device and browser notes`
       - update `## Current Test` so it says the Phase 48 live runtime
         verification session is prepared for cutover, topology ordering, and
         mixed-cadence closure
       - keep `total: 3`, `passed: 0`, `issues: 0`, `pending: 3`, `skipped: 0`,
         `blocked: 0`

    2. Create `.planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md`
       from scratch using the same frontmatter/section shape as the completed
       Phase 44 and Phase 46 HUMAN-UAT artifacts:
       - frontmatter:
         - `status: partial`
         - `phase: 45-polling-cadence-gap-closure`
         - `source: [45-VERIFICATION.md]`
         - `started:` and `updated:` timestamps set during execution
       - `## Current Test` should state that the Phase 48 live canvas/websocket
         cadence checkpoint is prepared and awaiting explicit runtime results
       - create exactly one test heading:
         `### 1. Live Canvas Cadence + Mixed-Tier Stability`
       - set `expected:` to the exact live requirement from `45-VERIFICATION.md`
       - set `result:` to:
         `pending — execute the Phase 48 live checkpoint and replace this with passed/failed plus device, panel, and override notes`
       - set summary counts to `total: 1`, `passed: 0`, `issues: 0`,
         `pending: 1`, `skipped: 0`, `blocked: 0`
       - set `## Gaps` to a short note that the live checkpoint is the only
         remaining step

    3. Reset to a clean environment before startup so the live proof is
       reproducible:
       - run `rtk make clean`
       - this removes the persistent `theia-data` volume so old seeded devices
         and topology state cannot leak into the checkpoint

    4. Bring the local runtime up for the checkpoint using the repo-supported
       dev entrypoint:
       - run `rtk make dev`
       - confirm the stack is running with `rtk docker compose --profile dev ps`
       - wait for backend/frontend readiness with a bounded health loop:
         `rtk bash -lc 'for i in $(seq 1 30); do curl -sf http://localhost:8080/api/v1/health >/dev/null && curl -I -sf http://localhost:3000 >/dev/null && exit 0; sleep 2; done; exit 1'`
       - capture the backend startup window immediately after the clean boot:
         `rtk bash -lc 'docker compose --profile dev logs backend > /tmp/phase48-backend-startup.log && ! rg -n "Poller started|Metrics collector started" /tmp/phase48-backend-startup.log'`
       - do not rely on bare `docker compose up -d`; this repo's browser stack
         is profile-gated behind the `dev` profile

    5. Do not invent any pass/fail outcomes in this task. This task only stages
       the artifacts and verification environment.
  </action>
  <verify>
    <automated>rtk make clean && rtk make dev && rtk docker compose --profile dev ps && rtk bash -lc 'for i in $(seq 1 30); do curl -sf http://localhost:8080/api/v1/health >/dev/null && curl -I -sf http://localhost:3000 >/dev/null && exit 0; sleep 2; done; exit 1' && rtk bash -lc 'docker compose --profile dev logs backend > /tmp/phase48-backend-startup.log && ! rg -n "Poller started|Metrics collector started" /tmp/phase48-backend-startup.log'</automated>
  </verify>
  <acceptance_criteria>
    - `rtk bash -lc 'test -f .planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md'` exits 0
    - `rtk bash -lc 'test -f .planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md'` exits 0
    - `rtk rg -n '### 1\\. Live Cutover Smoke Test|### 2\\. Topology Ordering Check|### 3\\. Classified Scheduling Smoke Test' .planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md` returns 3 matches
    - `rtk rg -n '### 1\\. Live Canvas Cadence \\+ Mixed-Tier Stability' .planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md` returns 1 match
    - `rtk rg -n 'pending: 3' .planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md` returns 1 match
    - `rtk rg -n 'pending: 1' .planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md` returns 1 match
    - `rtk make clean` exits 0 before startup
    - `rtk docker compose --profile dev ps` exits 0 after `rtk make dev`
    - `rtk bash -lc 'for i in $(seq 1 30); do curl -sf http://localhost:8080/api/v1/health >/dev/null && curl -I -sf http://localhost:3000 >/dev/null && exit 0; sleep 2; done; exit 1'` exits 0 after `rtk make dev`
    - `rtk bash -lc 'test -f /tmp/phase48-backend-startup.log && ! rg -n "Poller started|Metrics collector started" /tmp/phase48-backend-startup.log'` exits 0 after `rtk make dev`
    - `rtk make clean && rtk make dev && rtk docker compose --profile dev ps && rtk bash -lc 'for i in $(seq 1 30); do curl -sf http://localhost:8080/api/v1/health >/dev/null && curl -I -sf http://localhost:3000 >/dev/null && exit 0; sleep 2; done; exit 1' && rtk bash -lc 'docker compose --profile dev logs backend > /tmp/phase48-backend-startup.log && ! rg -n "Poller started|Metrics collector started" /tmp/phase48-backend-startup.log'` exits 0
  </acceptance_criteria>
  <done>
    The Phase 42 and Phase 45 HUMAN-UAT files are staged in the correct format,
    and the clean profile-gated local runtime is up for the live checkpoint.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the missing live cutover, topology, and cadence checks in the browser</name>
  <files>
    .planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md
    .planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md
  </files>
  <read_first>
    - .planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md
    - .planning/phases/42-pipeline-orchestrator-cutover/42-VERIFICATION.md
    - .planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md
    - .planning/phases/45-polling-cadence-gap-closure/45-VERIFICATION.md
    - .planning/phases/44-frontend-integration/44-HUMAN-UAT.md
    - .planning/phases/46-detail-delta-gap-closure/46-HUMAN-UAT.md
    - SETUP.md
    - Makefile
    - scripts/seed.sh
    - internal/api/device_handler.go
  </read_first>
  <action>
    Use the running local stack from Task 1 and perform the exact live checks
    below. Record the result of every check as pass/fail plus a short note with
    the device ID or hostname involved, and the override seconds used for the
    cadence check. This checkpoint owns the clean reseed event that creates the
    topology-changing condition for the Phase 42 ordering proof.
  </action>
  <verify>
    <automated>rtk docker compose --profile dev ps && rtk curl -sf http://localhost:8080/api/v1/health >/dev/null && rtk curl -I -sf http://localhost:3000 >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `rtk docker compose --profile dev ps` exits 0
    - `rtk curl -sf http://localhost:8080/api/v1/health >/dev/null` exits 0
    - `rtk curl -I -sf http://localhost:3000 >/dev/null` exits 0
    - `rtk bash -lc 'json=$(curl -sf http://localhost:8080/api/v1/devices); printf "%s" "$json" | rg -q "172\\.28\\.10\\.10" && printf "%s" "$json" | rg -q "172\\.28\\.10\\.11" && printf "%s" "$json" | rg -q "172\\.28\\.10\\.12"'` exits 0 after the checkpoint seed step
  </acceptance_criteria>
  <what-built>Checkpoint-ready Phase 42 and Phase 45 HUMAN-UAT artifacts plus a running clean local Theia stack at `http://localhost:3000` and `http://localhost:8080`.</what-built>
  <how-to-verify>
    1. Open `http://localhost:3000` with browser devtools available.

    2. With the browser already connected, trigger the clean simulator inventory
       load that both populates the lab and creates the first topology-changing
       event:
       - in another terminal, run `rtk make seed`
       - verify that all three simulator devices now exist with:
         `rtk bash -lc 'json=$(curl -sf http://localhost:8080/api/v1/devices); printf "%s" "$json" | rg -q "172\\.28\\.10\\.10" && printf "%s" "$json" | rg -q "172\\.28\\.10\\.11" && printf "%s" "$json" | rg -q "172\\.28\\.10\\.12"'`
       - keep the overview client connected while this clean reseed runs

    3. Phase 42 — Live Cutover Smoke Test:
       - connect to the normal overview canvas
       - inspect the websocket session for `/api/v1/ws` if helpful
       - confirm the first payload is an initial `snapshot`
       - confirm later websocket traffic includes `snapshot_delta`
       - inspect the startup log capture from Task 1 with:
         `rtk bash -lc '! rg -n "Poller started|Metrics collector started" /tmp/phase48-backend-startup.log'`
       - confirm the captured clean-start logs do not show legacy runtime
         ownership starting while the live snapshot/delta flow is working
       - confirm live metrics continue updating on the canvas while the app is running

    4. Phase 42 — Topology Ordering Check:
       - use the clean reseed from step 2 as the topology-changing event to
         observe; do not rely on an arbitrary reprobe that may not change
         topology
       - keep browser devtools or websocket inspection open from before the
         seed begins
       - watch the first discovery-driven topology population on the clean DB
       - confirm refreshed snapshot data is visible before any
         `topology_changed` effect is reflected in the UI and that no stale-map
         or split-brain behavior appears
       - if you miss the initial topology-changing event, rerun the same clean
         sequence (`rtk make clean`, `rtk make dev`, reconnect browser, then
         `rtk make seed`) and recheck rather than reprobeing an arbitrary
         already-discovered device

    5. Phase 42 — Classified Scheduling Smoke Test:
       - leave the app running long enough to observe mixed cadence behavior
       - confirm devices continue to update on their effective schedules and
         last-known performance data remains visible until staleness instead of
         flickering empty on transient misses

    6. Phase 45 — Live Canvas Cadence + Mixed-Tier Stability:
       - open a managed device on the canvas
       - note the current effective cadence from the visible `Polling every ...`
         label before changing anything
       - choose a new `poll_interval_override` value different from that current
         effective cadence:
         - if the device is currently `30s`, change it to `15s`
         - if the device is currently `15s` or `60s`, change it to `30s`
         - if the device is any other cadence, choose either `15s` or `30s`
           so long as it is different from the current effective cadence
       - confirm the visible `Polling every ...` label updates immediately
         without a page refresh
       - keep the card/detail panel open through the next performance poll and a
         later operational/static poll
       - confirm the displayed freshness timestamp and cadence remain
         performance-owned and are not replaced with `60s` or `300s`
       - record both the before and after cadence values in the human note

    7. Capture the exact pass/fail outcomes and the concrete device notes needed
       for Task 3.
  </how-to-verify>
  <resume-signal>Type `approved` plus five lines: `42-1: pass/fail — note`, `42-2: pass/fail — note`, `42-3: pass/fail — note`, `45-1: pass/fail — note`, and `devices/override: device=..., before=..., after=...`.</resume-signal>
  <done>
    Human runtime observations have been collected for the four missing live
    checks and are ready to be persisted in the HUMAN-UAT artifacts.
  </done>
</task>

<task type="auto">
  <name>Task 3: Persist the live outcomes into the Phase 42 and Phase 45 HUMAN-UAT artifacts</name>
  <files>
    .planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md
    .planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md
  </files>
  <read_first>
    - .planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md
    - .planning/phases/42-pipeline-orchestrator-cutover/42-VERIFICATION.md
    - .planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md
    - .planning/phases/45-polling-cadence-gap-closure/45-VERIFICATION.md
    - .planning/phases/44-frontend-integration/44-HUMAN-UAT.md
    - .planning/phases/46-detail-delta-gap-closure/46-HUMAN-UAT.md
  </read_first>
  <action>
    Use the human checkpoint response exactly. Do not invent results or smooth
    over failures.

    1. Replace every pending `result:` line in the Phase 42 and Phase 45 files
       with the observed pass/fail outcome and a concise note that includes the
       device hostname/ID, websocket or UI behavior observed, and the override
       seconds used where relevant. The Phase 42 `42-1` note must also record
       the startup-log observation from Task 2, explicitly stating whether
       `/tmp/phase48-backend-startup.log` did or did not show `Poller started`
       or `Metrics collector started` while the pipeline-driven snapshot/delta
       flow was active.

    2. For Phase 42, compute summary counts from the three recorded results:
       - `passed` equals the number of `result: passed — ...` lines
       - `issues` equals the number of `result: failed — ...` lines
       - `pending` stays `0`
       - `skipped` stays `0`
       - `blocked` stays `0`

    3. If all Phase 42 checks passed:
       - update `## Current Test` to approved wording matching the style of
         Phase 44 and Phase 46, explicitly naming live pipeline cutover,
         topology ordering, and mixed-cadence verification
       - set summary counts to `total: 3`, `passed: 3`, `issues: 0`,
         `pending: 0`, `skipped: 0`, `blocked: 0`
       - set `## Gaps` to `None.`

    4. If any Phase 42 check failed:
       - keep `## Current Test` factual instead of approved
       - set summary counts from the real pass/fail totals from step 2
       - replace `## Gaps` with a concise list of the concrete failed live
         behaviors

    5. For Phase 45, compute summary counts from the single recorded result:
       - if the result is passed, set `passed: 1` and `issues: 0`
       - if the result is failed, set `passed: 0` and `issues: 1`
       - keep `pending: 0`, `skipped: 0`, and `blocked: 0`

    6. If the Phase 45 check passed:
       - update `## Current Test` to approved wording matching the Phase 44/46
         style
       - set summary counts to `total: 1`, `passed: 1`, `issues: 0`,
         `pending: 0`, `skipped: 0`, `blocked: 0`
       - set `## Gaps` to `None.`

    7. If the Phase 45 check failed:
       - keep `## Current Test` factual instead of approved
       - set summary counts from the real pass/fail total from step 5
       - replace `## Gaps` with the concrete failed cadence/runtime behavior

    8. Record failures faithfully with `result: failed — ...`; do not smooth
       over them, and do not force `## Gaps` to `None.` on any file that has a
       failed live check.

    9. Update each file's `updated:` timestamp during execution.
  </action>
  <verify>
    <automated>rtk bash -lc 'test -f .planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md' && ! rtk rg -n 'result: pending|pending: [1-9]' .planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md .planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md && rtk node -e "const fs=require('fs'); function check(path,total){ const c=fs.readFileSync(path,'utf8'); const results=[...c.matchAll(/^result: (passed|failed) — /gm)].map(m=>m[1]); if(results.length!==total) throw new Error(path+' result count'); const passed=Number((c.match(/^passed: (\\d+)$/m)||[])[1]); const issues=Number((c.match(/^issues: (\\d+)$/m)||[])[1]); const pending=Number((c.match(/^pending: (\\d+)$/m)||[])[1]); if(pending!==0) throw new Error(path+' pending'); if(passed!==results.filter(r=>r==='passed').length) throw new Error(path+' passed mismatch'); if(issues!==results.filter(r=>r==='failed').length) throw new Error(path+' issues mismatch'); const gapsNone=/## Gaps\\s*\\n\\s*None\\./m.test(c); if(results.includes('failed') && gapsNone) throw new Error(path+' gaps none on fail'); if(!results.includes('failed') && !gapsNone) throw new Error(path+' missing None'); } check('.planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md',3); check('.planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md',1);"</automated>
  </verify>
  <acceptance_criteria>
    - `rtk bash -lc 'test -f .planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md'` exits 0
    - `! rtk rg -n 'result: pending|pending: [1-9]' .planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md .planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md`
    - `rtk rg -n '^result: (passed|failed) — ' .planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md .planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md` returns 4 matches
    - `rtk rg -n 'Poller started|Metrics collector started' .planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md` returns at least 1 match
    - `rtk rg -n 'before=.*after=' .planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md` returns at least 1 match
    - `rtk node -e "const fs=require('fs'); function check(path,total){ const c=fs.readFileSync(path,'utf8'); const results=[...c.matchAll(/^result: (passed|failed) — /gm)].map(m=>m[1]); if(results.length!==total) throw new Error(path+' result count'); const passed=Number((c.match(/^passed: (\\d+)$/m)||[])[1]); const issues=Number((c.match(/^issues: (\\d+)$/m)||[])[1]); const pending=Number((c.match(/^pending: (\\d+)$/m)||[])[1]); if(pending!==0) throw new Error(path+' pending'); if(passed!==results.filter(r=>r==='passed').length) throw new Error(path+' passed mismatch'); if(issues!==results.filter(r=>r==='failed').length) throw new Error(path+' issues mismatch'); const gapsNone=/## Gaps\\s*\\n\\s*None\\./m.test(c); if(results.includes('failed') && gapsNone) throw new Error(path+' gaps none on fail'); if(!results.includes('failed') && !gapsNone) throw new Error(path+' missing None'); } check('.planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md',3); check('.planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md',1);"` exits 0
  </acceptance_criteria>
  <done>
    Phase 42 and Phase 45 now have persisted live-runtime artifacts with no
    pending entries and honest pass/fail evidence, ready for the audit refresh
    that follows in Plan 48-02.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| running app/browser -> HUMAN-UAT markdown | Live websocket and canvas observations are transcribed into milestone proof artifacts |
| human checkpoint response -> persisted result text | Human notes become the final audit-facing record for Phase 42 and Phase 45 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01-01 | R | `42-HUMAN-UAT.md`, `45-HUMAN-UAT.md` | mitigate | Require the checkpoint response to include per-test pass/fail plus device IDs/hostnames and override seconds, then copy those concrete notes into the final artifact. |
| T-48-01-02 | T | HUMAN-UAT summary counts | mitigate | Task 3 acceptance criteria require no `result: pending`, summary counts derived from the persisted pass/fail lines, and `## Gaps` cleared to `None.` only on the all-pass branch. |
| T-48-01-03 | I | Live verification notes | mitigate | Record only device IDs/hostnames, override values, and observable UI/websocket behavior; do not paste credentials, auth headers, or unrelated log payloads into the markdown artifacts. |
</threat_model>

<verification>
- `rtk bash -lc 'test -f .planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md'`
- `! rtk rg -n 'result: pending|pending: [1-9]' .planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md .planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md`
- `rtk node -e "const fs=require('fs'); function check(path,total){ const c=fs.readFileSync(path,'utf8'); const results=[...c.matchAll(/^result: (passed|failed) — /gm)].map(m=>m[1]); if(results.length!==total) throw new Error(path+' result count'); const passed=Number((c.match(/^passed: (\\d+)$/m)||[])[1]); const issues=Number((c.match(/^issues: (\\d+)$/m)||[])[1]); const pending=Number((c.match(/^pending: (\\d+)$/m)||[])[1]); if(pending!==0) throw new Error(path+' pending'); if(passed!==results.filter(r=>r==='passed').length) throw new Error(path+' passed mismatch'); if(issues!==results.filter(r=>r==='failed').length) throw new Error(path+' issues mismatch'); const gapsNone=/## Gaps\\s*\\n\\s*None\\./m.test(c); if(results.includes('failed') && gapsNone) throw new Error(path+' gaps none on fail'); if(!results.includes('failed') && !gapsNone) throw new Error(path+' missing None'); } check('.planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md',3); check('.planning/phases/45-polling-cadence-gap-closure/45-HUMAN-UAT.md',1);"` 
- `rtk rg -n 'Poller started|Metrics collector started' .planning/phases/42-pipeline-orchestrator-cutover/42-HUMAN-UAT.md`
</verification>

<success_criteria>
- `42-HUMAN-UAT.md` records all three live checks with `pending: 0` and summary counts that match the persisted pass/fail outcomes
- `45-HUMAN-UAT.md` exists, records the live cadence/websocket check with `pending: 0`, and includes the before/after cadence values
- Both artifacts clear `## Gaps` to `None.` only when their recorded live checks all passed; otherwise they list the concrete failed behaviors
</success_criteria>

<output>
After completion, create `.planning/phases/48-live-runtime-verification-closure/48-01-SUMMARY.md`
</output>
